---
quick: 260406-i2k
title: update deployment files for production-only app plus postgres without mailpit
status: planned
created: 2026-04-06
---

# Quick Task 260406-i2k Plan

1. Update the production deployment workflow and deployment-related repo assets so the server contract remains `~/app/app.yml`, the workflow targets the runner-reachable public host, and the repository provides operator-facing deployment inputs for a single app container on the existing external Docker network `app-network`.
2. Replace the current production compose assumptions that bundle PostgreSQL and Mailpit with a production-ready app-only compose template plus minimal env/template guidance that points Spring datasource and Liquibase settings at the user-managed PostgreSQL database named `postgres`, removes Mailpit dependencies, and preserves the existing Jib image deployment path.
3. Align production configuration and verification guidance with the no-Mailpit deployment shape by updating only the necessary deployment/config files, then validate the compose/workflow syntax and record the exact server-side expectations for image pull, env population, external network attachment, and readiness checks.
