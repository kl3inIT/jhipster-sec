---
phase: 03-secure-enforcement-core
plan: 05
type: execute
wave: 3
depends_on: [03-02, 03-03, 03-04]
files_modified:
  - src/test/java/com/vn/core/security/permission/RolePermissionServiceDbImplTest.java
  - src/test/java/com/vn/core/security/permission/AttributePermissionEvaluatorImplTest.java
  - src/test/java/com/vn/core/security/row/RowLevelPolicyProviderDbImplTest.java
  - src/test/java/com/vn/core/security/row/RowLevelSpecificationBuilderTest.java
  - src/test/java/com/vn/core/security/fetch/YamlFetchPlanRepositoryTest.java
  - src/test/java/com/vn/core/security/fetch/FetchPlanBuilderTest.java
  - src/test/java/com/vn/core/security/serialize/SecureEntitySerializerImplTest.java
  - src/test/java/com/vn/core/security/merge/SecureMergeServiceImplTest.java
  - src/test/java/com/vn/core/security/data/SecureDataManagerImplTest.java
  - src/test/java/com/vn/core/security/data/UnconstrainedDataManagerImplTest.java
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]

must_haves:
  truths:
    - "Unit tests prove DENY-wins permission semantics"
    - "Unit tests prove fail-closed row policy behavior"
    - "Unit tests prove YAML fetch plan loading and code builder"
    - "Unit tests prove attribute-filtered serialization with id always-visible"
    - "Unit tests prove merge rejects unauthorized attributes"
    - "Unit tests prove SecureDataManager orchestrates correct enforcement order"
    - "Unit tests prove UnconstrainedDataManager bypasses enforcement"
  artifacts:
    - path: "src/test/java/com/vn/core/security/permission/RolePermissionServiceDbImplTest.java"
      provides: "DENY-wins permission tests"
      contains: "assertThat"
    - path: "src/test/java/com/vn/core/security/serialize/SecureEntitySerializerImplTest.java"
      provides: "Serializer attribute filtering tests"
      contains: "canView"
    - path: "src/test/java/com/vn/core/security/data/SecureDataManagerImplTest.java"
      provides: "Data manager pipeline tests"
      contains: "AccessDeniedException"
  key_links:
    - from: "all test files"
      to: "corresponding implementation"
      via: "Mockito mocks verify enforcement behavior"
---

<objective>
Create unit tests for all Phase 3 enforcement components. Each test validates the critical behaviors locked by user decisions (DENY-wins, fail-closed, attribute filtering, enforcement order).

Purpose: These tests are the automated proof that Phase 3 enforcement works correctly. They satisfy the VALIDATION.md Wave 0 requirements and provide the `<verify>` commands for Plans 01-04.
Output: 10 unit test files covering all enforcement components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-secure-enforcement-core/03-CONTEXT.md
@.planning/phases/03-secure-enforcement-core/03-VALIDATION.md
@.planning/phases/03-secure-enforcement-core/03-01-SUMMARY.md
@.planning/phases/03-secure-enforcement-core/03-02-SUMMARY.md
@.planning/phases/03-secure-enforcement-core/03-03-SUMMARY.md
@.planning/phases/03-secure-enforcement-core/03-04-SUMMARY.md

<interfaces>
<!-- Test targets — read their implementations before writing tests -->

Test framework pattern from existing tests:
```java
// From src/test/java/com/vn/core/security/bridge/MergedSecurityContextBridgeTest.java
@ExtendWith(MockitoExtension.class)
class MergedSecurityContextBridgeTest {
    @Mock private SecurityContextBridge delegate;
    @Mock private AuthorityRepository authorityRepository;
    @InjectMocks private MergedSecurityContextBridge bridge;
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for permission evaluators and row policy components</name>
  <files>
    src/test/java/com/vn/core/security/permission/RolePermissionServiceDbImplTest.java,
    src/test/java/com/vn/core/security/permission/AttributePermissionEvaluatorImplTest.java,
    src/test/java/com/vn/core/security/row/RowLevelPolicyProviderDbImplTest.java,
    src/test/java/com/vn/core/security/row/RowLevelSpecificationBuilderTest.java
  </files>
  <read_first>
    src/main/java/com/vn/core/security/permission/RolePermissionServiceDbImpl.java,
    src/main/java/com/vn/core/security/permission/AttributePermissionEvaluatorImpl.java,
    src/main/java/com/vn/core/security/row/RowLevelPolicyProviderDbImpl.java,
    src/main/java/com/vn/core/security/row/RowLevelSpecificationBuilder.java,
    src/main/java/com/vn/core/security/permission/RolePermissionService.java,
    src/main/java/com/vn/core/security/permission/AttributePermissionEvaluator.java,
    src/main/java/com/vn/core/security/row/RowLevelPolicyProvider.java,
    src/main/java/com/vn/core/security/row/RowPolicyDefinition.java,
    src/main/java/com/vn/core/security/MergedSecurityService.java,
    src/main/java/com/vn/core/security/repository/SecPermissionRepository.java,
    src/main/java/com/vn/core/security/repository/SecRowPolicyRepository.java,
    src/main/java/com/vn/core/security/domain/SecPermission.java,
    src/main/java/com/vn/core/security/domain/SecRowPolicy.java,
    src/test/java/com/vn/core/security/bridge/MergedSecurityContextBridgeTest.java,
    angapp/src/test/java/com/mycompany/core/security/permission/AttributePermissionEvaluatorImplTest.java
  </read_first>
  <action>
    All tests use JUnit 5 + Mockito + AssertJ. Pattern: `@ExtendWith(MockitoExtension.class)`, `@Mock` for dependencies, `@InjectMocks` for the class under test.

    **1. `RolePermissionServiceDbImplTest.java`** — 5 tests minimum:
    - `testDenyWinsOverAllow()`: Mock `secPermissionRepository.findByRolesAndTarget` to return list with one ALLOW and one DENY SecPermission. Assert `hasPermission(...)` returns `false`. Verify DENY-wins per D-07.
    - `testAllowOnlyReturnsTrue()`: Mock to return single ALLOW permission. Assert returns `true`.
    - `testNoPermissionsReturnsFalse()`: Mock to return empty list. Assert returns `false`.
    - `testEmptyAuthoritiesReturnsFalse()`: Mock `mergedSecurityService.getCurrentUserAuthorityNames()` to return empty collection. Assert `isEntityOpPermitted` returns `false` without even calling repository.
    - `testEntityTargetNormalization()`: Call `isEntityOpPermitted(SomeEntity.class, EntityOp.READ)`. Verify `findByRolesAndTarget` was called with target = `"SOMEENTITY"` (upper-cased simple name).
    - Use a simple test entity inner class: `static class SomeEntity { Long id; }`

    **2. `AttributePermissionEvaluatorImplTest.java`** — 5 tests minimum:
    - `testCanViewWithNoRulesReturnsTrue()`: Mock repository to return empty list for the attribute. Assert `canView` returns `true` (permissive default).
    - `testCanViewWithDenyReturnsFalse()`: Mock to return DENY permission. Assert `canView` returns `false`.
    - `testCanViewWithAllowReturnsTrue()`: Mock to return ALLOW permission. Assert `canView` returns `true`.
    - `testCanEditWithDenyThrows()`: Mock to return DENY. Assert `canEdit` returns `false`.
    - `testTargetNormalization()`: Call `canView(SomeEntity.class, "myField")`. Verify repository was called with target = `"SOMEENTITY.MYFIELD"`.
    - Port test patterns from `angapp/src/test/.../AttributePermissionEvaluatorImplTest.java`.

    **3. `RowLevelPolicyProviderDbImplTest.java`** — 4 tests minimum:
    - `testSpecificationPolicyWithCurrentUserLogin()`: Create a `SecRowPolicy` with `policyType="SPECIFICATION"`, `expression="createdBy = CURRENT_USER_LOGIN"`. Mock `mergedSecurityService.getCurrentUserLogin()` to return `Optional.of("admin")`. Call `getPolicies`. Assert 1 `RowPolicyDefinition` returned, non-null.
    - `testUnparseableExpressionFailsClosed()`: Create a `SecRowPolicy` with `expression="INVALID GARBAGE"`. Assert calling `getPolicies` throws `AccessDeniedException` per D-13.
    - `testJavaPolicyTypeFailsClosed()`: Create a `SecRowPolicy` with `policyType="JAVA"`. Assert throws `AccessDeniedException` per D-14.
    - `testNoPoliciesReturnsEmptyList()`: Mock repository to return empty list. Assert `getPolicies` returns empty list.

    **4. `RowLevelSpecificationBuilderTest.java`** — 3 tests minimum:
    - `testNoPoliciesReturnsUnconstrainedSpec()`: Mock provider to return empty list. Call `build`. Assert returned Specification is not null (represents no constraint).
    - `testSinglePolicyReturnsItsSpec()`: Mock provider to return one `RowPolicyDefinition` with a mock Specification. Call `build`. Assert the returned spec includes that policy's spec.
    - `testMultiplePoliciesAreAndComposed()`: Mock provider to return two `RowPolicyDefinition`s. Call `build`. Verify both definitions' `getSpecification()` were called (indicating AND composition).
  </action>
  <verify>
    <automated>./gradlew test --tests "*.RolePermissionServiceDbImplTest" --tests "*.AttributePermissionEvaluatorImplTest" --tests "*.RowLevelPolicyProviderDbImplTest" --tests "*.RowLevelSpecificationBuilderTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `RolePermissionServiceDbImplTest.java` contains `testDenyWinsOverAllow` with assertion that result is `false` when both ALLOW and DENY exist
    - `RolePermissionServiceDbImplTest.java` contains `"DENY".equals` or verifies DENY-wins behavior
    - `AttributePermissionEvaluatorImplTest.java` contains `testCanViewWithNoRulesReturnsTrue` verifying permissive default
    - `AttributePermissionEvaluatorImplTest.java` contains `"SOMEENTITY.MYFIELD"` or equivalent normalized target assertion
    - `RowLevelPolicyProviderDbImplTest.java` contains `testUnparseableExpressionFailsClosed` with `assertThatThrownBy` or `assertThrows` for `AccessDeniedException`
    - `RowLevelPolicyProviderDbImplTest.java` contains `testJavaPolicyTypeFailsClosed`
    - `RowLevelSpecificationBuilderTest.java` contains `testMultiplePoliciesAreAndComposed`
    - All 4 files use `@ExtendWith(MockitoExtension.class)` and `@Mock`/`@InjectMocks`
  </acceptance_criteria>
  <done>17+ unit tests cover DENY-wins semantics, attribute target normalization, fail-closed row policy behavior, and Specification composition</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for fetch plans, serializer, merge, and data managers</name>
  <files>
    src/test/java/com/vn/core/security/fetch/YamlFetchPlanRepositoryTest.java,
    src/test/java/com/vn/core/security/fetch/FetchPlanBuilderTest.java,
    src/test/java/com/vn/core/security/serialize/SecureEntitySerializerImplTest.java,
    src/test/java/com/vn/core/security/merge/SecureMergeServiceImplTest.java,
    src/test/java/com/vn/core/security/data/SecureDataManagerImplTest.java,
    src/test/java/com/vn/core/security/data/UnconstrainedDataManagerImplTest.java
  </files>
  <read_first>
    src/main/java/com/vn/core/security/fetch/YamlFetchPlanRepository.java,
    src/main/java/com/vn/core/security/fetch/FetchPlanBuilder.java,
    src/main/java/com/vn/core/security/fetch/FetchPlan.java,
    src/main/java/com/vn/core/security/fetch/FetchPlanProperty.java,
    src/main/java/com/vn/core/security/serialize/SecureEntitySerializerImpl.java,
    src/main/java/com/vn/core/security/merge/SecureMergeServiceImpl.java,
    src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java,
    src/main/java/com/vn/core/security/data/UnconstrainedDataManagerImpl.java,
    angapp/src/test/java/com/mycompany/core/fetch/YamlFetchPlanRepositoryTest.java,
    angapp/src/test/java/com/mycompany/core/fetch/FetchPlanBuilderTest.java,
    angapp/src/test/java/com/mycompany/core/serialize/SecureEntitySerializerImplTest.java,
    src/test/java/com/vn/core/security/bridge/MergedSecurityContextBridgeTest.java
  </read_first>
  <action>
    All tests use JUnit 5 + Mockito + AssertJ.

    **1. `YamlFetchPlanRepositoryTest.java`** — Port from angapp donor test. 4 tests minimum:
    - `testLoadFromYaml()`: Create a temp YAML file with a fetch plan definition. Mock `ApplicationProperties` and `ResourceLoader` to point to it. Call `init()` then `findByEntityAndName`. Assert plan is found with correct properties.
    - `testExtendsMergesProperties()`: YAML with parent plan and child that `extends` it. Assert child has parent's properties plus its own.
    - `testMissingPlanReturnsEmpty()`: Query for non-existent plan. Assert `Optional.empty()`.
    - `testFindAllByEntity()`: Load multiple plans for same entity. Assert all returned.
    - Use `org.springframework.core.io.ByteArrayResource` or `ClassPathResource` for test YAML.

    **2. `FetchPlanBuilderTest.java`** — Port from angapp donor test. 3 tests:
    - `testBuildSimplePlan()`: Use `FetchPlanBuilder` to add scalar properties. Call `build()`. Assert plan has correct entity class, name, and property count.
    - `testBuildWithNestedPlan()`: Add a property with a nested `FetchPlan`. Assert the property's `fetchPlan()` is not null.
    - `testBuildProducesImmutableProperties()`: Build a plan, try to modify the properties list, assert `UnsupportedOperationException` or immutable list.

    **3. `SecureEntitySerializerImplTest.java`** — Port from angapp donor test. 5 tests minimum:
    - Create a test entity class with fields: `Long id`, `String name`, `String secret`, `String email`. Add getters/setters.
    - `testIdAlwaysVisible()`: Mock `canView("id")` to return `false`. Assert serialized map still contains `"id"` key per D-16.
    - `testDeniedAttributeOmitted()`: Mock `canView("secret")` to return `false`. Assert serialized map does NOT contain `"secret"` per D-15.
    - `testAllowedAttributeIncluded()`: Mock `canView("name")` to return `true`. Assert serialized map contains `"name"`.
    - `testNullEntityReturnsNull()`: Call `serialize(null, fetchPlan)`. Assert returns null.
    - `testCollectionReferenceSerializedRecursively()`: Create entity with a `List<ChildEntity>` reference. Mock `canView` for the association. Assert the list items are serialized through the nested fetch plan.

    **4. `SecureMergeServiceImplTest.java`** — 3 tests:
    - `testPermittedAttributeWritten()`: Mock `canEdit("name")` to return `true`. Call `mergeForUpdate`. Assert entity's `name` was set to the new value.
    - `testDeniedAttributeThrowsAccessDenied()`: Mock `canEdit("secret")` to return `false`. Assert `mergeForUpdate` throws `AccessDeniedException` with message containing `"secret"` per D-18.
    - `testIdAttributeSkipped()`: Include `"id"` in attributes map. Assert `canEdit` was never called for `"id"` and entity id was not changed.
    - Use the same test entity class as serializer tests.

    **5. `SecureDataManagerImplTest.java`** — 5 tests minimum:
    - Mock all 7 dependencies.
    - `testLoadByQueryEnforcesReadOrder()`: Call `loadByQuery`. Verify in order: `accessManager.applyRegisteredConstraints` called, `rowLevelSpecificationBuilder.build` called, `specRepo.findAll` called, `fetchPlanResolver.resolve` called, `serializer.serialize` called.
    - `testLoadByQueryDeniedCrudThrows()`: Set `CrudEntityContext.permitted` to `false` via `doAnswer`. Assert `loadByQuery` throws `AccessDeniedException`.
    - `testSaveEnforcesWriteOrder()`: Call `save`. Verify: CRUD check -> row-constrained lookup -> merge -> repo.save -> serialize.
    - `testDeleteEnforcesDeleteOrder()`: Call `delete`. Verify: CRUD check -> row-constrained lookup -> repo.delete.
    - `testUnknownEntityCodeThrows()`: Mock catalog to return empty. Assert throws `IllegalArgumentException`.
    - Use Mockito `InOrder` to verify call ordering.

    **6. `UnconstrainedDataManagerImplTest.java`** — 3 tests:
    - `testLoadDelegatesToRepository()`: Mock `repositoryRegistry.getRepository(SomeEntity.class)` to return a mock repo. Call `load`. Verify `repo.findById` called.
    - `testSaveDelegatesToRepository()`: Call `save`. Verify `repo.save` called.
    - `testDeleteLoadsAndDeletes()`: Call `delete`. Verify both `findById` and `delete` called.
    - Confirm no security-related mocks exist (no `AccessManager`, `RowLevel`, etc.).
  </action>
  <verify>
    <automated>./gradlew test --tests "*.YamlFetchPlanRepositoryTest" --tests "*.FetchPlanBuilderTest" --tests "*.SecureEntitySerializerImplTest" --tests "*.SecureMergeServiceImplTest" --tests "*.SecureDataManagerImplTest" --tests "*.UnconstrainedDataManagerImplTest" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `YamlFetchPlanRepositoryTest.java` contains `testLoadFromYaml` and `testExtendsMergesProperties`
    - `FetchPlanBuilderTest.java` contains `testBuildSimplePlan` and `testBuildWithNestedPlan`
    - `SecureEntitySerializerImplTest.java` contains `testIdAlwaysVisible` asserting id is present even when canView returns false
    - `SecureEntitySerializerImplTest.java` contains `testDeniedAttributeOmitted` asserting secret is absent
    - `SecureMergeServiceImplTest.java` contains `testDeniedAttributeThrowsAccessDenied` with `assertThatThrownBy` for `AccessDeniedException`
    - `SecureMergeServiceImplTest.java` contains `testIdAttributeSkipped`
    - `SecureDataManagerImplTest.java` contains `InOrder` or sequential verification of enforcement pipeline
    - `SecureDataManagerImplTest.java` contains `testLoadByQueryDeniedCrudThrows`
    - `UnconstrainedDataManagerImplTest.java` has NO imports of `AccessManager`, `RowLevel`, `FetchPlan`, or `Serializer`
    - All 6 files use `@ExtendWith(MockitoExtension.class)`
  </acceptance_criteria>
  <done>23+ unit tests cover all Phase 3 enforcement components, satisfying VALIDATION.md Wave 0 requirements</done>
</task>

</tasks>

<verification>
- All 10 test files compile and pass with `./gradlew test`
- DENY-wins is tested explicitly
- Fail-closed row policy is tested explicitly
- Attribute filtering (read) and rejection (write) are tested
- Enforcement order is verified with Mockito InOrder
- Unconstrained bypass is verified to have no security calls
</verification>

<success_criteria>
- `./gradlew test --tests "*.security.*Test"` passes with 0 failures
- At least 40 unit tests across 10 test files
- Every DATA-01 through DATA-05 requirement has at least one test covering it
- Validation map items 3-W0-01 through 3-W0-10 are addressed
</success_criteria>

<output>
After completion, create `.planning/phases/03-secure-enforcement-core/03-05-SUMMARY.md`
</output>
