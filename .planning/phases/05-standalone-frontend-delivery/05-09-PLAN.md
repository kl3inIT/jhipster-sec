---
phase: 05-standalone-frontend-delivery
plan: 09
type: execute
wave: 4
depends_on: ["05-03", "05-07"]
files_modified:
  - frontend/src/app/pages/entities/organization/update/organization-update.component.ts
  - frontend/src/app/pages/entities/organization/update/organization-update.component.html
  - frontend/src/app/pages/entities/organization/update/organization-update.component.spec.ts
  - frontend/src/app/pages/entities/department/update/department-update.component.ts
  - frontend/src/app/pages/entities/department/update/department-update.component.html
  - frontend/src/app/pages/entities/employee/update/employee-update.component.ts
  - frontend/src/app/pages/entities/employee/update/employee-update.component.html
  - frontend/src/app/pages/entities/employee/update/employee-update.component.spec.ts
autonomous: true
requirements: [ENT-03]
gap_closure: true
must_haves:
  truths:
    - "Denied create and edit routes redirect to `/accessdenied` instead of exposing the form."
    - "Budget and salary inputs stay hidden until capability data loads and the user has explicit attribute edit permission."
  artifacts:
    - path: "frontend/src/app/pages/entities/organization/update/organization-update.component.ts"
      provides: "Capability-driven create/edit route gating and budget-field gating"
    - path: "frontend/src/app/pages/entities/employee/update/employee-update.component.ts"
      provides: "Capability-driven create/edit route gating and salary-field gating"
  key_links:
    - from: "organization-update.component.ts"
      to: "secured-entity-capability.service.ts"
      via: "organization capability lookup before rendering the form"
      pattern: "getEntityCapability('organization')"
    - from: "employee-update.component.html"
      to: "employee-update.component.ts"
      via: "salary field rendered only when attribute edit capability is true"
      pattern: "salary"
---

<objective>
Use the shared capability service in the update screens so denied create/edit routes are blocked and sensitive form fields stay hidden until explicitly allowed.

Purpose: `05-VERIFICATION.md` identifies the remaining ENT-03 gap in the create/edit flow: new Organization and Employee forms expose gated inputs before capability is known.
Output: Capability-gated update screens for Organization, Department, and Employee.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-standalone-frontend-delivery/05-VERIFICATION.md
@.planning/phases/05-standalone-frontend-delivery/05-07-PLAN.md
</context>

<verification_evidence>
- `05-VERIFICATION.md` names `showBudgetField.set(true)` and `showSalaryField.set(true)` as the reason gated inputs are exposed before permission state is known.
- The same missing capability source also leaves denied create/edit routes unguarded at the page level.
</verification_evidence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate create/edit routes and sensitive form inputs from capability data</name>
  <files>
    frontend/src/app/pages/entities/organization/update/organization-update.component.ts,
    frontend/src/app/pages/entities/organization/update/organization-update.component.html,
    frontend/src/app/pages/entities/organization/update/organization-update.component.spec.ts,
    frontend/src/app/pages/entities/department/update/department-update.component.ts,
    frontend/src/app/pages/entities/department/update/department-update.component.html,
    frontend/src/app/pages/entities/employee/update/employee-update.component.ts,
    frontend/src/app/pages/entities/employee/update/employee-update.component.html,
    frontend/src/app/pages/entities/employee/update/employee-update.component.spec.ts
  </files>
  <read_first>
    frontend/src/app/pages/entities/shared/service/secured-entity-capability.service.ts,
    frontend/src/app/pages/entities/organization/update/organization-update.component.ts,
    frontend/src/app/pages/entities/organization/update/organization-update.component.html,
    frontend/src/app/pages/entities/department/update/department-update.component.ts,
    frontend/src/app/pages/entities/department/update/department-update.component.html,
    frontend/src/app/pages/entities/employee/update/employee-update.component.ts,
    frontend/src/app/pages/entities/employee/update/employee-update.component.html,
    .planning/phases/05-standalone-frontend-delivery/05-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: Organization update hides the budget input until capability loads and only renders it when attribute `budget` has `canEdit=true`.
    - Test 2: Employee update hides the salary input until capability loads and only renders it when attribute `salary` has `canEdit=true`.
    - Test 3: Denied create or update routes navigate to `/accessdenied`.
  </behavior>
  <action>
Update all three update components to load capability data before exposing the form.

For `organization-update.component.ts`:
- Load `organization` capability on init.
- While capability is loading, do not render the form body.
- On `/entities/organization/new`, redirect to `/accessdenied` when `canCreate` is false.
- On `/entities/organization/:id/edit`, redirect to `/accessdenied` when `canUpdate` is false.
- Remove the current `showBudgetField.set(true)` new-mode behavior.
- Show the budget input only when attribute `budget` has `canEdit === true`.

For `employee-update.component.ts`:
- Mirror the same behavior for `employee`.
- Remove `showSalaryField.set(true)`.
- Show the salary input only when attribute `salary` has `canEdit === true`.

For `department-update.component.ts`:
- Load `department` capability and apply the same `canCreate` / `canUpdate` redirects.
- Keep the form hidden until capability data is loaded so the route does not briefly expose controls before redirecting.

Add `organization-update.component.spec.ts` covering hidden/present budget input for `canEdit=false` and `canEdit=true`.

Add `employee-update.component.spec.ts` covering hidden/present salary input and one redirect assertion to `/accessdenied`.
  </action>
  <verify>
    <automated>cmd /c "cd frontend && npx ng test --watch=false"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/pages/entities/organization/update/organization-update.component.ts` no longer contains `showBudgetField.set(true)`.
    - `frontend/src/app/pages/entities/employee/update/employee-update.component.ts` no longer contains `showSalaryField.set(true)`.
    - `frontend/src/app/pages/entities/organization/update/organization-update.component.ts` contains `/accessdenied`.
    - `frontend/src/app/pages/entities/department/update/department-update.component.ts` contains `/accessdenied`.
    - `frontend/src/app/pages/entities/employee/update/employee-update.component.ts` contains `/accessdenied`.
    - `frontend/src/app/pages/entities/organization/update/organization-update.component.html` gates `id="budget"` behind capability state.
    - `frontend/src/app/pages/entities/employee/update/employee-update.component.html` gates `id="salary"` behind capability state.
    - `frontend/src/app/pages/entities/organization/update/organization-update.component.spec.ts` exists.
    - `frontend/src/app/pages/entities/employee/update/employee-update.component.spec.ts` exists.
    - `cmd /c "cd frontend && npx ng test --watch=false"` passes.
  </acceptance_criteria>
  <done>Create/edit routes redirect to `/accessdenied` when the user lacks entity capability, and sensitive budget/salary inputs remain hidden until capability data says the user may edit them.</done>
</task>

</tasks>

<verification>
- `cmd /c "cd frontend && npx ng test --watch=false"` passes.
- Organization and Employee update components no longer force gated inputs visible in new mode.
- Create/edit routes navigate to `/accessdenied` when capability denies the action.
</verification>

<success_criteria>
- Denied create/edit routes do not expose the form.
- Budget and salary inputs stay hidden until capability data confirms edit permission.
</success_criteria>

<output>
After completion, create `.planning/phases/05-standalone-frontend-delivery/05-09-SUMMARY.md`
</output>
