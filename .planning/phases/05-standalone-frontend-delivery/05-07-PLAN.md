---
phase: 05-standalone-frontend-delivery
plan: 07
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - frontend/src/app/pages/entities/shared/secured-entity-capability.model.ts
  - frontend/src/app/pages/entities/shared/service/secured-entity-capability.service.ts
  - frontend/src/app/pages/entities/shared/service/secured-entity-capability.service.spec.ts
  - src/main/java/com/vn/core/service/dto/security/SecuredAttributeCapabilityDTO.java
  - src/main/java/com/vn/core/service/dto/security/SecuredEntityCapabilityDTO.java
  - src/main/java/com/vn/core/service/security/SecuredEntityCapabilityService.java
  - src/main/java/com/vn/core/web/rest/SecuredEntityCapabilityResource.java
  - src/test/java/com/vn/core/web/rest/SecuredEntityCapabilityResourceIT.java
autonomous: true
requirements: [ENT-03]
gap_closure: true
must_haves:
  truths:
    - "The backend exposes current-user entity and attribute capabilities for the secured entities."
    - "The frontend has one cached service that can resolve capability data for `organization`, `department`, and `employee`."
  artifacts:
    - path: "src/main/java/com/vn/core/web/rest/SecuredEntityCapabilityResource.java"
      provides: "Authenticated capability endpoint"
    - path: "src/main/java/com/vn/core/service/security/SecuredEntityCapabilityService.java"
      provides: "Capability aggregation from entity and attribute evaluators"
    - path: "frontend/src/app/pages/entities/shared/service/secured-entity-capability.service.ts"
      provides: "Cached frontend capability lookup"
  key_links:
    - from: "SecuredEntityCapabilityResource.java"
      to: "SecuredEntityCapabilityService.java"
      via: "GET /api/security/entity-capabilities"
      pattern: "entity-capabilities"
    - from: "SecuredEntityCapabilityService.java"
      to: "EntityPermissionEvaluator.java"
      via: "create/read/update/delete evaluation"
      pattern: "isPermitted"
    - from: "secured-entity-capability.service.ts"
      to: "SecuredEntityCapabilityResource.java"
      via: "ApplicationConfigService.getEndpointFor('api/security/entity-capabilities')"
      pattern: "api/security/entity-capabilities"
---

<objective>
Create the shared capability contract that the protected-entity screens will consume for ENT-03 gating.

Purpose: The verifier explicitly called for a permission-aware capability source. This plan creates the backend endpoint and the shared frontend client, without mixing that foundation with the actual screen gating work.
Output: One authenticated backend capability endpoint plus one cached frontend capability service.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-standalone-frontend-delivery/05-CONTEXT.md
@.planning/phases/05-standalone-frontend-delivery/05-VERIFICATION.md
@.planning/phases/05-standalone-frontend-delivery/05-03-SUMMARY.md

<interfaces>
From `EntityPermissionEvaluator.java`:
```java
boolean isPermitted(Class<?> entityClass, EntityOp op);
```

From `AttributePermissionEvaluator.java`:
```java
boolean canView(Class<?> entityClass, String attribute);
boolean canEdit(Class<?> entityClass, String attribute);
```

From `MetamodelSecuredEntityCatalog.java`:
```java
entryFor(Organization.class, "organization", ...)
entryFor(Department.class, "department", ...)
entryFor(Employee.class, "employee", ...)
```
</interfaces>
</context>

<verification_evidence>
- `05-VERIFICATION.md` requires a permission-aware capability source for action-button visibility and field gating.
- No current frontend entity screen imports a capability source.
</verification_evidence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an authenticated current-user capability endpoint</name>
  <files>
    src/main/java/com/vn/core/service/dto/security/SecuredAttributeCapabilityDTO.java,
    src/main/java/com/vn/core/service/dto/security/SecuredEntityCapabilityDTO.java,
    src/main/java/com/vn/core/service/security/SecuredEntityCapabilityService.java,
    src/main/java/com/vn/core/web/rest/SecuredEntityCapabilityResource.java,
    src/test/java/com/vn/core/web/rest/SecuredEntityCapabilityResourceIT.java
  </files>
  <read_first>
    src/main/java/com/vn/core/security/catalog/SecuredEntityCatalog.java,
    src/main/java/com/vn/core/security/catalog/MetamodelSecuredEntityCatalog.java,
    src/main/java/com/vn/core/security/permission/EntityPermissionEvaluator.java,
    src/main/java/com/vn/core/security/permission/AttributePermissionEvaluator.java,
    src/test/resources/config/liquibase/changelog/20260321000800_seed_proof_security_test_data.xml
  </read_first>
  <behavior>
    - Test 1: `GET /api/security/entity-capabilities` as `ROLE_PROOF_READER` returns `organization` with `canRead=true`, `canCreate=false`, `canUpdate=false`, `canDelete=false`, and attribute `budget` with `canView=false`.
    - Test 2: The same endpoint as `ROLE_PROOF_EDITOR` returns `organization` with `canCreate=true`, `canRead=true`, `canUpdate=true`, `canDelete=true`, and attribute `budget` with `canEdit=false`.
    - Test 3: The same endpoint as `ROLE_PROOF_NONE` returns `organization` with all four entity action booleans false.
  </behavior>
  <action>
Add `SecuredAttributeCapabilityDTO.java` with fields `name`, `canView`, and `canEdit`.

Add `SecuredEntityCapabilityDTO.java` with fields `code`, `canCreate`, `canRead`, `canUpdate`, `canDelete`, and `List<SecuredAttributeCapabilityDTO> attributes`.

Add `SecuredEntityCapabilityService.java` that injects `SecuredEntityCatalog`, `EntityPermissionEvaluator`, `AttributePermissionEvaluator`, and `EntityManager`, then builds one DTO per secured catalog entry using:
- `entry.code()` for the response code
- `entityPermissionEvaluator.isPermitted(entry.entityClass(), EntityOp.CREATE|READ|UPDATE|DELETE)`
- JPA metamodel attribute enumeration
- `attributePermissionEvaluator.canView/canEdit`

Add `SecuredEntityCapabilityResource.java` at `GET /api/security/entity-capabilities` with `@PreAuthorize("isAuthenticated()")`.

Add `SecuredEntityCapabilityResourceIT.java` covering the three role cases above.
  </action>
  <verify>
    <automated>.\gradlew integrationTest --tests "com.vn.core.web.rest.SecuredEntityCapabilityResourceIT"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/vn/core/web/rest/SecuredEntityCapabilityResource.java` contains `/api/security/entity-capabilities`.
    - `src/main/java/com/vn/core/service/security/SecuredEntityCapabilityService.java` contains `EntityOp.CREATE`, `EntityOp.READ`, `EntityOp.UPDATE`, and `EntityOp.DELETE`.
    - `src/main/java/com/vn/core/service/security/SecuredEntityCapabilityService.java` contains `canView(` and `canEdit(`.
    - `src/test/java/com/vn/core/web/rest/SecuredEntityCapabilityResourceIT.java` exists.
    - `SecuredEntityCapabilityResourceIT.java` contains `ROLE_PROOF_READER`, `ROLE_PROOF_EDITOR`, and `ROLE_PROOF_NONE`.
    - `.\gradlew integrationTest --tests "com.vn.core.web.rest.SecuredEntityCapabilityResourceIT"` passes.
  </acceptance_criteria>
  <done>The backend exposes current-user entity and attribute capabilities for `organization`, `department`, and `employee`, and integration tests prove the response changes across reader, editor, and none roles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a cached frontend shared capability client</name>
  <files>
    frontend/src/app/pages/entities/shared/secured-entity-capability.model.ts,
    frontend/src/app/pages/entities/shared/service/secured-entity-capability.service.ts,
    frontend/src/app/pages/entities/shared/service/secured-entity-capability.service.spec.ts
  </files>
  <read_first>
    frontend/src/app/core/config/application-config.service.ts,
    frontend/src/app/pages/entities/organization/service/organization.service.ts,
    frontend/package.json,
    src/main/java/com/vn/core/web/rest/SecuredEntityCapabilityResource.java
  </read_first>
  <behavior>
    - Test 1: `getEntityCapability('organization')` returns the `organization` entry from a mocked capability list.
    - Test 2: Repeated subscriptions reuse one cached HTTP response via `shareReplay(1)`.
  </behavior>
  <action>
Add `secured-entity-capability.model.ts` with:
- `ISecuredAttributeCapability { name: string; canView: boolean; canEdit: boolean }`
- `ISecuredEntityCapability { code: string; canCreate: boolean; canRead: boolean; canUpdate: boolean; canDelete: boolean; attributes: ISecuredAttributeCapability[] }`

Add `secured-entity-capability.service.ts`:
- `resourceUrl = inject(ApplicationConfigService).getEndpointFor('api/security/entity-capabilities')`
- `query(): Observable<ISecuredEntityCapability[]>`
- `getEntityCapability(code: string): Observable<ISecuredEntityCapability | null>`
- Cache `query()` with `shareReplay(1)` so all protected-entity screens reuse one response per session

Add `secured-entity-capability.service.spec.ts` that mocks the HTTP layer and verifies the `organization` lookup plus one-request caching behavior.
  </action>
  <verify>
    <automated>cmd /c "cd frontend && npx ng test --watch=false"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/pages/entities/shared/service/secured-entity-capability.service.ts` contains `api/security/entity-capabilities`.
    - `secured-entity-capability.service.ts` contains `shareReplay`.
    - `frontend/src/app/pages/entities/shared/service/secured-entity-capability.service.spec.ts` exists.
    - `secured-entity-capability.service.spec.ts` contains `getEntityCapability('organization')`.
    - `cmd /c "cd frontend && npx ng test --watch=false"` passes.
  </acceptance_criteria>
  <done>The frontend has one cached shared capability client ready for all protected-entity screens to consume.</done>
</task>

</tasks>

<verification>
- `.\gradlew integrationTest --tests "com.vn.core.web.rest.SecuredEntityCapabilityResourceIT"` passes.
- `cmd /c "cd frontend && npx ng test --watch=false"` passes.
- Backend capability endpoint and frontend shared client are both present.
</verification>

<success_criteria>
- Current-user capability data exists at a stable authenticated endpoint.
- The frontend can query `organization`, `department`, and `employee` capabilities from one cached service.
</success_criteria>

<output>
After completion, create `.planning/phases/05-standalone-frontend-delivery/05-07-SUMMARY.md`
</output>
