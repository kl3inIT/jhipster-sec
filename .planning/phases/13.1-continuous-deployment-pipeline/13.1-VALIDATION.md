---
phase: 13.1
slug: continuous-deployment-pipeline
status: ready
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-06
---

# Phase 13.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | GitHub Actions workflow linting (actionlint) + manual smoke test |
| **Config file** | `.github/workflows/deploy.yml` — new file |
| **Quick run command** | `actionlint .github/workflows/deploy.yml` (if actionlint installed) |
| **Full suite command** | `actionlint .github/workflows/` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Validate YAML syntax and workflow structure
- **After every plan wave:** Run `actionlint .github/workflows/`
- **Before `/gsd-verify-work`:** Full suite must be green + manual trigger test confirmed
- **Max feedback latency:** ~5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 13.1-01-01 | 01 | 1 | D-03/D-13 | T-01 | workflow_run only triggers on main + success | lint | `actionlint .github/workflows/deploy.yml` | ✅ | ⬜ pending |
| 13.1-01-02 | 01 | 1 | D-06/D-07 | T-02 | Jib uses GITHUB_TOKEN, no hardcoded creds | lint | `actionlint .github/workflows/deploy.yml` | ✅ | ⬜ pending |
| 13.1-01-03 | 01 | 1 | D-08/D-09 | T-03 | SSH key from secret, not plaintext | lint | `actionlint .github/workflows/deploy.yml` | ✅ | ⬜ pending |
| 13.1-01-04 | 01 | 1 | D-11/D-12 | — | health check fails job on timeout | lint | `actionlint .github/workflows/deploy.yml` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements.

*This phase creates a single `.github/workflows/deploy.yml` file. No test framework installation needed. Validation is via actionlint (optional) and manual workflow trigger.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Workflow triggers after Backend CI success on main | D-03 | Requires push to main + CI completion | Push to main, verify deploy workflow starts in GitHub Actions UI |
| GHCR image published with :latest and :sha-XXXXXXX tags | D-05 | Requires actual push to GHCR | Check https://github.com/kl3inIT/jhipster-sec/pkgs/container/jhipster-sec after workflow run |
| SSH deploy runs docker compose pull + up -d | D-08 | Requires live server + secrets | Inspect workflow run logs for SSH step output |
| Health check endpoint returns 200 within 60s | D-11 | Requires deployed container + network reachability | Inspect workflow run logs for health check step |
| Workflow does NOT trigger when Backend CI fails | D-03 | Requires a failing CI run | Introduce a failing test, push to main, verify deploy workflow does not start |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 10s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-04-06
