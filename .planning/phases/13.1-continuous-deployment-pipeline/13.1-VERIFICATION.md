---
phase: 13.1-continuous-deployment-pipeline
verified: 2026-04-06T04:42:09Z
status: human_needed
score: 4/4 must-haves verified
human_verification:
  - test: "Backend CI success on main triggers Continuous Deployment automatically"
    expected: "A successful main-branch run of Backend CI creates a separate Continuous Deployment workflow_run execution, and failed Backend CI runs do not deploy."
    why_human: "workflow_run dispatch behavior must be observed in GitHub Actions against the default branch; static file inspection cannot prove the live event wiring end to end."
  - test: "GHCR publish produces latest and sha-tagged images for the CI-validated SHA"
    expected: "The workflow publishes ghcr.io/<owner>/jhipster-sec:latest and ghcr.io/<owner>/jhipster-sec:sha-<short_sha> for github.event.workflow_run.head_sha."
    why_human: "Registry publication requires a real Actions run and GHCR inspection; static code only proves intent, not successful push/auth in the live repository."
  - test: "SSH deployment reaches the production host and readiness passes within 60 seconds"
    expected: "The deploy step connects with DEPLOY_SSH_KEY, runs docker compose pull && up -d on the server, and the readiness probe returns HTTP 200 before timeout."
    why_human: "This depends on real repository secrets, server GHCR login, network reachability through VPN/Nginx Proxy Manager, and the production host state."
---

# Phase 13.1: Continuous Deployment Pipeline Verification Report

**Phase Goal:** Add a separate continuous deployment workflow that publishes the Jib-built backend image to GHCR and SSH-deploys to production only after Backend CI succeeds on main, then verifies readiness.
**Verified:** 2026-04-06T04:42:09Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Successful Backend CI runs on main trigger a separate Continuous Deployment workflow instead of embedding deploy logic in backend.yml. | ✓ VERIFIED | `D:/jhipster/.github/workflows/deploy.yml` is a distinct workflow named `Continuous Deployment` using `on.workflow_run.workflows: ["Backend CI"]`, `types: [completed]`, `branches: [main]`, and a job guard `github.event.workflow_run.conclusion == 'success'`. `D:/jhipster/.github/workflows/backend.yml` remains a separate CI-only workflow with no deploy job. |
| 2 | The deploy workflow builds and pushes the backend image to GHCR as both latest and sha-<short_sha> using the exact commit SHA that passed Backend CI. | ✓ VERIFIED | `deploy.yml` checks out `ref: ${{ github.event.workflow_run.head_sha }}` and runs `./gradlew -Pprod jib` with `-Djib.to.image=ghcr.io/${{ github.repository_owner }}/jhipster-sec:latest`, `-Djib.to.tags=sha-${SHORT_SHA}`, and GHCR auth from `secrets.GITHUB_TOKEN`. This overrides the local default image `jhipster-sec:latest` defined in `D:/jhipster/buildSrc/src/main/groovy/jhipster.docker-conventions.gradle`. |
| 3 | After the image push, the deploy workflow connects to the production server over SSH, runs docker compose pull and docker compose up -d against the existing app.yml manifest, and fails if the deployment does not become ready within 60 seconds. | ✓ VERIFIED | `deploy.yml` uses `webfactory/ssh-agent@v0.9.0`, `ssh-keyscan -H ${{ secrets.DEPLOY_HOST }}`, and raw `ssh deploy@${{ secrets.DEPLOY_HOST }}` to run `docker compose -f ~/app/app.yml pull && docker compose -f ~/app/app.yml up -d`, then polls `http://${{ secrets.DEPLOY_HOST }}/management/health/readiness` with `timeout 60 bash -c 'until curl -sf ...; do sleep 3; done'`. |
| 4 | The workflow declares the exact repository secrets and server-side prerequisites needed for the first production deployment without modifying backend.yml, frontend.yml, or prod-validation.yml. | ✓ VERIFIED | `deploy.yml` documents `DEPLOY_HOST` and `DEPLOY_SSH_KEY`, server GHCR login, Docker Compose access, compose file path, topology/reachability, and host verification prerequisites. Git diff shows no edits to `D:/jhipster/.github/workflows/backend.yml`, `D:/jhipster/.github/workflows/frontend.yml`, or `D:/jhipster/.github/workflows/prod-validation.yml`. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `D:/jhipster/.github/workflows/deploy.yml` | Workflow-run based continuous deployment lane for GHCR publish, SSH deploy, and readiness verification | ✓ VERIFIED | Exists, is substantive, and contains the full trigger, Jib publish, SSH deploy, prerequisite notes, and fail-closed readiness logic. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `deploy.yml` | `backend.yml` | `workflow_run` on `Backend CI` plus success guard | ✓ WIRED | Exact workflow name match confirmed in `backend.yml` (`name: Backend CI`), with branch filtering to `main` and conclusion guard in `deploy.yml`. |
| `deploy.yml` | `buildSrc/src/main/groovy/jhipster.docker-conventions.gradle` | `./gradlew -Pprod jib` overriding `jib.to.image` and `jib.to.tags` | ✓ WIRED | Build convention defaults to `jhipster-sec:latest`; deploy workflow overrides target image to GHCR and adds SHA tag using the CI-validated SHA. |
| `deploy.yml` | `src/main/docker/app.yml` | remote `docker compose -f ~/app/app.yml pull && up -d` | ✓ WIRED | Workflow deploys using the same compose manifest contract and image name expected by `src/main/docker/app.yml` (`image: jhipster-sec:latest`) via the documented server-side copy at `~/app/app.yml`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `D:/jhipster/.github/workflows/deploy.yml` | `github.event.workflow_run.head_sha`, `secrets.DEPLOY_HOST` | GitHub Actions event payload and repository secrets | Yes, if live workflow run occurs with configured secrets | ✓ FLOWING by design; live proof still requires human execution |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Workflow file contains required CD trigger/build/deploy structure | Static file inspection and pattern checks | Required trigger, checkout SHA, Jib push, SSH deploy, and readiness timeout all present | ✓ PASS |
| Existing CI workflows remain unchanged | `git -C "D:/jhipster" diff --name-only -- .github/workflows/backend.yml .github/workflows/frontend.yml .github/workflows/prod-validation.yml .github/workflows/deploy.yml` | No changes reported to backend/frontend/prod-validation workflows | ✓ PASS |
| Live GitHub Actions / GHCR / production deployment behavior | N/A | Cannot be exercised safely without real secrets and infrastructure | ? SKIP |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| D-01 | `13.1-01-PLAN.md` | Publish to GHCR using `GITHUB_TOKEN` | ✓ SATISFIED | `deploy.yml` pushes to `ghcr.io/${{ github.repository_owner }}/jhipster-sec:latest` with `-Djib.to.auth.password=${{ secrets.GITHUB_TOKEN }}` and `packages: write`. |
| D-02 | `13.1-01-PLAN.md` | Do not use Docker Hub | ✓ SATISFIED | No Docker Hub references; only GHCR path is present. |
| D-03 | `13.1-01-PLAN.md` | Trigger from `Backend CI` completion on `main` and guard on success | ✓ SATISFIED | `workflow_run` + `branches: [main]` + `github.event.workflow_run.conclusion == 'success'`. |
| D-04 | `13.1-01-PLAN.md` | Keep CI and CD in separate workflow files | ✓ SATISFIED | New separate `deploy.yml`; no deploy logic added to `backend.yml`. |
| D-05 | `13.1-01-PLAN.md` | Push `latest` and `sha-<short_sha>` tags | ✓ SATISFIED | `-Djib.to.image=...:latest` and `-Djib.to.tags=sha-${SHORT_SHA}` after shortening `head_sha`. |
| D-06 | `13.1-01-PLAN.md` | Use `./gradlew -Pprod jib` | ✓ SATISFIED | Exact command present. |
| D-07 | `13.1-01-PLAN.md` | Override Jib target image/auth for GHCR | ✓ SATISFIED | `jib.to.image`, `jib.to.auth.username`, and `jib.to.auth.password` overrides present. |
| D-08 | `13.1-01-PLAN.md` | SSH deploy using docker compose pull/up against app manifest | ✓ SATISFIED | Raw SSH command runs `docker compose -f ~/app/app.yml pull && docker compose -f ~/app/app.yml up -d`. |
| D-09 | `13.1-01-PLAN.md` | Use `DEPLOY_SSH_KEY` and `DEPLOY_HOST` with ssh-agent/ssh-keyscan/ssh | ✓ SATISFIED | `webfactory/ssh-agent`, `ssh-keyscan`, and `ssh deploy@${{ secrets.DEPLOY_HOST }}` present. |
| D-10 | `13.1-01-PLAN.md` | Document server-side Docker Compose/app.yml prerequisites | ✓ SATISFIED | Operator prerequisite step lists Docker Compose access, GHCR login, and `~/app/app.yml`. |
| D-11 | `13.1-01-PLAN.md` | Fail on readiness timeout after deploy | ✓ SATISFIED | `timeout 60 bash -c 'until curl -sf ...'` fails closed on timeout. |
| D-12 | `13.1-01-PLAN.md` | Build readiness URL from `DEPLOY_HOST` | ✓ SATISFIED | Health URL uses `http://${{ secrets.DEPLOY_HOST }}/management/health/readiness`. |
| D-13 | `13.1-01-PLAN.md` | New file named `Continuous Deployment` | ✓ SATISFIED | `.github/workflows/deploy.yml` exists with `name: Continuous Deployment`. |
| D-14 | `13.1-01-PLAN.md` | Do not modify `backend.yml`, `frontend.yml`, or `prod-validation.yml` | ✓ SATISFIED | Static inspection and git diff show no changes to those files. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| None | — | No TODO/FIXME placeholders, empty stub logic, rollback/staging/watchtower additions, or forbidden CI workflow edits found in the deliverable | — | — |

### Human Verification Required

### 1. Backend CI success triggers Continuous Deployment

**Test:** Merge or push a commit to `main` that causes `Backend CI` to pass, then inspect the GitHub Actions UI.
**Expected:** A separate `Continuous Deployment` run starts from the `workflow_run` event; if Backend CI fails, no deployment job runs.
**Why human:** GitHub evaluates `workflow_run` behavior on the live default branch; static YAML review cannot prove the event fires correctly in production.

### 2. GHCR publication proves the exact tags were pushed

**Test:** Inspect the resulting workflow logs and the GHCR package after a successful deployment run.
**Expected:** The package contains `latest` and `sha-<short_sha>` matching the triggering `github.event.workflow_run.head_sha`.
**Why human:** Successful push/auth and package visibility require a real workflow run and registry access.

### 3. Production deploy and readiness succeed against real infrastructure

**Test:** Observe a full deployment run with configured `DEPLOY_HOST` and `DEPLOY_SSH_KEY`, then inspect the server and workflow logs.
**Expected:** SSH connects, `docker compose pull && up -d` completes on `~/app/app.yml`, and readiness returns HTTP 200 within 60 seconds.
**Why human:** This depends on real secrets, server GHCR pull credentials, reachable ingress/VPN topology, and a live production host.

### Gaps Summary

No code-level implementation gaps were found in the Phase 13.1 deliverable. The workflow is present, substantive, separately wired from Backend CI, builds the CI-validated SHA, pushes GHCR tags, deploys over SSH, documents operator prerequisites, and fails closed on readiness timeout. Final proof still requires live GitHub Actions and production-environment verification, so the phase cannot be marked fully passed through static analysis alone.

---

_Verified: 2026-04-06T04:42:09Z_
_Verifier: Claude (gsd-verifier)_