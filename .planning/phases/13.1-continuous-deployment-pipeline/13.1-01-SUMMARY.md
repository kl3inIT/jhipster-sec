---
phase: 13.1-continuous-deployment-pipeline
plan: 01
subsystem: infra
tags: [github-actions, ghcr, jib, ssh, docker-compose, deployment]
requires:
  - phase: 13-split-ci-verification-lanes
    provides: Backend CI workflow named Backend CI for workflow_run triggering
provides:
  - workflow_run-based continuous deployment lane for backend image publishing
  - GHCR latest and sha-tag image publication from the CI-validated commit
  - SSH deployment and readiness-gated production verification
affects: [phase-14-benchmark-baseline, operations, production-deployments]
tech-stack:
  added: [GitHub Actions workflow_run CD workflow]
  patterns: [build exact CI-validated SHA with Jib, raw SSH deploy via webfactory/ssh-agent, fail-closed readiness polling]
key-files:
  created: [.github/workflows/deploy.yml]
  modified: []
key-decisions:
  - "Use workflow_run on Backend CI plus a success guard so CD stays separate from backend.yml and only deploys validated main commits."
  - "Use github.repository_owner with GITHUB_TOKEN for GHCR auth and push latest plus sha-<short_sha> tags from ./gradlew -Pprod jib."
  - "Use webfactory/ssh-agent, ssh-keyscan, and raw ssh to deploy with docker compose -f ~/app/app.yml pull && up -d, then fail closed on a 60-second readiness timeout."
patterns-established:
  - "Continuous Deployment workflow checks out github.event.workflow_run.head_sha before building deployment artifacts."
  - "Production deploy workflows must document operator prerequisites inline when external secrets and server setup are required."
requirements-completed: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14]
duration: 14min
completed: 2026-04-06
---

# Phase 13.1 Plan 01: Continuous Deployment Pipeline Summary

**GitHub Actions continuous deployment lane that builds the CI-validated backend commit with Jib, publishes latest and sha-tagged images to GHCR, deploys over SSH, and blocks on readiness.**

## Performance

- **Duration:** 14 min
- **Started:** 2026-04-06T04:23:00Z
- **Completed:** 2026-04-06T04:37:17Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Added `.github/workflows/deploy.yml` as a new `Continuous Deployment` workflow triggered by `workflow_run` from `Backend CI` on `main` only.
- Configured `./gradlew -Pprod jib` to push `ghcr.io/${{ github.repository_owner }}/jhipster-sec:latest` and `sha-${SHORT_SHA}` using the exact `github.event.workflow_run.head_sha` commit.
- Added SSH deployment, host verification, inline operator prerequisites, and a fail-closed 60-second readiness probe against `/management/health/readiness`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Author the workflow_run-based GHCR build and publish lane** - `ffb298d` (feat)
2. **Task 2: Add the SSH deploy, readiness gate, and explicit operator prerequisites** - `8cc276b` (feat)

**Plan metadata:** Captured in the final docs/state commit for this plan.

## Files Created/Modified
- `D:/jhipster/.github/workflows/deploy.yml` - Continuous Deployment workflow that publishes the backend image to GHCR, deploys it over SSH, and verifies readiness.

## Decisions Made
- Used the exact `Backend CI` workflow name with `workflow_run` and a job-level success guard to preserve CI/CD separation and fail closed.
- Used `github.repository_owner` rather than `github.actor` for GHCR authentication to keep the registry path and auth user aligned with the repository owner.
- Used `webfactory/ssh-agent` plus explicit `ssh-keyscan` and raw `ssh` because the plan required that deployment shape instead of a higher-level SSH action.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- `actionlint` was not installed in the execution environment, so workflow linting could not run. YAML parsing and both task-specific Python structural verification scripts passed instead.
- The GSD init command was executed from the spawned worktree environment and returned worktree-local project_root metadata instead of the main tree path, but execution proceeded against `D:/jhipster` as requested.

## User Setup Required
None - no external service configuration file was generated.

Operator-owned prerequisites are documented inline in `D:/jhipster/.github/workflows/deploy.yml`:
- Configure `DEPLOY_HOST` and `DEPLOY_SSH_KEY` repository secrets.
- Ensure the production server is already logged into `ghcr.io` for private image pulls.
- Ensure the `deploy` user can run `docker compose` and that `~/app/app.yml` exists.
- Ensure `DEPLOY_HOST` is reachable from the GitHub runner through the actual VPN / Nginx Proxy Manager topology for readiness checks.

## Next Phase Readiness
- Phase 13.1 now provides a repository-native production CD lane for later benchmark and production-proof phases.
- First live proof still requires a successful `Backend CI` run on `main` with the required secrets and server prerequisites in place.

## Self-Check: PASSED
- Verified `D:/jhipster/.github/workflows/deploy.yml` exists.
- Verified `D:/jhipster/.planning/phases/13.1-continuous-deployment-pipeline/13.1-01-SUMMARY.md` exists.
- Verified task commits `ffb298d` and `8cc276b` exist in git history.

---
*Phase: 13.1-continuous-deployment-pipeline*
*Completed: 2026-04-06*
