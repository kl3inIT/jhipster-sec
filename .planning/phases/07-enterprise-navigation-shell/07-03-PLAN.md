---
phase: 07-enterprise-navigation-shell
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - frontend/src/app/core/auth/user-route-access.service.ts
  - frontend/src/app/core/auth/user-route-access.service.spec.ts
  - frontend/src/app.routes.ts
  - frontend/src/app.routes.spec.ts
  - frontend/src/app/layout/component/menu/app.menu.ts
  - frontend/src/app/layout/component/menu/app.menu.spec.ts
  - frontend/src/app/pages/admin/admin.routes.ts
  - frontend/src/app/pages/admin/security/security.routes.ts
  - frontend/src/app/pages/admin/user-management/user-management.routes.ts
  - frontend/src/app/pages/entities/organization/organization.routes.ts
  - frontend/src/app/pages/entities/department/department.routes.ts
  - frontend/src/app/pages/entities/employee/employee.routes.ts
autonomous: true
requirements: [ROUTE-02, ROUTE-03]

must_haves:
  truths:
    - "Route reachability and menu visibility come from the same navigation-node metadata and current-user grant service instead of separate hardcoded authority assumptions"
    - "Admin and entity areas remain behind distinct lazy boundaries while deep routes inherit stable shell ids for highlighting and denial decisions"
  artifacts:
    - path: "frontend/src/app/core/auth/user-route-access.service.ts"
      provides: "Navigation-aware route gate with blocked-destination redirect metadata"
    - path: "frontend/src/app/layout/component/menu/app.menu.ts"
      provides: "Menu rendering from filtered navigation registry instead of inline role checks"
    - path: "frontend/src/app.routes.ts"
      provides: "Route metadata contract for shell navigation and lazy boundaries"
  key_links:
    - from: "frontend/src/app/layout/component/menu/app.menu.ts"
      to: "frontend/src/app/layout/navigation/navigation.service.ts"
      via: "filtered visible tree"
      pattern: "visibleTree"
    - from: "frontend/src/app/core/auth/user-route-access.service.ts"
      to: "frontend/src/app/layout/navigation/navigation.service.ts"
      via: "node visibility and fallback lookup"
      pattern: "isNodeVisible"
---

<objective>
Replace the current hardcoded menu role checks and static route-authority assumptions with navigation-node metadata and a shared current-user visibility service.

Purpose: ROUTE-02 requires denied routes and hidden navigation entries to be decided before component content renders, and ROUTE-03 requires that to happen without collapsing the existing lazy route boundaries.
Output: Route metadata, navigation-aware `UserRouteAccessService`, menu integration, and route or menu tests.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-enterprise-navigation-shell/07-CONTEXT.md
@.planning/phases/07-enterprise-navigation-shell/07-RESEARCH.md
@.planning/phases/07-enterprise-navigation-shell/07-UI-SPEC.md
@.planning/phases/07-enterprise-navigation-shell/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add route metadata for navigation nodes and preserve lazy boundaries</name>
  <files>
    frontend/src/app.routes.ts,
    frontend/src/app.routes.spec.ts,
    frontend/src/app/pages/admin/admin.routes.ts,
    frontend/src/app/pages/admin/security/security.routes.ts,
    frontend/src/app/pages/admin/user-management/user-management.routes.ts,
    frontend/src/app/pages/entities/organization/organization.routes.ts,
    frontend/src/app/pages/entities/department/department.routes.ts,
    frontend/src/app/pages/entities/employee/employee.routes.ts
  </files>
  <read_first>
    frontend/src/app.routes.ts,
    frontend/src/app.routes.spec.ts,
    frontend/src/app/pages/admin/admin.routes.ts,
    frontend/src/app/pages/admin/security/security.routes.ts,
    frontend/src/app/pages/admin/user-management/user-management.routes.ts,
    frontend/src/app/pages/entities/organization/organization.routes.ts,
    frontend/src/app/pages/entities/department/department.routes.ts,
    frontend/src/app/pages/entities/employee/employee.routes.ts,
    frontend/src/app/layout/navigation/navigation-registry.ts,
    .planning/phases/07-enterprise-navigation-shell/07-RESEARCH.md,
    .planning/phases/07-enterprise-navigation-shell/07-UI-SPEC.md
  </read_first>
  <action>
Update every shell-mounted entity and admin route to carry consistent route data:
- `navigationNodeId`
- `sectionId`
- `breadcrumbKey`
- `pageTitleKey`
- `deniedMode`

Use these exact node ids:
- organization routes -> `entities.organization`
- department routes -> `entities.department`
- employee routes -> `entities.employee`
- user-management routes -> `security.users`
- roles routes -> `security.roles`
- row-policies routes -> `security.row-policies`

For list routes set `deniedMode: 'in-shell'` on entity list routes and `deniedMode: 'route'` on admin routes.
For detail, edit, and new entity routes, keep the parent entity `navigationNodeId` so deep routes highlight the same menu leaf.

Update `frontend/src/app.routes.ts` so:
- the `admin` lazy boundary stays lazy-loaded
- the `entities` lazy boundary stays lazy-loaded
- the top-level `admin` shell child no longer hardcodes `data: { authorities: ['ROLE_ADMIN'] }`
- public error and login routes remain outside navigation-node gating

Update `frontend/src/app.routes.spec.ts` to assert:
- the same lazy child paths remain mounted
- `admin` and `entities` stay lazy boundaries
- role-specific shell routes now expose `navigationNodeId` metadata rather than relying on top-level `ROLE_ADMIN`
  </action>
  <verify>
    <automated>npm --prefix frontend exec ng test -- --watch=false --include src/app.routes.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app.routes.ts` does not contain `data: { authorities: ['ROLE_ADMIN'] }`
    - `frontend/src/app/pages/entities/organization/organization.routes.ts` contains `navigationNodeId: 'entities.organization'`
    - `frontend/src/app/pages/entities/department/department.routes.ts` contains `navigationNodeId: 'entities.department'`
    - `frontend/src/app/pages/entities/employee/employee.routes.ts` contains `navigationNodeId: 'entities.employee'`
    - `frontend/src/app/pages/admin/user-management/user-management.routes.ts` contains `navigationNodeId: 'security.users'`
    - `frontend/src/app/pages/admin/security/security.routes.ts` contains `navigationNodeId: 'security.roles'` and `navigationNodeId: 'security.row-policies'`
    - `frontend/src/app.routes.spec.ts` contains assertions for `navigationNodeId`
  </acceptance_criteria>
  <done>Route metadata now defines shell identity and denial mode without breaking the existing lazy route boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Make the menu and route guard consume the navigation service</name>
  <files>
    frontend/src/app/core/auth/user-route-access.service.ts,
    frontend/src/app/core/auth/user-route-access.service.spec.ts,
    frontend/src/app/layout/component/menu/app.menu.ts,
    frontend/src/app/layout/component/menu/app.menu.spec.ts
  </files>
  <read_first>
    frontend/src/app/core/auth/user-route-access.service.ts,
    frontend/src/app/layout/component/menu/app.menu.ts,
    frontend/src/app/layout/component/menu/app.menu.spec.ts,
    frontend/src/app/layout/navigation/navigation.service.ts,
    frontend/src/app/layout/navigation/navigation-registry.ts,
    frontend/src/app/core/auth/state-storage.service.ts,
    frontend/src/app/pages/entities/shared/service/secured-entity-capability.service.ts,
    .planning/phases/07-enterprise-navigation-shell/07-CONTEXT.md,
    .planning/phases/07-enterprise-navigation-shell/07-UI-SPEC.md
  </read_first>
  <action>
Update `frontend/src/app/layout/component/menu/app.menu.ts` to stop building an inline hardcoded array with `Authority.ADMIN`.
Instead:
- remove the `Authority` import
- inject the shared `NavigationService`
- rebuild `model` from `navigationService.visibleTree()`
- translate labels from each registry node's `labelKey`
- attach each menu item's `id`, `icon`, `routerLink`, and `path` using the registry values so `LayoutService.activePath` remains stable across language changes

Update `frontend/src/app/core/auth/user-route-access.service.ts` so the guard:
- still redirects unauthenticated users to `/login` and stores the original URL
- reads `navigationNodeId`, `sectionId`, and `breadcrumbKey` from route data
- uses `NavigationService.isNodeVisible(navigationNodeId)` for authenticated shell routes
- redirects unreachable routes to `/accessdenied`
- passes blocked-route metadata through navigation extras state with keys `blockedUrl`, `blockedLabelKey`, and `sectionId`
- allows routes without a `navigationNodeId` to proceed when they are public shell routes like `/accessdenied`, `/404`, or `/error`

Create `frontend/src/app/core/auth/user-route-access.service.spec.ts` covering:
- unauthenticated redirect to `/login`
- authenticated hidden-leaf redirect to `/accessdenied`
- allowed leaf returns `true`
- `/accessdenied` route without `navigationNodeId` is allowed

Update `frontend/src/app/layout/component/menu/app.menu.spec.ts` so it no longer stubs `hasAnyAuthority(...)`; instead it stubs `NavigationService.visibleTree()` and confirms hidden leaves are absent while language changes still update labels without changing stable ids.
  </action>
  <verify>
    <automated>npm --prefix frontend exec ng test -- --watch=false --include src/app/core/auth/user-route-access.service.spec.ts --include src/app/layout/component/menu/app.menu.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/layout/component/menu/app.menu.ts` does not import `Authority`
    - `frontend/src/app/layout/component/menu/app.menu.ts` contains `navigationService.visibleTree`
    - `frontend/src/app/layout/component/menu/app.menu.ts` contains `path:`
    - `frontend/src/app/core/auth/user-route-access.service.ts` contains `blockedLabelKey`
    - `frontend/src/app/core/auth/user-route-access.service.ts` contains `sectionId`
    - `frontend/src/app/core/auth/user-route-access.service.spec.ts` exists
    - `frontend/src/app/layout/component/menu/app.menu.spec.ts` contains a hidden-leaf assertion based on navigation filtering
  </acceptance_criteria>
  <done>The menu and route guard now use the same backend-aware visibility contract, preventing unauthorized routes and menu entries before protected content renders.</done>
</task>

</tasks>

<verification>
- `npm --prefix frontend exec ng test -- --watch=false --include src/app.routes.spec.ts --include src/app/core/auth/user-route-access.service.spec.ts --include src/app/layout/component/menu/app.menu.spec.ts` passes
</verification>

<success_criteria>
- All shell routes expose stable navigation-node metadata
- The menu is filtered from the shared navigation service rather than inline role checks
- Unreachable authenticated routes redirect to `/accessdenied` with blocked-destination metadata
- Admin and entity lazy route boundaries remain intact
</success_criteria>

<output>
After completion, create `.planning/phases/07-enterprise-navigation-shell/07-03-SUMMARY.md`
</output>
