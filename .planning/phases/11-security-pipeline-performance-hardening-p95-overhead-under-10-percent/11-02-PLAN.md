---
phase: 11-security-pipeline-performance-hardening-p95-overhead-under-10-percent
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/vn/core/security/access/AccessManagerImpl.java
  - src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java
  - src/main/java/com/vn/core/security/serialize/SecureEntitySerializerImpl.java
  - src/main/java/com/vn/core/security/web/SecuredEntityJsonAdapter.java
  - src/main/java/com/vn/core/web/rest/OrganizationResource.java
  - src/test/java/com/vn/core/security/serialize/SecureEntitySerializerImplTest.java
  - src/test/java/com/vn/core/web/rest/SecuredEntityEnforcementIT.java
  - load-tests/results/benchmark-run-summary-2026-03-31.md
autonomous: true
requirements: [PERF-04]
must_haves:
  truths:
    - "Organization detail reads no longer pay the Criteria API id-lookup penalty because secured single-entity loads use the direct id path per D-07."
    - "List and detail serialization keep the same attribute-filtered JSON contract while resolving the fetch plan once per response and replacing BeanWrapper property access with Jackson ObjectReader per D-08 and D-09."
    - "Phase 11 produces a persisted before/after benchmark comparison proving whether secured p95 overhead is under 10% against the Phase 10 baseline, satisfying PERF-04."
  artifacts:
    - path: "src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java"
      provides: "Direct-id secured read path without id Specification overhead"
      contains: "loadOne"
    - path: "src/main/java/com/vn/core/security/web/SecuredEntityJsonAdapter.java"
      provides: "Single fetch-plan resolution per list/detail serialization call"
      contains: "FetchPlan"
    - path: "src/main/java/com/vn/core/security/serialize/SecureEntitySerializerImpl.java"
      provides: "Jackson ObjectReader-based property access preserving attribute filtering"
      contains: "ObjectReader"
    - path: "load-tests/results/benchmark-run-summary-2026-03-31.md"
      provides: "Persisted Phase 11 before/after benchmark comparison against the Phase 10 baseline"
      contains: "Phase 11"
  key_links:
    - from: "src/main/java/com/vn/core/web/rest/OrganizationResource.java"
      to: "src/main/java/com/vn/core/security/web/SecuredEntityJsonAdapter.java"
      via: "detail/list JSON response generation"
      pattern: "securedEntityJsonAdapter\.toJson"
    - from: "src/main/java/com/vn/core/security/web/SecuredEntityJsonAdapter.java"
      to: "src/main/java/com/vn/core/security/serialize/SecureEntitySerializerImpl.java"
      via: "resolved FetchPlan passed into serializer path"
      pattern: "serialize"
    - from: "load-tests/scripts/org-list-benchmark.js"
      to: "load-tests/results/benchmark-run-summary-2026-03-31.md"
      via: "persisted benchmark rerun summary"
      pattern: "Phase 11"
---

<objective>
Cut the remaining secured-read overhead from detail and serialization hot paths, then prove the result with a persisted benchmark rerun against the Phase 10 baseline.

Purpose: PERF-04 is only complete if the code-path fixes ship and the k6 evidence shows whether p95 secured overhead is below 10%. Per D-07, D-08, D-09, and D-11, this plan removes the id-spec lookup, resolves fetch plans once per response, replaces BeanWrapper property access with Jackson ObjectReader while preserving attribute filtering, and pre-sorts access constraints.
Output: Direct-id secure load path, fetch-plan reuse, ObjectReader-backed serializer path, constructor-sorted constraints, and an updated benchmark summary with before/after Phase 10 versus Phase 11 numbers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@load-tests/results/performance-analysis-2026-03-31.md
@load-tests/results/benchmark-run-summary-2026-03-31.md
@.planning/phases/08.3-user-registration-live-permission-refresh-entity-native-serialization-validation-hardening-and-row-policy-removal/08.3-03-SUMMARY.md
@.planning/phases/10-performance-benchmarking-and-openapi-documentation-for-the-security-pipeline-jmeter-k6-load-tests-comparing-secured-vs-standard-endpoints-and-swagger-annotations-for-variable-response-schemas-and-fetch-plan-params/10-03-SUMMARY.md

<interfaces>
From src/main/java/com/vn/core/security/data/SecureDataManager.java:
```java
<E> Optional<E> loadOne(Class<E> entityClass, Object id);
Page<Map<String, Object>> loadList(String entityCode, String fetchPlanCode, Pageable pageable);
Optional<Map<String, Object>> loadOne(String entityCode, Object id, String fetchPlanCode);
```

From src/main/java/com/vn/core/security/data/DataManager.java:
```java
void checkCrud(Class<?> entityClass, EntityOp op);
<T> T load(Class<T> entityClass, Object id);
<T> Optional<T> loadOne(Class<T> entityClass, Specification<T> spec, EntityOp op);
```

From src/main/java/com/vn/core/security/web/SecuredEntityJsonAdapter.java:
```java
public JsonNode toJson(Object entity, String fetchPlanCode);
public String toJsonString(Object entity, String fetchPlanCode);
public String toJsonArrayString(Iterable<?> entities, String fetchPlanCode);
```

From src/main/java/com/vn/core/security/serialize/SecureEntitySerializerImpl.java:
```java
public Map<String, Object> serialize(Object entity, FetchPlan fetchPlan);
```

From src/main/java/com/vn/core/security/access/AccessManagerImpl.java:
```java
public <C extends AccessContext> C applyRegisteredConstraints(C context)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create direct adapter and access-manager tests before implementation</name>
  <files>src/test/java/com/vn/core/security/access/AccessManagerImplTest.java, src/test/java/com/vn/core/security/web/SecuredEntityJsonAdapterTest.java</files>
  <read_first>src/main/java/com/vn/core/security/access/AccessManagerImpl.java, src/main/java/com/vn/core/security/web/SecuredEntityJsonAdapter.java, src/test/java/com/vn/core/security/serialize/SecureEntitySerializerImplTest.java, .planning/phases/11-security-pipeline-performance-hardening-p95-overhead-under-10-percent/11-VALIDATION.md</read_first>
  <behavior>
    - Test 1: `AccessManagerImplTest` proves constraints still apply in `getOrder()` sequence after sorting is moved to constructor time.
    - Test 2: `SecuredEntityJsonAdapterTest` proves detail and list paths resolve the fetch plan once per response call rather than once per entity.
  </behavior>
  <action>Create the missing Wave 0 tests identified in `11-VALIDATION.md` before implementation tasks rely on them. Add `AccessManagerImplTest` under `src/test/java/com/vn/core/security/access/` covering constructor-time ordering and preserved application order. Add `SecuredEntityJsonAdapterTest` under `src/test/java/com/vn/core/security/web/` using spies or counters around the fetch-plan resolver so the test can assert one resolution for one detail call and one resolution for one list call regardless of entity count. Keep these tests focused so they become the direct verification anchors for D-08 and D-11 in the implementation tasks.</action>
  <acceptance_criteria>
    - `src/test/java/com/vn/core/security/access/AccessManagerImplTest.java` exists and proves constructor-time sorting preserves `getOrder()` execution sequence.
    - `src/test/java/com/vn/core/security/web/SecuredEntityJsonAdapterTest.java` exists and directly proves one fetch-plan resolution per detail/list response call.
    - Both test files match the package paths referenced by `11-VALIDATION.md` Wave 0 requirements.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.vn.core.security.access.AccessManagerImplTest" --tests "com.vn.core.security.web.SecuredEntityJsonAdapterTest"</automated>
  </verify>
  <done>Wave 0 adapter and access-manager tests exist, pass, and directly verify the two performance behaviors this plan depends on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Replace secure detail id Specification and move serializer hot paths to fetch-plan reuse plus ObjectReader</name>
  <files>src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java, src/main/java/com/vn/core/security/serialize/SecureEntitySerializerImpl.java, src/main/java/com/vn/core/security/web/SecuredEntityJsonAdapter.java, src/main/java/com/vn/core/web/rest/OrganizationResource.java, src/test/java/com/vn/core/security/serialize/SecureEntitySerializerImplTest.java, src/test/java/com/vn/core/security/web/SecuredEntityJsonAdapterTest.java, src/test/java/com/vn/core/web/rest/SecuredEntityEnforcementIT.java</files>
  <read_first>src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java, src/main/java/com/vn/core/security/serialize/SecureEntitySerializerImpl.java, src/main/java/com/vn/core/security/web/SecuredEntityJsonAdapter.java, src/main/java/com/vn/core/web/rest/OrganizationResource.java, src/test/java/com/vn/core/security/serialize/SecureEntitySerializerImplTest.java, src/test/java/com/vn/core/security/web/SecuredEntityJsonAdapterTest.java, src/test/java/com/vn/core/web/rest/SecuredEntityEnforcementIT.java, load-tests/results/performance-analysis-2026-03-31.md, .planning/phases/11-security-pipeline-performance-hardening-p95-overhead-under-10-percent/11-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `GET /api/organizations/{id}` still returns the same secured detail contract while the READ path uses direct id loading after the CRUD check per D-07.
    - Test 2: Organization list and detail serialization still omit denied fields while resolving the fetch plan once per response per D-08.
    - Test 3: The serializer preserves current attribute filtering behavior after replacing BeanWrapper property access with Jackson ObjectReader per D-09, where the real property-access seam lives in `SecureEntitySerializerImpl` even though the optimization is initiated from `SecuredEntityJsonAdapter`.
  </behavior>
  <action>Refactor the secured read and serialization hot paths without changing the authorization contract. In `SecureDataManagerImpl`, eliminate the id-based `Specification` READ detail path and route secured single-entity loads through the direct id load seam per D-07, while preserving the existing missing-row and forbidden-access behavior and keeping CRUD checks before data access. In `SecuredEntityJsonAdapter`, resolve the `FetchPlan` once for each detail call and once before each list iteration per D-08, then pass the resolved plan into overloads or helpers rather than resolving inside the entity loop. Implement D-09 at the actual property-access seam by replacing `BeanWrapperImpl` with Jackson `ObjectReader` inside `SecureEntitySerializerImpl`, because that serializer is where property extraction occurs even though the optimization is entered through `SecuredEntityJsonAdapter`. Preserve the existing attribute-permission filtering semantics exactly and keep D-10's deferred full-streaming work out of scope. Touch `OrganizationResource` only if required to keep list/detail wiring readable after the adapter or serializer seam changes. Update the focused serializer, adapter, and secured-entity integration tests so they prove the JSON contract, single fetch-plan resolution behavior, and attribute filtering are unchanged after the refactor.</action>
  <acceptance_criteria>
    - `src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java` no longer uses an id-based `Specification` for the secured READ detail path.
    - `src/main/java/com/vn/core/security/web/SecuredEntityJsonAdapter.java` resolves the fetch plan once per detail call and once per list serialization call rather than once per entity.
    - `src/main/java/com/vn/core/security/serialize/SecureEntitySerializerImpl.java` uses Jackson `ObjectReader` rather than `BeanWrapperImpl` for property access per D-09.
    - `src/test/java/com/vn/core/security/web/SecuredEntityJsonAdapterTest.java` directly proves one fetch-plan resolution per detail/list response call.
    - `src/test/java/com/vn/core/security/serialize/SecureEntitySerializerImplTest.java` and `src/test/java/com/vn/core/web/rest/SecuredEntityEnforcementIT.java` prove denied fields remain omitted after the serializer-path refactor.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.vn.core.security.web.SecuredEntityJsonAdapterTest" --tests "com.vn.core.security.serialize.SecureEntitySerializerImplTest" && ./gradlew integrationTest --tests "com.vn.core.web.rest.SecuredEntityEnforcementIT"</automated>
  </verify>
  <done>Secured detail loads and serializer execution avoid the identified hot-path overhead while preserving the existing secured JSON behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pre-sort access constraints and rerun persisted Phase 10 k6 benchmarks</name>
  <files>src/main/java/com/vn/core/security/access/AccessManagerImpl.java, load-tests/results/benchmark-run-summary-2026-03-31.md</files>
  <read_first>src/main/java/com/vn/core/security/access/AccessManagerImpl.java, src/test/java/com/vn/core/security/access/AccessManagerImplTest.java, load-tests/scripts/org-list-benchmark.js, load-tests/scripts/org-detail-benchmark.js, load-tests/results/benchmark-run-summary-2026-03-31.md, load-tests/results/performance-analysis-2026-03-31.md, .planning/phases/11-security-pipeline-performance-hardening-p95-overhead-under-10-percent/11-RESEARCH.md
  <behavior>
    - Test 1: AccessManager still applies constraints in `getOrder()` sequence after sorting moves to constructor time per D-11.
    - Test 2: The Phase 10 list and detail k6 scripts are rerun against the post-fix code and persisted as a before/after comparison tied to the original baseline, closing PERF-04 proof.
    - Test 3: The updated benchmark summary explicitly states whether secured p95 overhead is below 10% for list and detail.
  </behavior>
  <action>Finish the remaining CPU-only hot path and produce the required proof artifact for PERF-04. In `AccessManagerImpl`, sort the injected constraints once in the constructor and remove per-call sorting from `applyRegisteredConstraints` per D-11. Then rerun the same Phase 10 k6 benchmark scripts for secured versus standard Organization list and detail endpoints, using the documented valid detail id or revalidating it first if needed. Persist the new measurements in `load-tests/results/benchmark-run-summary-2026-03-31.md` by adding a clear Phase 11 before/after comparison section that includes Phase 10 baseline numbers, post-hardening numbers, overhead deltas, and an explicit pass/fail statement for the under-10-percent KPI. Do not create a separate ephemeral note; the benchmark evidence must live in the persisted summary artifact tied to the existing scripts.</action>
  <acceptance_criteria>
    - `src/main/java/com/vn/core/security/access/AccessManagerImpl.java` no longer sorts constraints inside `applyRegisteredConstraints`.
    - `load-tests/results/benchmark-run-summary-2026-03-31.md` contains persisted post-Phase-11 list and detail benchmark results alongside the original Phase 10 baseline.
    - The same benchmark summary explicitly states whether PERF-04 is met for list and detail secured overhead.
    - The benchmark rerun uses the existing Phase 10 k6 scripts rather than new throwaway scripts.
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test --tests "com.vn.core.security.access.AccessManagerImplTest" && k6 run load-tests/scripts/org-list-benchmark.js && k6 run --env ORG_ID=1501 load-tests/scripts/org-detail-benchmark.js</automated>
  </verify>
  <done>AccessManager no longer pays per-call sorting overhead, and the repository contains a persisted benchmark comparison proving the Phase 11 performance outcome against the Phase 10 baseline.</done>
</task>

</tasks>

<verification>
Re-run focused serializer, access-manager, and secured-entity tests, then execute the Phase 10 list/detail k6 scripts and persist the before/after summary. Inspect the modified classes to confirm there is no id-based READ Specification, no per-entity fetch-plan resolution, no BeanWrapper property access in the serializer path, and no per-call sorting in AccessManagerImpl.
</verification>

<success_criteria>
- Secure detail reads use the direct id path instead of Criteria-based id specifications.
- SecuredEntityJsonAdapter resolves fetch plans once per response and the serializer path uses Jackson ObjectReader while preserving attribute filtering.
- AccessManagerImpl sorts constraints once during construction, not per check.
- Persisted k6 benchmark evidence compares Phase 10 versus Phase 11 and states whether the p95 overhead target is achieved.
</success_criteria>

<output>
After completion, create `.planning/phases/11-security-pipeline-performance-hardening-p95-overhead-under-10-percent/11-02-SUMMARY.md`
</output>
