---
phase: 08.2-multi-app-menu-roles-and-jmix-style-json-entity-controllers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/vn/core/security/repository/SecMenuPermissionRepository.java
  - src/main/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResource.java
  - src/test/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResourceIT.java
autonomous: true
requirements:
  - SC-01

must_haves:
  truths:
    - "One role can be queried across multiple apps from the admin menu-permission API without flattening the `appName` dimension"
    - "The admin menu-permission GET contract keeps optional per-app filtering instead of forcing a single app"
    - "Create and delete remain row-level admin operations on `SecMenuPermission` and do not change their DTO shape"
    - "Current-user menu resolution stays on the existing per-app `/api/security/menu-permissions` contract"
  artifacts:
    - path: "src/main/java/com/vn/core/security/repository/SecMenuPermissionRepository.java"
      provides: "Repository queries for role-wide and role+app admin reads"
      contains: "findAllByRoleOrderByAppNameAscMenuIdAsc"
    - path: "src/main/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResource.java"
      provides: "Admin contract for role menu grants across all apps"
      contains: "@RequestParam(required = false) String appName"
    - path: "src/test/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResourceIT.java"
      provides: "Backend proof that one role can hold grants in multiple apps"
      contains: "queryByRoleWithoutAppFilterReturnsRowsAcrossApps"
  key_links:
    - from: "AdminMenuPermissionResource"
      to: "SecMenuPermissionRepository"
      via: "role-wide and role+app query methods"
      pattern: "findAllByRoleOrderByAppNameAscMenuIdAsc"
    - from: "AdminMenuPermissionResourceIT"
      to: "AdminMenuPermissionResource"
      via: "GET /api/admin/sec/menu-permissions"
      pattern: "role="
---

<objective>
Extend the backend admin menu-permission contract so one role can hold and query menu grants across multiple apps, while leaving the current-user per-app menu endpoint unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.2-multi-app-menu-roles-and-jmix-style-json-entity-controllers/08.2-CONTEXT.md
@.planning/phases/08.2-multi-app-menu-roles-and-jmix-style-json-entity-controllers/08.2-RESEARCH.md
@AGENTS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add role-wide and optional app-filtered admin menu-permission queries</name>
  <files>
    src/main/java/com/vn/core/security/repository/SecMenuPermissionRepository.java,
    src/main/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResource.java
  </files>
  <read_first>
    src/main/java/com/vn/core/security/repository/SecMenuPermissionRepository.java,
    src/main/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResource.java,
    src/main/java/com/vn/core/web/rest/MenuPermissionResource.java,
    src/main/java/com/vn/core/service/security/CurrentUserMenuPermissionService.java,
    src/main/java/com/vn/core/service/dto/security/SecMenuPermissionDTO.java,
    .planning/phases/08.2-multi-app-menu-roles-and-jmix-style-json-entity-controllers/08.2-CONTEXT.md
  </read_first>
  <action>
    In `SecMenuPermissionRepository.java`, add these exact repository methods and keep the existing current-user helpers intact:
    - `List<SecMenuPermission> findAllByRoleOrderByAppNameAscMenuIdAsc(String role);`
    - `List<SecMenuPermission> findAllByRoleAndAppNameOrderByMenuIdAsc(String role, String appName);`

    In `AdminMenuPermissionResource.java`:
    - keep `POST` and `DELETE` behavior unchanged
    - change `getMenuPermissions(...)` so `appName` becomes optional instead of defaulting every request to `jhipster-security-platform`
    - when `appName` is null or blank, query `findAllByRoleOrderByAppNameAscMenuIdAsc(role)`
    - when `appName` is present, query `findAllByRoleAndAppNameOrderByMenuIdAsc(role, appName)`
    - remove the current `findAllByRole(role).stream().filter(...)` path entirely
    - keep the response DTO rows as `{ id, role, appName, menuId, effect }`
    - update method Javadoc and debug logging so they describe role-wide multi-app reads plus optional per-app filtering

    Do not modify `MenuPermissionResource` or `CurrentUserMenuPermissionService` in this task.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.core.web.rest.admin.security.AdminMenuPermissionResourceIT"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/vn/core/security/repository/SecMenuPermissionRepository.java` contains `findAllByRoleOrderByAppNameAscMenuIdAsc`
    - `src/main/java/com/vn/core/security/repository/SecMenuPermissionRepository.java` contains `findAllByRoleAndAppNameOrderByMenuIdAsc`
    - `src/main/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResource.java` contains `@RequestParam(required = false) String appName`
    - `src/main/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResource.java` does not contain `.findAllByRole(role).stream().filter`
    - `src/main/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResource.java` contains `findAllByRoleOrderByAppNameAscMenuIdAsc`
    - `src/main/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResource.java` contains `findAllByRoleAndAppNameOrderByMenuIdAsc`
    - `./gradlew test --tests "com.vn.core.web.rest.admin.security.AdminMenuPermissionResourceIT"` exits 0
  </acceptance_criteria>
  <done>The backend admin endpoint can now read one role's menu grants across multiple apps without weakening the app dimension.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the admin API supports multi-app role queries and app filtering</name>
  <files>
    src/test/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResourceIT.java
  </files>
  <read_first>
    src/test/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResourceIT.java,
    src/main/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResource.java,
    src/main/java/com/vn/core/security/repository/SecMenuPermissionRepository.java,
    src/test/java/com/vn/core/web/rest/MenuPermissionResourceIT.java,
    .planning/phases/08.2-multi-app-menu-roles-and-jmix-style-json-entity-controllers/08.2-RESEARCH.md
  </read_first>
  <action>
    Extend `AdminMenuPermissionResourceIT.java` with focused cases for the new read contract:
    - add a test named `queryByRoleWithoutAppFilterReturnsRowsAcrossApps` that creates menu grants for the same role in at least two different `appName` values and proves `GET /api/admin/sec/menu-permissions?role=ROLE_X` returns rows from both apps
    - add a test named `queryByRoleWithAppFilterReturnsOnlyRequestedApp` that proves `GET /api/admin/sec/menu-permissions?role=ROLE_X&appName=other-app` returns only that app's rows
    - keep the existing create, delete, missing-role, and non-admin coverage
    - assert both `appName` and `menuId` values in the JSON response so the contract stays explicit

    Do not add any current-user menu assertions here; those remain in `MenuPermissionResourceIT` and Phase 08.2 Plan 04.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.core.web.rest.admin.security.AdminMenuPermissionResourceIT"</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResourceIT.java` contains `queryByRoleWithoutAppFilterReturnsRowsAcrossApps`
    - `src/test/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResourceIT.java` contains `queryByRoleWithAppFilterReturnsOnlyRequestedApp`
    - `src/test/java/com/vn/core/web/rest/admin/security/AdminMenuPermissionResourceIT.java` contains `jsonPath("$[*].appName")`
    - `./gradlew test --tests "com.vn.core.web.rest.admin.security.AdminMenuPermissionResourceIT"` exits 0
  </acceptance_criteria>
  <done>The multi-app admin query contract is covered by integration tests before the frontend starts depending on it.</done>
</task>
</tasks>

<verification>
- `./gradlew test --tests "com.vn.core.web.rest.admin.security.AdminMenuPermissionResourceIT"` passes
- `AdminMenuPermissionResource` supports role-wide reads when `appName` is omitted
- `AdminMenuPermissionResource` still supports per-app reads when `appName` is supplied
</verification>

<success_criteria>
One role's menu grants can now be queried across multiple apps from the admin API without changing the current-user per-app runtime menu contract.
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-multi-app-menu-roles-and-jmix-style-json-entity-controllers/08.2-01-SUMMARY.md`
</output>
