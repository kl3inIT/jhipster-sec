---
phase: 08.2-multi-app-menu-roles-and-jmix-style-json-entity-controllers
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/main/java/com/vn/core/security/data/SecuredLoadQuery.java
  - src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java
  - src/main/java/com/vn/core/security/data/SecureQuerySpecificationFactory.java
  - src/main/java/com/vn/core/web/rest/vm/SecuredEntityQueryVM.java
  - src/main/java/com/vn/core/web/rest/OrganizationResource.java
  - src/main/java/com/vn/core/web/rest/DepartmentResource.java
  - src/main/java/com/vn/core/web/rest/EmployeeResource.java
  - src/main/java/com/vn/core/service/OrganizationService.java
  - src/main/java/com/vn/core/service/DepartmentService.java
  - src/main/java/com/vn/core/service/EmployeeService.java
  - src/test/java/com/vn/core/security/data/SecureDataManagerImplTest.java
  - src/test/java/com/vn/core/web/rest/SecuredEntityEnforcementIT.java
autonomous: true
requirements:
  - SC-03
  - SC-04

must_haves:
  truths:
    - "Protected entity create and update endpoints stop exposing `Map<String, Object>` request signatures"
    - "Each protected entity resource gets an explicit JSON-based `/query` endpoint wired into `SecureDataManager.loadByQuery(...)`"
    - "`SecureDataManagerImpl.loadByQuery(...)` combines row-level restrictions with a limited JSON filter contract when JPQL text is blank"
    - "Nonblank JPQL remains explicitly rejected unless the code also adds the missing catalog/config support"
  artifacts:
    - path: "src/main/java/com/vn/core/web/rest/vm/SecuredEntityQueryVM.java"
      provides: "Explicit JSON query request contract"
      contains: "Map<String, Object> filters"
    - path: "src/main/java/com/vn/core/security/data/SecureQuerySpecificationFactory.java"
      provides: "Limited JSON-filter-to-specification builder"
      contains: "cb.equal"
    - path: "src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java"
      provides: "Reachable secured query path with row-level enforcement"
      contains: "query.parameters()"
    - path: "src/main/java/com/vn/core/web/rest/OrganizationResource.java"
      provides: "Raw JSON secured entity controller boundary plus `/query`"
      contains: "@PostMapping(\"/query\")"
  key_links:
    - from: "OrganizationResource / DepartmentResource / EmployeeResource"
      to: "SecuredEntityQueryVM"
      via: "JSON query request body"
      pattern: "@RequestBody SecuredEntityQueryVM"
    - from: "SecureDataManagerImpl"
      to: "SecureQuerySpecificationFactory"
      via: "blank-JPQL JSON filter query handling"
      pattern: "build(entityClass, query.parameters())"
---

<objective>
Move the protected entity boundary to explicit raw JSON request carriers and make `loadByQuery` reachable through dedicated secured JSON query endpoints without promising arbitrary JPQL support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.2-multi-app-menu-roles-and-jmix-style-json-entity-controllers/08.2-CONTEXT.md
@.planning/phases/08.2-multi-app-menu-roles-and-jmix-style-json-entity-controllers/08.2-RESEARCH.md
@AGENTS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add the limited JSON query contract behind `SecureDataManager.loadByQuery(...)`</name>
  <files>
    src/main/java/com/vn/core/security/data/SecuredLoadQuery.java,
    src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java,
    src/main/java/com/vn/core/security/data/SecureQuerySpecificationFactory.java,
    src/main/java/com/vn/core/web/rest/vm/SecuredEntityQueryVM.java,
    src/test/java/com/vn/core/security/data/SecureDataManagerImplTest.java
  </files>
  <read_first>
    src/main/java/com/vn/core/security/data/SecuredLoadQuery.java,
    src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java,
    src/main/java/com/vn/core/security/catalog/MetamodelSecuredEntityCatalog.java,
    src/main/java/com/vn/core/security/catalog/SecuredEntityEntry.java,
    src/main/java/com/vn/core/security/data/DataManager.java,
    src/test/java/com/vn/core/security/data/SecureDataManagerImplTest.java,
    .planning/phases/08.2-multi-app-menu-roles-and-jmix-style-json-entity-controllers/08.2-RESEARCH.md
  </read_first>
  <action>
    Create `SecureQuerySpecificationFactory.java` in `com.vn.core.security.data` with one focused responsibility: build a conjunction of equality predicates from a top-level JSON filter map.
    - method signature: `<T> Specification<T> build(Class<T> entityClass, Map<String, Object> filters)`
    - treat a null or empty filter map as `(root, query, cb) -> null`
    - for each `filters` entry, create `cb.equal(root.get(fieldName), value)`
    - do not add nested-path parsing, OR groups, or arbitrary operator syntax in this phase

    Create `SecuredEntityQueryVM.java` in `src/main/java/com/vn/core/web/rest/vm/` with these exact fields:
    - `String fetchPlanCode`
    - `Integer page`
    - `Integer size`
    - `List<String> sort`
    - `Map<String, Object> filters`

    Update `SecuredLoadQuery.java` and `SecureDataManagerImpl.java` so the secured query path works like this:
    - keep `jpql` and `parameters` fields on `SecuredLoadQuery`
    - when `query.jpql()` is null or blank, interpret `query.parameters()` as the JSON filter map
    - build `filterSpec = secureQuerySpecificationFactory.build(entityClass, query.parameters())`
    - combine `filterSpec` with the existing row-level spec before calling `dataManager.loadPage(...)`
    - keep the current explicit denial for nonblank JPQL text unless the phase later adds real translation support

    Update `SecureDataManagerImplTest.java` with:
    - one test proving blank-JPQL queries with filter parameters delegate through `dataManager.loadPage(...)`
    - one test proving the row-level spec and filter spec are both present in the final load path
    - the current nonblank-JPQL denial expectation staying intact
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.core.security.data.SecureDataManagerImplTest"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/vn/core/security/data/SecureQuerySpecificationFactory.java` exists
    - `src/main/java/com/vn/core/security/data/SecureQuerySpecificationFactory.java` contains `cb.equal`
    - `src/main/java/com/vn/core/web/rest/vm/SecuredEntityQueryVM.java` contains `Map<String, Object> filters`
    - `src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java` contains `query.parameters()`
    - `src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java` contains `build(entityClass, query.parameters())`
    - `src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java` contains `JPQL query translation is not implemented`
    - `src/test/java/com/vn/core/security/data/SecureDataManagerImplTest.java` contains `blankJpql`
    - `./gradlew test --tests "com.vn.core.security.data.SecureDataManagerImplTest"` exits 0
  </acceptance_criteria>
  <done>The internal secured query path now has a real, limited JSON filter contract instead of an unreachable placeholder method.</done>
</task>

<task type="auto">
  <name>Task 2: Expose raw JSON create/update and explicit `/query` endpoints on protected entities</name>
  <files>
    src/main/java/com/vn/core/web/rest/OrganizationResource.java,
    src/main/java/com/vn/core/web/rest/DepartmentResource.java,
    src/main/java/com/vn/core/web/rest/EmployeeResource.java,
    src/main/java/com/vn/core/service/OrganizationService.java,
    src/main/java/com/vn/core/service/DepartmentService.java,
    src/main/java/com/vn/core/service/EmployeeService.java,
    src/test/java/com/vn/core/web/rest/SecuredEntityEnforcementIT.java
  </files>
  <read_first>
    src/main/java/com/vn/core/web/rest/OrganizationResource.java,
    src/main/java/com/vn/core/web/rest/DepartmentResource.java,
    src/main/java/com/vn/core/web/rest/EmployeeResource.java,
    src/main/java/com/vn/core/service/OrganizationService.java,
    src/main/java/com/vn/core/service/DepartmentService.java,
    src/main/java/com/vn/core/service/EmployeeService.java,
    src/main/java/com/vn/core/security/data/SecureDataManager.java,
    src/test/java/com/vn/core/web/rest/SecuredEntityEnforcementIT.java,
    frontend/src/app/pages/entities/organization/service/organization.service.ts
  </read_first>
  <action>
    In `OrganizationService.java`, `DepartmentService.java`, and `EmployeeService.java`:
    - inject Jackson `ObjectMapper`
    - change `list(...)` to return JSON trees for page content instead of `Map<String, Object>` signatures
    - change `findOne(...)` to return `Optional<JsonNode>`
    - change `create(...)` and `update(...)` to accept raw JSON request carriers (`JsonNode` or `ObjectNode`)
    - convert those carriers to `Map<String, Object>` using `objectMapper.convertValue(...)` immediately before calling `secureDataManager.save(...)`
    - convert the secure serializer result back to JSON with `objectMapper.valueToTree(...)`
    - add a `query(SecuredEntityQueryVM request)` method that builds a `PageRequest` from `page`, `size`, and `sort`, creates `SecuredLoadQuery(entityCode, null, filters, pageable, pageable.getSort(), resolvedFetchPlanCode)`, and returns JSON results
    - when `fetchPlanCode` is null or blank, default to the existing list fetch-plan constant for the entity

    In `OrganizationResource.java`, `DepartmentResource.java`, and `EmployeeResource.java`:
    - switch `GET ""` and `GET "/{id}"` controller signatures to JSON carrier return types instead of `Map<String, Object>`-typed signatures
    - replace `@RequestBody Map<String, Object>` on `POST` and `PUT` with a raw JSON request carrier
    - stop exposing `Map<String, Object>` in controller method signatures
    - add `@PostMapping("/query")` that accepts `@RequestBody SecuredEntityQueryVM` and returns JSON results for the entity
    - keep existing route paths unchanged

    In `SecuredEntityEnforcementIT.java`, add one explicit secured query case, for example:
    - `POST /api/organizations/query` with body `{"fetchPlanCode":"organization-list","page":0,"size":20,"filters":{"ownerLogin":"proof-owner"}}`
    - assert `200 OK`
    - assert the owned organization row is returned
    - assert denied attributes such as `budget` remain absent
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.core.web.rest.SecuredEntityEnforcementIT"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/vn/core/web/rest/OrganizationResource.java` contains `@PostMapping("/query")`
    - `src/main/java/com/vn/core/web/rest/DepartmentResource.java` contains `@PostMapping("/query")`
    - `src/main/java/com/vn/core/web/rest/EmployeeResource.java` contains `@PostMapping("/query")`
    - `src/main/java/com/vn/core/web/rest/OrganizationResource.java` does not contain `@RequestBody Map<String, Object>`
    - `src/main/java/com/vn/core/web/rest/OrganizationResource.java` does not contain `ResponseEntity<List<Map<String, Object>>>`
    - `src/main/java/com/vn/core/service/OrganizationService.java` contains `ObjectMapper`
    - `src/main/java/com/vn/core/service/OrganizationService.java` contains `loadByQuery`
    - `src/main/java/com/vn/core/service/OrganizationService.java` contains `Optional<JsonNode>`
    - `src/test/java/com/vn/core/web/rest/SecuredEntityEnforcementIT.java` contains `/api/organizations/query`
    - `./gradlew test --tests "com.vn.core.web.rest.SecuredEntityEnforcementIT"` exits 0
  </acceptance_criteria>
  <done>The protected entity resources now expose raw JSON request carriers and an explicit secured query endpoint that actually reaches the secure data layer.</done>
</task>
</tasks>

<verification>
- `./gradlew test --tests "com.vn.core.security.data.SecureDataManagerImplTest"` passes
- `./gradlew test --tests "com.vn.core.web.rest.SecuredEntityEnforcementIT"` passes
- Protected entity resources no longer expose `Map<String, Object>` request signatures for create/update/query
</verification>

<success_criteria>
The codebase now has an explicit secured JSON query contract and raw JSON create/update boundaries on the protected entity resources, without widening scope into unsupported arbitrary JPQL.
</success_criteria>

<output>
After completion, create `.planning/phases/08.2-multi-app-menu-roles-and-jmix-style-json-entity-controllers/08.2-03-SUMMARY.md`
</output>
