---
phase: 04-protected-entity-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/config/liquibase/changelog/20260321000500_create_proof_organization.xml
  - src/main/resources/config/liquibase/changelog/20260321000600_create_proof_department.xml
  - src/main/resources/config/liquibase/changelog/20260321000700_create_proof_employee.xml
  - src/main/resources/config/liquibase/master.xml
  - src/main/java/com/vn/core/domain/proof/Organization.java
  - src/main/java/com/vn/core/domain/proof/Department.java
  - src/main/java/com/vn/core/domain/proof/Employee.java
  - src/main/java/com/vn/core/repository/proof/OrganizationRepository.java
  - src/main/java/com/vn/core/repository/proof/DepartmentRepository.java
  - src/main/java/com/vn/core/repository/proof/EmployeeRepository.java
autonomous: true
requirements: [ENT-01]

must_haves:
  truths:
    - "Three proof entities exist in a real JPA hierarchy: Organization -> Department -> Employee"
    - "The proof schema includes ownerLogin on Organization for row-policy enforcement and sensitive fields budget and salary for attribute-level proof"
    - "Every proof repository extends both JpaRepository and JpaSpecificationExecutor so SecureDataManager can apply row-policy specs"
    - "The proof schema is registered through Liquibase and loaded by the normal application changelog chain"
  artifacts:
    - path: "src/main/java/com/vn/core/domain/proof/Organization.java"
      provides: "Top-level secured proof entity with ownerLogin and budget"
      contains: "@Table(name = \"proof_organization\")"
    - path: "src/main/java/com/vn/core/domain/proof/Department.java"
      provides: "Middle proof entity linked to Organization and Employee"
      contains: "@JoinColumn(name = \"organization_id\")"
    - path: "src/main/java/com/vn/core/domain/proof/Employee.java"
      provides: "Leaf proof entity with sensitive salary field"
      contains: "@Table(name = \"proof_employee\")"
    - path: "src/main/resources/config/liquibase/master.xml"
      provides: "Liquibase registration for proof schema"
      contains: "20260321000500_create_proof_organization.xml"
  key_links:
    - from: "Organization.java"
      to: "Department.java"
      via: "One-to-many proof hierarchy"
      pattern: "mappedBy = \"organization\""
    - from: "Department.java"
      to: "Employee.java"
      via: "One-to-many proof hierarchy"
      pattern: "mappedBy = \"department\""
    - from: "OrganizationRepository.java"
      to: "RepositoryRegistry"
      via: "Specification-backed lookup compatibility"
      pattern: "JpaSpecificationExecutor<Organization>"
---

<objective>
Create the proof-domain persistence baseline for Phase 4.

Purpose: Phase 4 cannot prove secured CRUD, recursive serialization, or row-level filtering until real JPA entities and tables exist. This plan stands up the concrete proof model and schema that every later Phase 4 plan depends on.
Output: 3 Liquibase changelogs, 3 proof entities, and 3 proof repositories in isolated `proof` packages.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-protected-entity-proof/04-CONTEXT.md
@.planning/phases/04-protected-entity-proof/04-RESEARCH.md
@.planning/phases/03-secure-enforcement-core/03-VERIFICATION.md

<interfaces>
<!-- Existing conventions and integration points the executor must respect -->

Domain patterns from root app:
```java
// src/main/java/com/vn/core/domain/User.java
@Entity
@Table(name = "jhi_user")
public class User implements Serializable { ... }
```

Repository registry requirement:
```java
// src/main/java/com/vn/core/security/repository/RepositoryRegistry.java
<T> JpaRepository<T, ?> getRepository(Class<T> entityClass);
<T> JpaSpecificationExecutor<T> getSpecificationExecutor(Class<T> entityClass);
```

Research-locked proof fields:
```text
Organization: code, name, ownerLogin, budget
Department: code, name, costCenter, organization
Employee: employeeNumber, firstName, lastName, email, salary, department
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Liquibase proof schema changelogs and register them in master.xml</name>
  <files>
    src/main/resources/config/liquibase/changelog/20260321000500_create_proof_organization.xml,
    src/main/resources/config/liquibase/changelog/20260321000600_create_proof_department.xml,
    src/main/resources/config/liquibase/changelog/20260321000700_create_proof_employee.xml,
    src/main/resources/config/liquibase/master.xml
  </files>
  <read_first>
    src/main/resources/config/liquibase/master.xml,
    src/main/resources/config/liquibase/changelog/20260321000200_create_sec_permission.xml,
    src/main/resources/config/liquibase/changelog/20260321000300_create_sec_row_policy.xml,
    .planning/phases/04-protected-entity-proof/04-CONTEXT.md,
    .planning/phases/04-protected-entity-proof/04-RESEARCH.md
  </read_first>
  <action>
    Create these three Liquibase changelog files with one `changeSet` each and include them in `src/main/resources/config/liquibase/master.xml` immediately after `20260321000400_seed_security_roles.xml`:

    1. `20260321000500_create_proof_organization.xml`
    - Create table `proof_organization`
    - Columns:
      - `id bigint` primary key, non-null, autoIncrement true
      - `code varchar(100)` non-null, unique
      - `name varchar(255)` non-null
      - `owner_login varchar(50)` non-null
      - `budget decimal(21,2)` nullable

    2. `20260321000600_create_proof_department.xml`
    - Create table `proof_department`
    - Columns:
      - `id bigint` primary key, non-null, autoIncrement true
      - `code varchar(100)` non-null, unique
      - `name varchar(255)` non-null
      - `cost_center varchar(100)` nullable
      - `organization_id bigint` non-null
    - Add FK `proof_department.organization_id -> proof_organization.id`

    3. `20260321000700_create_proof_employee.xml`
    - Create table `proof_employee`
    - Columns:
      - `id bigint` primary key, non-null, autoIncrement true
      - `employee_number varchar(50)` non-null, unique
      - `first_name varchar(100)` non-null
      - `last_name varchar(100)` non-null
      - `email varchar(255)` nullable
      - `salary decimal(21,2)` nullable
      - `department_id bigint` non-null
    - Add FK `proof_employee.department_id -> proof_department.id`

    Follow existing changelog style: XML header, `databaseChangeLog`, `changeSet id="<timestamp>-1" author="dev"`, `relativeToChangelogFile="false"` includes in `master.xml`.
  </action>
  <verify>
    <automated>grep -rn "proof_organization\|proof_department\|proof_employee" src/main/resources/config/liquibase && grep -rn "20260321000500_create_proof_organization.xml\|20260321000600_create_proof_department.xml\|20260321000700_create_proof_employee.xml" src/main/resources/config/liquibase/master.xml</automated>
  </verify>
  <acceptance_criteria>
    - `20260321000500_create_proof_organization.xml` contains `tableName="proof_organization"` and `owner_login` and `budget`
    - `20260321000600_create_proof_department.xml` contains `tableName="proof_department"` and `organization_id`
    - `20260321000700_create_proof_employee.xml` contains `tableName="proof_employee"` and `department_id` and `salary`
    - `20260321000600_create_proof_department.xml` contains a foreign key to `proof_organization`
    - `20260321000700_create_proof_employee.xml` contains a foreign key to `proof_department`
    - `master.xml` contains all three proof changelog filenames in order after `20260321000400_seed_security_roles.xml`
  </acceptance_criteria>
  <done>The database schema contains the proof tables and the main Liquibase chain loads them automatically.</done>
</task>

<task type="auto">
  <name>Task 2: Create proof entities and repositories in isolated proof packages</name>
  <files>
    src/main/java/com/vn/core/domain/proof/Organization.java,
    src/main/java/com/vn/core/domain/proof/Department.java,
    src/main/java/com/vn/core/domain/proof/Employee.java,
    src/main/java/com/vn/core/repository/proof/OrganizationRepository.java,
    src/main/java/com/vn/core/repository/proof/DepartmentRepository.java,
    src/main/java/com/vn/core/repository/proof/EmployeeRepository.java
  </files>
  <read_first>
    src/main/java/com/vn/core/domain/User.java,
    src/main/java/com/vn/core/domain/Authority.java,
    src/main/java/com/vn/core/security/repository/RepositoriesRegistryImpl.java,
    angapp/src/main/java/com/mycompany/myapp/domain/Organization.java,
    angapp/src/main/java/com/mycompany/myapp/domain/Department.java,
    angapp/src/main/java/com/mycompany/myapp/domain/Employee.java,
    .planning/phases/04-protected-entity-proof/04-CONTEXT.md
  </read_first>
  <action>
    Create `com.vn.core.domain.proof` and `com.vn.core.repository.proof` with these exact classes and fields:

    1. `Organization.java`
    - `@Entity`
    - `@Table(name = "proof_organization")`
    - Fields:
      - `Long id`
      - `String code`
      - `String name`
      - `String ownerLogin`
      - `BigDecimal budget`
      - `Set<Department> departments = new HashSet<>()`
    - Constraints:
      - `@NotNull` on `code`, `name`, `ownerLogin`
      - `@Column(name = "code", nullable = false, unique = true)`
      - `@Column(name = "owner_login", nullable = false)`
    - Relationship:
      - `@OneToMany(mappedBy = "organization", cascade = CascadeType.ALL, orphanRemoval = true)`

    2. `Department.java`
    - `@Entity`
    - `@Table(name = "proof_department")`
    - Fields:
      - `Long id`
      - `String code`
      - `String name`
      - `String costCenter`
      - `Organization organization`
      - `Set<Employee> employees = new HashSet<>()`
    - Relationship:
      - `@ManyToOne(optional = false)` + `@JoinColumn(name = "organization_id", nullable = false)`
      - `@OneToMany(mappedBy = "department", cascade = CascadeType.ALL, orphanRemoval = true)`

    3. `Employee.java`
    - `@Entity`
    - `@Table(name = "proof_employee")`
    - Fields:
      - `Long id`
      - `String employeeNumber`
      - `String firstName`
      - `String lastName`
      - `String email`
      - `BigDecimal salary`
      - `Department department`
    - Relationship:
      - `@ManyToOne(optional = false)` + `@JoinColumn(name = "department_id", nullable = false)`

    For all three entities:
    - implement `Serializable`
    - use the root-app sequence-generator pattern for `id`
    - provide standard getters/setters
    - implement `equals`/`hashCode` on `id`
    - keep package-local dependency shape domain-only; do not inject services or repositories

    Create repositories:
    - `OrganizationRepository extends JpaRepository<Organization, Long>, JpaSpecificationExecutor<Organization>`
    - `DepartmentRepository extends JpaRepository<Department, Long>, JpaSpecificationExecutor<Department>`
    - `EmployeeRepository extends JpaRepository<Employee, Long>, JpaSpecificationExecutor<Employee>`
  </action>
  <verify>
    <automated>./gradlew compileJava --console=plain && grep -rn "JpaSpecificationExecutor<Organization>\|JpaSpecificationExecutor<Department>\|JpaSpecificationExecutor<Employee>" src/main/java/com/vn/core/repository/proof</automated>
  </verify>
  <acceptance_criteria>
    - `Organization.java` contains `package com.vn.core.domain.proof;` and `@Table(name = "proof_organization")`
    - `Organization.java` contains fields `ownerLogin` and `budget`
    - `Department.java` contains `@JoinColumn(name = "organization_id"` and `Set<Employee> employees`
    - `Employee.java` contains `employeeNumber`, `salary`, and `@JoinColumn(name = "department_id"`
    - `OrganizationRepository.java` contains both `JpaRepository<Organization, Long>` and `JpaSpecificationExecutor<Organization>`
    - `DepartmentRepository.java` contains both `JpaRepository<Department, Long>` and `JpaSpecificationExecutor<Department>`
    - `EmployeeRepository.java` contains both `JpaRepository<Employee, Long>` and `JpaSpecificationExecutor<Employee>`
    - `./gradlew compileJava --console=plain` exits 0
  </acceptance_criteria>
  <done>The proof entities and repositories compile and are usable by SecureDataManager and RepositoryRegistry.</done>
</task>

</tasks>

<verification>
- `./gradlew compileJava --console=plain` succeeds
- Liquibase master registers all three proof changelogs
- Proof repositories expose both repository interfaces required by `RepositoryRegistry`
- The domain hierarchy includes the exact row-policy and sensitive fields required by Phase 4 tests
</verification>

<success_criteria>
- Proof tables exist in Liquibase for Organization, Department, and Employee
- Proof entities compile under `com.vn.core.domain.proof`
- Proof repositories compile under `com.vn.core.repository.proof`
- Phase 4 now has a real persistence baseline to register in the secured catalog and expose via API
</success_criteria>

<output>
After completion, create `.planning/phases/04-protected-entity-proof/04-01-SUMMARY.md`
</output>
