---
phase: 04-protected-entity-proof
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/main/java/com/vn/core/service/proof/OrganizationService.java
  - src/main/java/com/vn/core/service/proof/DepartmentService.java
  - src/main/java/com/vn/core/service/proof/EmployeeService.java
  - src/main/java/com/vn/core/web/rest/proof/OrganizationResource.java
  - src/main/java/com/vn/core/web/rest/proof/DepartmentResource.java
  - src/main/java/com/vn/core/web/rest/proof/EmployeeResource.java
autonomous: true
requirements: [ENT-01]

must_haves:
  truths:
    - "All proof REST endpoints go through service classes and SecureDataManager; controllers do not touch repositories"
    - "Proof APIs expose list, single read, create, update, and delete for Organization, Department, and Employee"
    - "Controllers require only isAuthenticated(); CRUD, attribute, and row-level decisions remain inside SecureDataManager"
    - "Responses return the secure fetch-plan-shaped maps directly, with no DTO layer"
  artifacts:
    - path: "src/main/java/com/vn/core/service/proof/OrganizationService.java"
      provides: "Proof service using entity code organization and proof fetch plans"
      contains: "organization-detail"
    - path: "src/main/java/com/vn/core/web/rest/proof/OrganizationResource.java"
      provides: "Protected organization REST API"
      contains: "@RequestMapping(\"/api/proof/organizations\")"
    - path: "src/main/java/com/vn/core/web/rest/proof/DepartmentResource.java"
      provides: "Protected department REST API"
      contains: "@RequestMapping(\"/api/proof/departments\")"
    - path: "src/main/java/com/vn/core/web/rest/proof/EmployeeResource.java"
      provides: "Protected employee REST API"
      contains: "@RequestMapping(\"/api/proof/employees\")"
  key_links:
    - from: "OrganizationService.java"
      to: "SecureDataManager"
      via: "loadByQuery/loadOne/save/delete"
      pattern: "secureDataManager"
    - from: "OrganizationResource.java"
      to: "OrganizationService.java"
      via: "controller-to-service boundary"
      pattern: "private final OrganizationService"
    - from: "DepartmentResource.java"
      to: "PaginationUtil"
      via: "paged list response headers"
      pattern: "generatePaginationHttpHeaders"
---

<objective>
Expose the proof entities through secured service and REST layers.

Purpose: Phase 4 is not proven until the runtime-enforced entities are reachable through real APIs. This plan adds the thin, conventional service and controller surface that exercises the proof entities strictly through SecureDataManager.
Output: 3 proof services and 3 proof REST controllers with minimal CRUD.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-protected-entity-proof/04-CONTEXT.md
@.planning/phases/04-protected-entity-proof/04-RESEARCH.md
@.planning/phases/04-protected-entity-proof/04-01-SUMMARY.md
@.planning/phases/04-protected-entity-proof/04-02-SUMMARY.md

<interfaces>
Required runtime methods after Plan 02:
```java
Page<Map<String, Object>> loadByQuery(SecuredLoadQuery query);
Optional<Map<String, Object>> loadOne(String entityCode, Object id, String fetchPlanCode);
Map<String, Object> save(String entityCode, Object id, Map<String, Object> attributes, String fetchPlanCode);
void delete(String entityCode, Object id);
```

Controller response patterns:
```java
PaginationUtil.generatePaginationHttpHeaders(ServletUriComponentsBuilder.fromCurrentRequest(), page)
ResponseUtil.wrapOrNotFound(optionalMap)
@PreAuthorize("isAuthenticated()")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create proof services that use SecureDataManager only</name>
  <files>
    src/main/java/com/vn/core/service/proof/OrganizationService.java,
    src/main/java/com/vn/core/service/proof/DepartmentService.java,
    src/main/java/com/vn/core/service/proof/EmployeeService.java
  </files>
  <read_first>
    src/main/java/com/vn/core/security/data/SecureDataManager.java,
    src/main/java/com/vn/core/security/data/SecuredLoadQuery.java,
    angapp/src/main/java/com/mycompany/myapp/service/impl/OrganizationServiceImpl.java,
    .planning/phases/04-protected-entity-proof/04-CONTEXT.md,
    .planning/phases/04-protected-entity-proof/04-02-SUMMARY.md
  </read_first>
  <action>
    Create three services under `com.vn.core.service.proof` with constructor injection of `SecureDataManager` and no DTO mappers:

    1. `OrganizationService`
    - constants:
      - `ENTITY_CODE = "organization"`
      - `LIST_FETCH_PLAN = "organization-list"`
      - `DETAIL_FETCH_PLAN = "organization-detail"`
    - methods:
      - `Page<Map<String, Object>> list(Pageable pageable)` -> `secureDataManager.loadByQuery(SecuredLoadQuery.of(ENTITY_CODE, LIST_FETCH_PLAN, pageable))`
      - `Optional<Map<String, Object>> findOne(Long id)` -> `secureDataManager.loadOne(ENTITY_CODE, id, DETAIL_FETCH_PLAN)`
      - `Map<String, Object> create(Map<String, Object> attributes)` -> `secureDataManager.save(ENTITY_CODE, null, attributes, DETAIL_FETCH_PLAN)`
      - `Map<String, Object> update(Long id, Map<String, Object> attributes)` -> `secureDataManager.save(ENTITY_CODE, id, attributes, DETAIL_FETCH_PLAN)`
      - `void delete(Long id)` -> `secureDataManager.delete(ENTITY_CODE, id)`

    2. `DepartmentService`
    - same shape with `department`, `department-list`, `department-detail`

    3. `EmployeeService`
    - same shape with `employee`, `employee-list`, `employee-detail`

    For all three services:
    - annotate `@Service`
    - annotate class `@Transactional`
    - mark list/findOne methods `@Transactional(readOnly = true)`
    - return `Optional<Map<String, Object>>` from single reads; do not translate missing entities inside the service
    - do not inject repositories or EntityManager
  </action>
  <verify>
    <automated>grep -rn "SecuredLoadQuery.of" src/main/java/com/vn/core/service/proof && grep -rn "loadOne(ENTITY_CODE" src/main/java/com/vn/core/service/proof && ./gradlew compileJava --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - `OrganizationService.java` contains `ENTITY_CODE = "organization"` and both `organization-list` and `organization-detail`
    - `DepartmentService.java` contains `ENTITY_CODE = "department"` and both `department-list` and `department-detail`
    - `EmployeeService.java` contains `ENTITY_CODE = "employee"` and both `employee-list` and `employee-detail`
    - All three services call only `secureDataManager.loadByQuery`, `loadOne`, `save`, and `delete`
    - None of the three services inject a repository or `EntityManager`
    - `./gradlew compileJava --console=plain` exits 0
  </acceptance_criteria>
  <done>The proof service layer exists and routes every operation through SecureDataManager.</done>
</task>

<task type="auto">
  <name>Task 2: Create authenticated proof REST controllers with direct secure-map responses</name>
  <files>
    src/main/java/com/vn/core/web/rest/proof/OrganizationResource.java,
    src/main/java/com/vn/core/web/rest/proof/DepartmentResource.java,
    src/main/java/com/vn/core/web/rest/proof/EmployeeResource.java
  </files>
  <read_first>
    src/main/java/com/vn/core/web/rest/UserResource.java,
    src/main/java/com/vn/core/web/rest/admin/security/SecRoleAdminResource.java,
    src/main/java/com/vn/core/web/rest/errors/BadRequestAlertException.java,
    src/main/java/com/vn/core/service/proof/OrganizationService.java,
    src/main/java/com/vn/core/service/proof/DepartmentService.java,
    src/main/java/com/vn/core/service/proof/EmployeeService.java,
    .planning/phases/04-protected-entity-proof/04-CONTEXT.md
  </read_first>
  <action>
    Create three controllers under `com.vn.core.web.rest.proof` with class-level `@PreAuthorize("isAuthenticated()")` and no DTOs:

    1. `OrganizationResource`
    - `@RestController`
    - `@RequestMapping("/api/proof/organizations")`
    - constructor-inject `OrganizationService`
    - endpoints:
      - `@GetMapping` list method with `Pageable pageable`
        - call `organizationService.list(pageable)`
        - generate pagination headers with `PaginationUtil.generatePaginationHttpHeaders(ServletUriComponentsBuilder.fromCurrentRequest(), page)`
        - return `ResponseEntity.ok().headers(headers).body(page.getContent())`
      - `@GetMapping("/{id}")`
        - return `ResponseUtil.wrapOrNotFound(organizationService.findOne(id))`
      - `@PostMapping`
        - accept `@RequestBody Map<String, Object> attributes`
        - call `organizationService.create(attributes)`
        - return `ResponseEntity.created(URI.create("/api/proof/organizations/" + result.get("id"))).body(result)`
      - `@PutMapping("/{id}")`
        - accept `@RequestBody Map<String, Object> attributes`
        - return `ResponseEntity.ok(organizationService.update(id, attributes))`
      - `@DeleteMapping("/{id}")`
        - call `organizationService.delete(id)`
        - return `ResponseEntity.noContent().build()`

    2. `DepartmentResource`
    - same endpoint shape under `/api/proof/departments`

    3. `EmployeeResource`
    - same endpoint shape under `/api/proof/employees`

    For all three controllers:
    - do not inject `SecureDataManager`, repositories, or mappers
    - do not add DTO or VM classes
    - rely on service + existing exception translation for 403/404 behavior
  </action>
  <verify>
    <automated>grep -rn "@RequestMapping(\"/api/proof/organizations\")\|@RequestMapping(\"/api/proof/departments\")\|@RequestMapping(\"/api/proof/employees\")" src/main/java/com/vn/core/web/rest/proof && grep -rn "@PreAuthorize(\"isAuthenticated()\")" src/main/java/com/vn/core/web/rest/proof && ./gradlew compileJava --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - `OrganizationResource.java` contains `@RequestMapping("/api/proof/organizations")` and methods for GET list, GET by id, POST, PUT, DELETE
    - `DepartmentResource.java` contains `@RequestMapping("/api/proof/departments")` and methods for GET list, GET by id, POST, PUT, DELETE
    - `EmployeeResource.java` contains `@RequestMapping("/api/proof/employees")` and methods for GET list, GET by id, POST, PUT, DELETE
    - All three controllers contain `@PreAuthorize("isAuthenticated()")`
    - All three controllers call only their corresponding service
    - The list endpoints use `PaginationUtil.generatePaginationHttpHeaders`
    - The single-read endpoints use `ResponseUtil.wrapOrNotFound`
    - `./gradlew compileJava --console=plain` exits 0
  </acceptance_criteria>
  <done>The proof REST surface exists with authenticated CRUD endpoints for all three proof entities.</done>
</task>

</tasks>

<verification>
- `./gradlew compileJava --console=plain` succeeds
- All three proof services use only SecureDataManager APIs
- All three proof controllers exist under `web/rest/proof`
- All proof endpoints are authenticated and return secure map responses directly
</verification>

<success_criteria>
- `/api/proof/organizations`, `/api/proof/departments`, and `/api/proof/employees` expose minimal CRUD
- The API surface follows the locked controller -> service -> SecureDataManager layering
- Single-record reads can return 404 through `ResponseUtil.wrapOrNotFound`
- The Phase 4 proof entities are now reachable through real backend endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/04-protected-entity-proof/04-03-SUMMARY.md`
</output>
