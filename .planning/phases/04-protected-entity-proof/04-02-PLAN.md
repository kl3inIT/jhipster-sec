---
phase: 04-protected-entity-proof
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/main/java/com/vn/core/security/catalog/SecuredEntity.java
  - src/main/java/com/vn/core/security/catalog/MetamodelSecuredEntityCatalog.java
  - src/main/java/com/vn/core/domain/proof/Organization.java
  - src/main/java/com/vn/core/domain/proof/Department.java
  - src/main/java/com/vn/core/domain/proof/Employee.java
  - src/main/java/com/vn/core/security/data/SecureDataManager.java
  - src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java
  - src/main/java/com/vn/core/security/fetch/YamlFetchPlanRepository.java
  - src/main/resources/fetch-plans.yml
  - src/test/java/com/vn/core/security/data/SecureDataManagerImplTest.java
  - src/test/java/com/vn/core/security/fetch/YamlFetchPlanRepositoryTest.java
autonomous: true
requirements: [ENT-01]

must_haves:
  truths:
    - "Only proof entities explicitly marked for security participation are exposed by the runtime catalog"
    - "Proof entity catalog entries are discovered from the JPA metamodel but still fail closed through an annotation gate"
    - "SecureDataManager supports a secured single-record read path without introducing JPQL access for proof entities"
    - "Any unresolved secured JPQL query path fails closed instead of logging and widening access"
    - "Fetch plans for proof entities can express nested Department and Employee graphs through supported loader syntax"
  artifacts:
    - path: "src/main/java/com/vn/core/security/catalog/MetamodelSecuredEntityCatalog.java"
      provides: "Primary catalog implementation for proof entities"
      contains: "@Primary"
    - path: "src/main/java/com/vn/core/security/data/SecureDataManager.java"
      provides: "Single-record secured read contract"
      contains: "Optional<Map<String, Object>> loadOne"
    - path: "src/main/resources/fetch-plans.yml"
      provides: "Proof fetch plan definitions"
      contains: "organization-detail"
    - path: "src/main/java/com/vn/core/security/fetch/YamlFetchPlanRepository.java"
      provides: "Nested inline-property parsing for proof fetch plans"
      contains: "propNode.has(\"properties\")"
  key_links:
    - from: "Organization.java"
      to: "MetamodelSecuredEntityCatalog.java"
      via: "SecuredEntity annotation gate"
      pattern: "@SecuredEntity"
    - from: "SecureDataManagerImpl.java"
      to: "RepositoryRegistry"
      via: "row-constrained loadOne lookup"
      pattern: "findOne"
    - from: "YamlFetchPlanRepository.java"
      to: "fetch-plans.yml"
      via: "recursive nested property parsing"
      pattern: "properties"
---

<objective>
Wire the proof entities into the Phase 3 enforcement runtime.

Purpose: Phase 4 needs more than tables. The proof entities must be discoverable by the secured catalog, readable through a single-record secured path, and serializable through nested fetch plans that the current YAML parser can actually resolve.
Output: annotation-gated metamodel catalog, `SecureDataManager.loadOne(...)`, fail-closed JPQL behavior, nested YAML fetch-plan support, and populated proof fetch plans.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-protected-entity-proof/04-CONTEXT.md
@.planning/phases/04-protected-entity-proof/04-RESEARCH.md
@.planning/phases/04-protected-entity-proof/04-01-SUMMARY.md

<interfaces>
Existing runtime contracts:
```java
// src/main/java/com/vn/core/security/catalog/SecuredEntityCatalog.java
List<SecuredEntityEntry> entries();
Optional<SecuredEntityEntry> findByEntityClass(Class<?> entityClass);
Optional<SecuredEntityEntry> findByCode(String code);

// src/main/java/com/vn/core/security/data/SecureDataManager.java
Page<Map<String, Object>> loadByQuery(SecuredLoadQuery query);
Map<String, Object> save(String entityCode, Object id, Map<String, Object> attributes, String fetchPlanCode);
void delete(String entityCode, Object id);
```

Current YAML gap:
```text
YamlFetchPlanRepository handles scalar properties and `extends`, but today it does not recursively build nested `properties` blocks.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add secured-entity opt-in annotation and a metamodel-backed primary catalog</name>
  <files>
    src/main/java/com/vn/core/security/catalog/SecuredEntity.java,
    src/main/java/com/vn/core/security/catalog/MetamodelSecuredEntityCatalog.java,
    src/main/java/com/vn/core/domain/proof/Organization.java,
    src/main/java/com/vn/core/domain/proof/Department.java,
    src/main/java/com/vn/core/domain/proof/Employee.java
  </files>
  <read_first>
    src/main/java/com/vn/core/security/catalog/SecuredEntityCatalog.java,
    src/main/java/com/vn/core/security/catalog/SecuredEntityEntry.java,
    src/main/java/com/vn/core/security/catalog/DefaultSecuredEntityCatalog.java,
    src/main/java/com/vn/core/security/permission/EntityOp.java,
    src/main/java/com/vn/core/domain/proof/Organization.java,
    src/main/java/com/vn/core/domain/proof/Department.java,
    src/main/java/com/vn/core/domain/proof/Employee.java,
    .planning/phases/04-protected-entity-proof/04-CONTEXT.md
  </read_first>
  <action>
    Create `src/main/java/com/vn/core/security/catalog/SecuredEntity.java` as a runtime-retained type annotation:
    - `@Target(ElementType.TYPE)`
    - `@Retention(RetentionPolicy.RUNTIME)`

    Annotate all three proof entities with `@SecuredEntity`.

    Create `MetamodelSecuredEntityCatalog` in `com.vn.core.security.catalog`:
    - `@Component`
    - `@Primary`
    - constructor-inject `EntityManager`
    - `entries()` must call `entityManager.getMetamodel().getEntities()`
    - filter to Java types annotated with `@SecuredEntity`
    - map the three proof entities into `SecuredEntityEntry` values with these exact codes and plan names:
      - `Organization.class -> code "organization", fetchPlanCodes ["organization-list", "organization-detail"]`
      - `Department.class -> code "department", fetchPlanCodes ["department-list", "department-detail"]`
      - `Employee.class -> code "employee", fetchPlanCodes ["employee-list", "employee-detail"]`
    - `operations = EnumSet.of(EntityOp.READ, EntityOp.CREATE, EntityOp.UPDATE, EntityOp.DELETE)`
    - `jpqlAllowed = false` for all proof entries
    - keep `findByEntityClass(...)` and `findByCode(...)` backed by `entries()`

    Do not modify `DefaultSecuredEntityCatalog`; the `@Primary` bean replaces it at runtime.
  </action>
  <verify>
    <automated>grep -rn "@SecuredEntity" src/main/java/com/vn/core/domain/proof && grep -rn "class MetamodelSecuredEntityCatalog" src/main/java/com/vn/core/security/catalog && grep -rn "jpqlAllowed(false)" src/main/java/com/vn/core/security/catalog/MetamodelSecuredEntityCatalog.java</automated>
  </verify>
  <acceptance_criteria>
    - `SecuredEntity.java` contains both `@Target(ElementType.TYPE)` and `@Retention(RetentionPolicy.RUNTIME)`
    - `Organization.java`, `Department.java`, and `Employee.java` each contain `@SecuredEntity`
    - `MetamodelSecuredEntityCatalog.java` contains `@Primary` and `entityManager.getMetamodel().getEntities()`
    - `MetamodelSecuredEntityCatalog.java` contains codes `organization`, `department`, and `employee`
    - `MetamodelSecuredEntityCatalog.java` contains fetch plan codes `organization-list`, `organization-detail`, `department-list`, `department-detail`, `employee-list`, `employee-detail`
    - `MetamodelSecuredEntityCatalog.java` contains `jpqlAllowed(false)` or equivalent builder call for all proof entries
  </acceptance_criteria>
  <done>The proof entities are discoverable by the secured runtime through a fail-closed metamodel catalog.</done>
</task>

<task type="auto">
  <name>Task 2: Add nested proof fetch plans and a secured loadOne path, and fail closed on unresolved JPQL</name>
  <files>
    src/main/java/com/vn/core/security/data/SecureDataManager.java,
    src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java,
    src/main/java/com/vn/core/security/fetch/YamlFetchPlanRepository.java,
    src/main/resources/fetch-plans.yml,
    src/test/java/com/vn/core/security/data/SecureDataManagerImplTest.java,
    src/test/java/com/vn/core/security/fetch/YamlFetchPlanRepositoryTest.java
  </files>
  <read_first>
    src/main/java/com/vn/core/security/data/SecureDataManager.java,
    src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java,
    src/main/java/com/vn/core/security/data/SecuredLoadQuery.java,
    src/main/java/com/vn/core/security/fetch/YamlFetchPlanRepository.java,
    src/main/resources/fetch-plans.yml,
    src/test/java/com/vn/core/security/data/SecureDataManagerImplTest.java,
    src/test/java/com/vn/core/security/fetch/YamlFetchPlanRepositoryTest.java,
    .planning/phases/04-protected-entity-proof/04-RESEARCH.md
  </read_first>
  <action>
    Make these exact runtime changes:

    1. Extend `SecureDataManager.java` with:
    ```java
    Optional<Map<String, Object>> loadOne(String entityCode, Object id, String fetchPlanCode);
    ```

    2. Implement `loadOne(...)` in `SecureDataManagerImpl.java` with this flow:
    - resolve `SecuredEntityEntry` by `entityCode`
    - `checkCrud(entityClass, EntityOp.READ)`
    - build `idSpec = (root, query, cb) -> cb.equal(root.get("id"), id)`
    - build `rowSpec = rowLevelSpecificationBuilder.build(entityClass, EntityOp.READ)`
    - query `repositoryRegistry.getSpecificationExecutor(entityClass).findOne(idSpec.and(rowSpec))`
    - if empty: return `Optional.empty()`
    - if present: resolve `FetchPlan`, serialize, return `Optional.of(serializedMap)`

    3. Change the existing `loadByQuery(...)` JPQL placeholder branch in `SecureDataManagerImpl.java`:
    - keep the existing `if (!entry.jpqlAllowed()) throw new AccessDeniedException("JPQL queries not allowed for " + query.entityCode())`
    - replace the current warning-and-continue branch with `throw new AccessDeniedException("JPQL query translation is not implemented for secured queries: " + query.jpql())`
    - do not leave a warn-and-continue path

    4. Extend `YamlFetchPlanRepository.java` so object property nodes with `name` plus nested `properties` arrays are parsed recursively into `FetchPlanProperty(name, nestedFetchPlan, FetchMode.AUTO)`.
    - Preserve current behavior for scalar string properties
    - Preserve current behavior for top-level `extends`
    - Preserve current behavior for object nodes with `fetchPlan`
    - New recursive case:
      - when `propNode.has("properties")`, build a nested `FetchPlan` instance with those child properties
      - assign a deterministic nested plan name like `planName + ":" + propName`
      - use that nested plan in the created `FetchPlanProperty`

    5. Replace `src/main/resources/fetch-plans.yml` contents with proof plans using the new nested inline-property syntax:
    - `com.vn.core.domain.proof.Organization / organization-list`
      - `id`, `code`, `name`, `ownerLogin`
    - `com.vn.core.domain.proof.Organization / organization-detail`
      - `extends: organization-list`
      - add `budget`
      - add nested `departments` with child properties `id`, `code`, `name`, `costCenter`
      - inside `departments`, add nested `employees` with child properties `id`, `employeeNumber`, `firstName`, `lastName`, `email`, `salary`
    - `com.vn.core.domain.proof.Department / department-list`
      - `id`, `code`, `name`, `costCenter`
    - `com.vn.core.domain.proof.Department / department-detail`
      - `extends: department-list`
      - add nested `organization` with `id`, `code`, `name`, `ownerLogin`
      - add nested `employees` with `id`, `employeeNumber`, `firstName`, `lastName`, `email`, `salary`
    - `com.vn.core.domain.proof.Employee / employee-list`
      - `id`, `employeeNumber`, `firstName`, `lastName`, `email`
    - `com.vn.core.domain.proof.Employee / employee-detail`
      - `extends: employee-list`
      - add `salary`
      - add nested `department` with `id`, `code`, `name`

    6. Update tests:
    - `SecureDataManagerImplTest.java`
      - add a `loadOne` success case that verifies `checkCrud -> row spec -> findOne -> resolve -> serialize`
      - add a `loadOne` empty case that returns `Optional.empty()`
      - change the JPQL placeholder assertion to expect `AccessDeniedException` instead of allowing fallback execution
    - `YamlFetchPlanRepositoryTest.java`
      - add a test that loads nested inline `properties` and asserts `organization-detail` contains `departments` with a nested plan, and that nested plan contains `employees` with a nested plan
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.core.security.fetch.YamlFetchPlanRepositoryTest" --tests "com.vn.core.security.data.SecureDataManagerImplTest" --console=plain && grep -rn "loadOne" src/main/java/com/vn/core/security/data/SecureDataManager.java src/main/java/com/vn/core/security/data/SecureDataManagerImpl.java</automated>
  </verify>
  <acceptance_criteria>
    - `SecureDataManager.java` contains `Optional<Map<String, Object>> loadOne(String entityCode, Object id, String fetchPlanCode)`
    - `SecureDataManagerImpl.java` contains a `loadOne` method with `findOne(idSpec.and(rowSpec))`
    - `SecureDataManagerImpl.java` no longer contains a warn-and-continue JPQL branch
    - `SecureDataManagerImpl.java` contains `throw new AccessDeniedException("JPQL query translation is not implemented`
    - `YamlFetchPlanRepository.java` contains a recursive branch for `propNode.has("properties")`
    - `fetch-plans.yml` contains `organization-detail`, `department-detail`, and `employee-detail`
    - `fetch-plans.yml` contains nested `departments`, `employees`, `organization`, and `department` property blocks
    - `./gradlew test --tests "com.vn.core.security.fetch.YamlFetchPlanRepositoryTest" --tests "com.vn.core.security.data.SecureDataManagerImplTest" --console=plain` exits 0
  </acceptance_criteria>
  <done>The proof runtime can resolve nested fetch plans, read a single secured entity safely, and fails closed on unsupported secured JPQL paths.</done>
</task>

</tasks>

<verification>
- The primary secured catalog exposes only the three proof entities
- `SecureDataManager.loadOne(...)` compiles and returns `Optional<Map<String, Object>>`
- `loadByQuery(...)` fails closed on unresolved JPQL rather than widening access
- Proof fetch plans exist and can represent nested Department and Employee graphs
- The two focused unit test classes pass
</verification>

<success_criteria>
- Proof entities are registered in the secured runtime with explicit codes and plan names
- Nested proof read responses can be serialized from supported fetch-plan definitions
- Phase 4 services can ask for a secured single entity without reintroducing direct repository access
- Unsupported JPQL paths are now fail-closed
</success_criteria>

<output>
After completion, create `.planning/phases/04-protected-entity-proof/04-02-SUMMARY.md`
</output>
