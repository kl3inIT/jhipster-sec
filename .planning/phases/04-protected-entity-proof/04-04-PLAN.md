---
phase: 04-protected-entity-proof
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - src/test/resources/config/application.yml
  - src/main/java/com/vn/core/config/LiquibaseConfiguration.java
  - src/test/resources/config/liquibase/test-master.xml
  - src/test/resources/config/liquibase/changelog/20260321000800_seed_proof_security_test_data.xml
  - src/test/java/com/vn/core/web/rest/proof/SecuredEntityEnforcementIT.java
autonomous: true
requirements: [ENT-01, ENT-02]

must_haves:
  truths:
    - "Integration tests run against the full proof schema plus dedicated test security data loaded through test-only Liquibase resources"
    - "All test roles used by @WithMockUser exist in jhi_authority so MergedSecurityContextBridge does not filter them out"
    - "Allow and deny paths are proven through real MockMvc calls, not unit mocks"
    - "Phase 4 proves recursive attribute filtering by showing a nested employee salary field omitted from secured JSON"
    - "Phase 4 proves row-level enforcement by restricting Organization reads, updates, and deletes to ownerLogin = CURRENT_USER_LOGIN"
  artifacts:
    - path: "src/test/resources/config/liquibase/test-master.xml"
      provides: "Test-only Liquibase overlay selected by spring.liquibase.change-log that adds proof seed data"
      contains: "20260321000800_seed_proof_security_test_data.xml"
    - path: "src/test/resources/config/liquibase/changelog/20260321000800_seed_proof_security_test_data.xml"
      provides: "Proof test fixtures for authorities, permissions, row policies, and entity rows"
      contains: "ROLE_PROOF_READER"
    - path: "src/test/java/com/vn/core/web/rest/proof/SecuredEntityEnforcementIT.java"
      provides: "End-to-end allow/deny proof for Phase 4"
      contains: "@AutoConfigureMockMvc"
  key_links:
    - from: "20260321000800_seed_proof_security_test_data.xml"
      to: "MergedSecurityContextBridge"
      via: "jhi_authority rows for test roles"
      pattern: "ROLE_PROOF_EDITOR"
    - from: "SecuredEntityEnforcementIT.java"
      to: "OrganizationResource.java"
      via: "MockMvc API calls"
      pattern: "/api/proof/organizations"
    - from: "SecuredEntityEnforcementIT.java"
      to: "RowLevelPolicyProviderDbImpl"
      via: "ownerLogin row-policy seed"
      pattern: "CURRENT_USER_LOGIN"
---

<objective>
Prove Phase 4 end to end with backend allow/deny coverage.

Purpose: The phase goal is not "entities exist" but "the merged security engine is proven against them." This plan seeds deterministic proof data and runs real integration tests against the protected APIs to verify CRUD allow/deny, recursive attribute filtering, and row-level behavior.
Output: test-only Liquibase fixtures and one dedicated `SecuredEntityEnforcementIT`.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-protected-entity-proof/04-CONTEXT.md
@.planning/phases/04-protected-entity-proof/04-VALIDATION.md
@.planning/phases/04-protected-entity-proof/04-01-SUMMARY.md
@.planning/phases/04-protected-entity-proof/04-02-SUMMARY.md
@.planning/phases/04-protected-entity-proof/04-03-SUMMARY.md

<interfaces>
Test base patterns:
```java
@IntegrationTest
@AutoConfigureMockMvc
@Transactional
class SomeResourceIT { ... }
```

Phase-4 locked behaviors to prove:
```text
CRUD deny -> 403
Attribute read deny -> field absent from JSON
Attribute write deny -> 403
Row-level policy -> only permitted rows returned
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test-only Liquibase overlay and deterministic proof security fixtures</name>
  <files>
    src/test/resources/config/application.yml,
    src/main/java/com/vn/core/config/LiquibaseConfiguration.java,
    src/test/resources/config/liquibase/test-master.xml,
    src/test/resources/config/liquibase/changelog/20260321000800_seed_proof_security_test_data.xml
  </files>
  <read_first>
    src/main/resources/config/liquibase/master.xml,
    src/test/resources/config/application.yml,
    src/main/resources/config/liquibase/changelog/20260321000400_seed_security_roles.xml,
    .planning/phases/04-protected-entity-proof/04-CONTEXT.md,
    .planning/phases/04-protected-entity-proof/04-RESEARCH.md
  </read_first>
  <action>
    Create a test-only Liquibase overlay by adding `src/test/resources/config/liquibase/test-master.xml`, then point `src/test/resources/config/application.yml` at it via `spring.liquibase.change-log`. `LiquibaseConfiguration` must honor that property instead of hardcoding the production changelog. The dedicated test entrypoint must include:
    - `00000000000000_initial_schema.xml`
    - `20260321000100_add_authority_type_columns.xml`
    - `20260321000200_create_sec_permission.xml`
    - `20260321000300_create_sec_row_policy.xml`
    - `20260321000400_seed_security_roles.xml`
    - `20260321000500_create_proof_organization.xml`
    - `20260321000600_create_proof_department.xml`
    - `20260321000700_create_proof_employee.xml`
    - new test-only `20260321000800_seed_proof_security_test_data.xml`

    Create `src/test/resources/config/liquibase/changelog/20260321000800_seed_proof_security_test_data.xml` with these exact rows:

    1. `jhi_authority`
    - `ROLE_PROOF_READER`
    - `ROLE_PROOF_EDITOR`
    - `ROLE_PROOF_NONE`

    2. `proof_organization`
    - id `100`, code `ORG-OWNED`, name `Owned Org`, owner_login `proof-owner`, budget `100000.00`
    - id `101`, code `ORG-OTHER`, name `Other Org`, owner_login `other-user`, budget `250000.00`

    3. `proof_department`
    - id `200`, code `DEPT-OWNED`, name `Owned Department`, cost_center `FIN`, organization_id `100`
    - id `201`, code `DEPT-OTHER`, name `Other Department`, cost_center `SALES`, organization_id `101`

    4. `proof_employee`
    - id `300`, employee_number `EMP-001`, first_name `Alice`, last_name `Owner`, email `alice@example.com`, salary `9000.00`, department_id `200`
    - id `301`, employee_number `EMP-002`, first_name `Bob`, last_name `Other`, email `bob@example.com`, salary `12000.00`, department_id `201`

    5. `sec_permission`
    - for `ROLE_PROOF_READER`
      - ENTITY READ ALLOW on `ORGANIZATION`
      - ENTITY READ ALLOW on `DEPARTMENT`
      - ENTITY READ ALLOW on `EMPLOYEE`
      - ATTRIBUTE VIEW DENY on `ORGANIZATION.BUDGET`
      - ATTRIBUTE VIEW DENY on `EMPLOYEE.SALARY`
    - for `ROLE_PROOF_EDITOR`
      - ENTITY CREATE/READ/UPDATE/DELETE ALLOW on `ORGANIZATION`
      - ENTITY READ ALLOW on `DEPARTMENT`
      - ENTITY READ ALLOW on `EMPLOYEE`
      - ATTRIBUTE EDIT DENY on `ORGANIZATION.BUDGET`

    6. `sec_row_policy`
    - code `PROOF_ORG_READ_OWNERSHIP`, entity_name `Organization`, operation `READ`, policy_type `SPECIFICATION`, expression `ownerLogin = CURRENT_USER_LOGIN`
    - code `PROOF_ORG_UPDATE_OWNERSHIP`, entity_name `Organization`, operation `UPDATE`, policy_type `SPECIFICATION`, expression `ownerLogin = CURRENT_USER_LOGIN`
    - code `PROOF_ORG_DELETE_OWNERSHIP`, entity_name `Organization`, operation `DELETE`, policy_type `SPECIFICATION`, expression `ownerLogin = CURRENT_USER_LOGIN`

    Keep the changelog in the test resources tree only. Do not add these proof test fixtures to the main Liquibase chain.
  </action>
  <verify>
    <automated>grep -rn "20260321000800_seed_proof_security_test_data.xml" src/test/resources/config/liquibase/test-master.xml && grep -rn "ROLE_PROOF_READER\|ROLE_PROOF_EDITOR\|ROLE_PROOF_NONE\|ORG-OWNED\|PROOF_ORG_READ_OWNERSHIP" src/test/resources/config/liquibase/changelog/20260321000800_seed_proof_security_test_data.xml && grep -rn "change-log: classpath:config/liquibase/test-master.xml" src/test/resources/config/application.yml && grep -rn "liquibaseProperties.getChangeLog()" src/main/java/com/vn/core/config/LiquibaseConfiguration.java</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/resources/config/liquibase/test-master.xml` exists and contains all main changelog includes plus `20260321000800_seed_proof_security_test_data.xml`
    - `20260321000800_seed_proof_security_test_data.xml` contains `ROLE_PROOF_READER`, `ROLE_PROOF_EDITOR`, and `ROLE_PROOF_NONE`
    - `20260321000800_seed_proof_security_test_data.xml` contains `ORG-OWNED` and `ORG-OTHER`
    - `20260321000800_seed_proof_security_test_data.xml` contains `ORGANIZATION.BUDGET` and `EMPLOYEE.SALARY`
    - `20260321000800_seed_proof_security_test_data.xml` contains `ownerLogin = CURRENT_USER_LOGIN`
    - Test fixtures live only under `src/test/resources/config/liquibase`
  </acceptance_criteria>
  <done>The integration-test runtime loads deterministic proof data, permissions, and row policies without polluting the main application dataset.</done>
</task>

<task type="auto">
  <name>Task 2: Create SecuredEntityEnforcementIT with allow/deny proof across CRUD, attributes, and row policies</name>
  <files>
    src/test/java/com/vn/core/web/rest/proof/SecuredEntityEnforcementIT.java
  </files>
  <read_first>
    src/test/java/com/vn/core/IntegrationTest.java,
    src/test/java/com/vn/core/web/rest/UserResourceIT.java,
    src/test/java/com/vn/core/web/rest/admin/security/SecRoleAdminResourceIT.java,
    src/main/java/com/vn/core/web/rest/proof/OrganizationResource.java,
    src/main/java/com/vn/core/web/rest/proof/DepartmentResource.java,
    src/main/java/com/vn/core/web/rest/proof/EmployeeResource.java,
    src/main/java/com/vn/core/security/bridge/MergedSecurityContextBridge.java,
    .planning/phases/04-protected-entity-proof/04-VALIDATION.md
  </read_first>
  <action>
    Create `src/test/java/com/vn/core/web/rest/proof/SecuredEntityEnforcementIT.java` with:
    - `@IntegrationTest`
    - `@AutoConfigureMockMvc`
    - `@Transactional`
    - injected `MockMvc` and `ObjectMapper`

    Use these exact base URLs:
    - `"/api/proof/organizations"`
    - `"/api/proof/departments"`
    - `"/api/proof/employees"`

    Add these exact integration tests:

    1. `getOrganizations_returnsOwnedRowsAndOmitsDeniedFields()`
    - `@WithMockUser(username = "proof-owner", authorities = "ROLE_PROOF_READER")`
    - GET `/api/proof/organizations?sort=id,asc`
    - expect 200
    - expect only one returned row with `id = 100`
    - expect top-level `budget` absent
    - expect nested `departments[0].employees[0].salary` absent
    - expect nested department and employee ids/names present

    2. `getOrganizations_withoutReadPermission_returnsForbidden()`
    - `@WithMockUser(username = "proof-owner", authorities = "ROLE_PROOF_NONE")`
    - GET `/api/proof/organizations`
    - expect 403

    3. `getOrganization_outsideRowPolicy_returnsNotFound()`
    - `@WithMockUser(username = "proof-owner", authorities = "ROLE_PROOF_READER")`
    - GET `/api/proof/organizations/101`
    - expect 404 because `loadOne(...)` returns empty when the row-policy-constrained lookup cannot see the row

    4. `createOrganization_withEditorRole_returnsCreated()`
    - `@WithMockUser(username = "proof-owner", authorities = "ROLE_PROOF_EDITOR")`
    - POST `/api/proof/organizations`
    - JSON body contains `code = "ORG-NEW"`, `name = "New Org"`, `ownerLogin = "proof-owner"`, `budget = 123.45`
    - expect 201
    - expect response body contains `code = "ORG-NEW"` and `name = "New Org"`

    5. `updateOrganization_withDeniedBudgetEdit_returnsForbidden()`
    - `@WithMockUser(username = "proof-owner", authorities = "ROLE_PROOF_EDITOR")`
    - PUT `/api/proof/organizations/100`
    - JSON body contains `name = "Owned Org Updated"` and `budget = 999999.00`
    - expect 403

    6. `deleteOrganization_outsideRowPolicy_returnsForbidden()`
    - `@WithMockUser(username = "proof-owner", authorities = "ROLE_PROOF_EDITOR")`
    - DELETE `/api/proof/organizations/101`
    - expect 403

    7. `getDepartments_withReadPermission_returnsOk()`
    - `@WithMockUser(username = "proof-owner", authorities = "ROLE_PROOF_READER")`
    - GET `/api/proof/departments?sort=id,asc`
    - expect 200
    - expect returned ids include `200`

    8. `getEmployees_withReadPermission_returnsOk()`
    - `@WithMockUser(username = "proof-owner", authorities = "ROLE_PROOF_READER")`
    - GET `/api/proof/employees?sort=id,asc`
    - expect 200
    - expect returned ids include `300`

    Use `jsonPath` assertions only; do not inspect repositories directly in the test body.
  </action>
  <verify>
    <automated>./gradlew integrationTest --tests "com.vn.core.web.rest.proof.SecuredEntityEnforcementIT" --console=plain && ./gradlew test integrationTest --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - `SecuredEntityEnforcementIT.java` contains `@IntegrationTest`, `@AutoConfigureMockMvc`, and `@Transactional`
    - `SecuredEntityEnforcementIT.java` contains the three base URLs for organizations, departments, and employees
    - `SecuredEntityEnforcementIT.java` contains `ROLE_PROOF_READER`, `ROLE_PROOF_EDITOR`, and `ROLE_PROOF_NONE`
    - `SecuredEntityEnforcementIT.java` contains a 403 assertion for no-read-permission access
    - `SecuredEntityEnforcementIT.java` contains a 404 assertion for row-policy-excluded single-record access
    - `SecuredEntityEnforcementIT.java` contains assertions that `budget` is absent and nested `salary` is absent
    - `./gradlew integrationTest --tests "com.vn.core.web.rest.proof.SecuredEntityEnforcementIT" --console=plain` exits 0
    - `./gradlew test integrationTest --console=plain` exits 0
  </acceptance_criteria>
  <done>Phase 4 is proven by a dedicated integration suite that exercises allow and deny paths against the real protected APIs.</done>
</task>

</tasks>

<verification>
- Test-only Liquibase resources load proof seed data and proof security rows
- The proof IT exercises CRUD allow/deny, recursive attribute omission, and row-level restriction
- The dedicated IT runs green in isolation and as part of the full suite
</verification>

<success_criteria>
- ENT-01 is proven by real protected entities and endpoints
- ENT-02 is proven by automated backend tests covering allow and deny scenarios
- The phase has one dedicated end-to-end enforcement proof instead of scattered partial tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-protected-entity-proof/04-04-SUMMARY.md`
</output>
