---
phase: 06-frontend-parity-foundation
plan: 05
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - frontend/src/app.routes.ts
  - frontend/src/app/app.routes.ts
  - frontend/src/app/pages/admin/admin.routes.ts
  - frontend/src/app/pages/admin/security/security.routes.ts
  - frontend/src/app.routes.spec.ts
autonomous: true
requirements: [I18N-01, I18N-02]

must_haves:
  truths:
    - "The live router tree exposes translated login, home, error, security, and user-management foundations instead of leaving them isolated in unused files"
    - "The admin route tree mounts both security routes and the user-management route skeleton under a shared guarded /admin boundary"
    - "The stale duplicate frontend/src/app/app.routes.ts no longer contradicts the live router source of truth"
  artifacts:
    - path: "frontend/src/app.routes.ts"
      provides: "Authoritative root route tree for the standalone app"
    - path: "frontend/src/app/pages/admin/admin.routes.ts"
      provides: "Admin child-route tree for security and user management"
    - path: "frontend/src/app.routes.spec.ts"
      provides: "Route reachability verification for login/home/error/admin/users/security"
  key_links:
    - from: "frontend/src/app.routes.ts"
      to: "frontend/src/app/pages/admin/admin.routes.ts"
      via: "loadChildren for /admin"
      pattern: "loadChildren: \\(\\) => import\\('./app/pages/admin/admin.routes'\\)"
    - from: "frontend/src/app/pages/admin/admin.routes.ts"
      to: "frontend/src/app/pages/admin/user-management/user-management.routes.ts"
      via: "admin users mount"
      pattern: "import\\('./user-management/user-management.routes'\\)"
---

<objective>
Wire the translated shell and user-management foundations into the live router tree.

Purpose: Phase 6 only achieves its goal if the copied routes and translated pages are actually reachable through the standalone app's root and admin router trees.
Output: A single authoritative route tree, a mounted `/admin/users` child route, translated route titles, and a route reachability spec.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-parity-foundation/06-CONTEXT.md
@.planning/phases/06-frontend-parity-foundation/06-RESEARCH.md
@.planning/phases/06-frontend-parity-foundation/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the root and admin route trees and verify reachability</name>
  <files>
    frontend/src/app.routes.ts,
    frontend/src/app/app.routes.ts,
    frontend/src/app/pages/admin/admin.routes.ts,
    frontend/src/app/pages/admin/security/security.routes.ts,
    frontend/src/app.routes.spec.ts
  </files>
  <read_first>
    frontend/src/app.routes.ts,
    frontend/src/app/app.routes.ts,
    frontend/src/app/pages/admin/security/security.routes.ts,
    frontend/src/app/pages/admin/user-management/user-management.routes.ts,
    frontend/src/app/core/auth/user-route-access.service.ts,
    frontend/src/app/app-page-title-strategy.ts
  </read_first>
  <action>
Create `frontend/src/app/pages/admin/admin.routes.ts` with a default `Routes` export that mounts:
- `path: 'security'` -> `loadChildren: () => import('./security/security.routes')`
- `path: 'users'` -> `loadChildren: () => import('./user-management/user-management.routes')`

Update `frontend/src/app.routes.ts` so the authoritative root route tree explicitly includes:
- `path: 'login'` with `title: 'pageTitle.login'`
- `path: ''` layout shell with child routes for:
  - `path: ''` home route with `title: 'pageTitle.home'`
  - `path: 'accessdenied'` with `title: 'pageTitle.accessDenied'`
  - `path: '404'` with `title: 'pageTitle.notFound'`
  - `path: 'error'` with `title: 'pageTitle.error'`
  - `path: 'entities'` for the existing entity lazy routes
  - `path: 'admin'` with `loadChildren: () => import('./app/pages/admin/admin.routes')`, `canActivate: [UserRouteAccessService]`, and `data: { authorities: ['ROLE_ADMIN'] }`
- keep the final wildcard redirect to `404`

Update `frontend/src/app/pages/admin/security/security.routes.ts` so the child titles use translation keys:
- `pageTitle.securityRoles`
- `pageTitle.permissionMatrix`
- `pageTitle.rowPolicies`

Update `frontend/src/app/app.routes.ts` so it no longer contains an empty stale route array. Re-export the authoritative root route tree from `../app.routes`, or mirror it exactly, so AGENTS/context readers do not see a contradictory empty route definition.

Create `frontend/src/app.routes.spec.ts` that verifies:
- the root route tree includes `login`, `''`, `accessdenied`, `404`, `error`, `entities`, and `admin`
- the `admin` route uses `data.authorities = ['ROLE_ADMIN']`
- the admin child routes include `security` and `users`
- the security route titles are translation keys and the user-management lazy route is mounted under `/admin/users`
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <automated>npm --prefix frontend exec ng test -- --watch=false --include src/app.routes.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app.routes.ts` contains `path: 'login'`
    - `frontend/src/app.routes.ts` contains `title: 'pageTitle.login'`
    - `frontend/src/app.routes.ts` contains `path: 'admin'`
    - `frontend/src/app.routes.ts` contains `import('./app/pages/admin/admin.routes')`
    - `frontend/src/app.routes.ts` contains `authorities: ['ROLE_ADMIN']`
    - `frontend/src/app/pages/admin/admin.routes.ts` exists and contains `path: 'security'`
    - `frontend/src/app/pages/admin/admin.routes.ts` contains `path: 'users'`
    - `frontend/src/app/pages/admin/security/security.routes.ts` contains `title: 'pageTitle.securityRoles'`
    - `frontend/src/app/app.routes.ts` no longer contains `export const routes: Routes = [];`
    - `frontend/src/app.routes.spec.ts` exists and contains assertions for `admin` and `users`
  </acceptance_criteria>
  <done>The standalone router tree exposes the translated shell pages and mounts the user-management foundation under `/admin/users` through a single authoritative route definition.</done>
</task>

</tasks>

<verification>
- `npm --prefix frontend run build` passes
- `npm --prefix frontend exec ng test -- --watch=false --include src/app.routes.spec.ts` passes
</verification>

<success_criteria>
- The live router tree exposes login, home, error, security, and user-management foundations through one authoritative route source
- `/admin/users` is mounted through the guarded admin child route tree
- Both root and security route titles are translation keys compatible with `AppPageTitleStrategy`
- `frontend/src/app/app.routes.ts` no longer misrepresents the live route tree as empty
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-parity-foundation/06-05-SUMMARY.md`
</output>
