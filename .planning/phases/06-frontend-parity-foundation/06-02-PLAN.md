---
phase: 06-frontend-parity-foundation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/app/core/util/alert.service.ts
  - frontend/src/app/shared/alert/alert.component.ts
  - frontend/src/app/shared/auth/has-any-authority.directive.ts
  - frontend/src/app/core/interceptor/notification.interceptor.ts
  - frontend/src/app/layout/component/main/app.layout.ts
  - frontend/src/app/core/interceptor/notification.interceptor.spec.ts
  - frontend/src/app/core/util/alert.service.spec.ts
autonomous: true
requirements: [I18N-01, I18N-02]

must_haves:
  truths:
    - "Backend app-alert and app-params headers surface as visible translated feedback instead of console-only logging, with current jhipstersec.* alert keys normalized into canonical frontend translation keys before rendering"
    - "A donor-compatible template authority directive exists so later angapp template ports can gate content without re-implementing authority checks"
    - "The PrimeNG shell has a mounted alert outlet before translated page copy work begins"
  artifacts:
    - path: "frontend/src/app/core/util/alert.service.ts"
      provides: "Translated alert state and add/dismiss behavior"
    - path: "frontend/src/app/shared/alert/alert.component.ts"
      provides: "Visible alert outlet mounted into the shell"
    - path: "frontend/src/app/shared/auth/has-any-authority.directive.ts"
      provides: "Donor-compatible template authority gating"
  key_links:
    - from: "frontend/src/app/core/interceptor/notification.interceptor.ts"
      to: "frontend/src/app/core/util/alert.service.ts"
      via: "translated success alerts"
      pattern: "addAlert\\("
    - from: "frontend/src/app/core/interceptor/notification.interceptor.ts"
      to: "frontend/public/i18n/en.json"
      via: "normalize backend jhipstersec.* alert keys into canonical donor or app-specific translation keys"
      pattern: "normalizeAlertTranslationKey|jhipstersec\\."
    - from: "frontend/src/app/layout/component/main/app.layout.ts"
      to: "frontend/src/app/shared/alert/alert.component.ts"
      via: "shell-level alert outlet"
      pattern: "jhi-alert|app-alert"
---

<objective>
Port the shared alert and authority helpers that other translated shell work will consume.

Purpose: Phase 6 needs the donor alert semantics and template authority directive before topbar, menu, and admin foundations start consuming translated runtime behavior.
Output: Visible alert outlet, translated alert service, and donor-compatible template authority gating.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-parity-foundation/06-CONTEXT.md
@.planning/phases/06-frontend-parity-foundation/06-RESEARCH.md
@.planning/phases/06-frontend-parity-foundation/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port alert, notification, and template authority helpers</name>
  <files>
    frontend/src/app/core/util/alert.service.ts,
    frontend/src/app/shared/alert/alert.component.ts,
    frontend/src/app/shared/auth/has-any-authority.directive.ts,
    frontend/src/app/core/interceptor/notification.interceptor.ts,
    frontend/src/app/layout/component/main/app.layout.ts,
    frontend/src/app/core/interceptor/notification.interceptor.spec.ts,
    frontend/src/app/core/util/alert.service.spec.ts
  </files>
  <read_first>
    frontend/src/app/core/interceptor/notification.interceptor.ts,
    frontend/src/app/layout/component/main/app.layout.ts,
    frontend/src/app/core/auth/account.service.ts,
    src/main/java/com/vn/core/web/rest/AuthorityResource.java,
    src/main/java/com/vn/core/web/rest/admin/security/SecRoleAdminResource.java,
    angapp/src/main/webapp/app/core/util/alert.service.ts,
    angapp/src/main/webapp/app/shared/alert/alert.component.ts,
    angapp/src/main/webapp/app/shared/auth/has-any-authority.directive.ts
  </read_first>
  <action>
Create `frontend/src/app/core/util/alert.service.ts` by adapting the donor alert contract to the PrimeNG shell. Include:
- `type AlertType = 'success' | 'danger' | 'warning' | 'info'`
- `interface Alert` with `translationKey`, `translationParams`, `timeout`, `toast`, `position`, and `close`
- default timeout `5000`
- `addAlert(...)`, `get()`, `clear()`, and close behavior that translate `translationKey` through `TranslateService.instant(...)`

Create `frontend/src/app/shared/alert/alert.component.ts` as a standalone component with selector `jhi-alert`. Render the current alerts from `AlertService` with PrimeNG components (`MessageModule`, `ToastModule`, or both), not ng-bootstrap alerts. Mount the alert outlet in `frontend/src/app/layout/component/main/app.layout.ts` above `<router-outlet>`.

Create `frontend/src/app/shared/auth/has-any-authority.directive.ts` with selector `[jhiHasAnyAuthority]`, backed by `AccountService.trackCurrentAccount()` and `AccountService.hasAnyAuthority(...)`. Keep the donor-compatible selector so later angapp templates can port cleanly.

Update `frontend/src/app/core/interceptor/notification.interceptor.ts` to:
- inspect response headers for keys ending with `app-alert`
- inspect response headers for keys ending with `app-params`
- decode `app-params`
- add a small `normalizeAlertTranslationKey(...)` helper before publishing alerts
- treat backend `jhipstersec.*` values as transport/header inputs only, not as canonical frontend translation keys
- normalize to the canonical frontend key contract established by Plan 06-01:
  - `jhipstersec.userManagement.created|updated|deleted` -> `userManagement.created|updated|deleted`
  - `jhipstersec.adminAuthority.created|updated|deleted` -> `angappApp.adminAuthority.created|updated|deleted`
  - `jhipstersec.organization.created|updated|deleted` -> `angappApp.organization.created|updated|deleted`
  - `jhipstersec.department.created|updated|deleted` -> `angappApp.department.created|updated|deleted`
  - `jhipstersec.secRole.created|updated|deleted` -> `security.alert.secRole.created|updated|deleted`
  - `jhipstersec.secPermission.created|updated|deleted` -> `security.alert.secPermission.created|updated|deleted`
  - `jhipstersec.secRowPolicy.created|updated|deleted` -> `security.alert.secRowPolicy.created|updated|deleted`
  - unknown keys pass through unchanged
- call `alertService.addAlert({ type: 'success', translationKey: normalizeAlertTranslationKey(alert), translationParams: { param: alertParams } })`
- remove the `console.warn('App alert:', alert)` path

Create tests:
- `frontend/src/app/core/interceptor/notification.interceptor.spec.ts` covering `app-alert`, `app-params`, and normalization for:
  - a donor user-management key such as `jhipstersec.userManagement.created` -> `userManagement.created`
  - a donor-backed entity/admin key such as `jhipstersec.adminAuthority.created` -> `angappApp.adminAuthority.created`
  - a current security-admin key such as `jhipstersec.secRole.created` -> `security.alert.secRole.created`
- `frontend/src/app/core/util/alert.service.spec.ts` covering translated message resolution and dismissal for both donor canonical keys and app-specific security alert keys
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <automated>npm --prefix frontend exec ng test -- --watch=false --include src/app/core/interceptor/notification.interceptor.spec.ts --include src/app/core/util/alert.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/core/util/alert.service.ts` exists and contains `translationKey`
    - `frontend/src/app/core/util/alert.service.ts` contains `translationParams`
    - `frontend/src/app/shared/alert/alert.component.ts` exists and contains `selector: 'jhi-alert'`
    - `frontend/src/app/shared/auth/has-any-authority.directive.ts` exists and contains `selector: '[jhiHasAnyAuthority]'`
    - `frontend/src/app/core/interceptor/notification.interceptor.ts` contains `app-params`
    - `frontend/src/app/core/interceptor/notification.interceptor.ts` contains `normalizeAlertTranslationKey`
    - `frontend/src/app/core/interceptor/notification.interceptor.ts` contains `jhipstersec.`
    - `frontend/src/app/core/interceptor/notification.interceptor.ts` contains `angappApp.adminAuthority` and `security.alert.secRole`
    - `frontend/src/app/core/interceptor/notification.interceptor.ts` contains `this.alertService.addAlert(`
    - `frontend/src/app/core/interceptor/notification.interceptor.ts` no longer contains `console.warn('App alert:'`
    - `frontend/src/app/layout/component/main/app.layout.ts` contains `jhi-alert`
    - `frontend/src/app/core/interceptor/notification.interceptor.spec.ts` exists
    - `frontend/src/app/core/interceptor/notification.interceptor.spec.ts` contains assertions for `userManagement.created`, `angappApp.adminAuthority.created`, and `security.alert.secRole.created`
    - `frontend/src/app/core/util/alert.service.spec.ts` exists
  </acceptance_criteria>
  <done>Success headers become visible translated alerts in the shell after backend jhipstersec.* keys are normalized into canonical donor or app-specific frontend translation keys, and donor-compatible alert and authority helpers are available for later template ports.</done>
</task>

</tasks>

<verification>
- `npm --prefix frontend run build` passes
- `npm --prefix frontend exec ng test -- --watch=false --include src/app/core/interceptor/notification.interceptor.spec.ts --include src/app/core/util/alert.service.spec.ts` passes
</verification>

<success_criteria>
- Visible alerts replace console-only success feedback, including current backend jhipstersec.* entity and security alerts
- A reusable `[jhiHasAnyAuthority]` directive exists for later donor template ports
- The shell mounts a shared alert outlet that later translated surfaces can reuse
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-parity-foundation/06-02-SUMMARY.md`
</output>
