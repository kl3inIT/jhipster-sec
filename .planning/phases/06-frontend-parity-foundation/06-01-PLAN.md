---
phase: 06-frontend-parity-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/public/i18n/en.json
  - frontend/public/i18n/vi.json
  - frontend/src/app/config/translation.config.ts
  - frontend/src/app/config/language.constants.ts
  - frontend/src/app/config/dayjs.ts
  - frontend/src/app/app-page-title-strategy.ts
  - frontend/src/app.config.ts
  - frontend/src/app/app.ts
  - frontend/src/app/shared/language/translate.directive.ts
autonomous: true
requirements: [I18N-01, I18N-02]

must_haves:
  truths:
    - "The standalone frontend loads translations from frontend/public/i18n/en.json and frontend/public/i18n/vi.json through ngx-translate providers in frontend/src/app.config.ts, preserving donor canonical hierarchies including angappApp.adminAuthority.*, angappApp.organization.*, and angappApp.department.*"
    - "Vietnamese is the default first-load locale, fallbackLang is en, and a stored locale in StateStorageService overrides the default while account.langKey only applies when no stored locale exists"
    - "Document title, html[lang], and dayjs locale update when the active language changes using registered vi and en dayjs locale packs"
  artifacts:
    - path: "frontend/src/app/config/translation.config.ts"
      provides: "Missing-translation handler and static i18n loader contract"
    - path: "frontend/src/app/config/dayjs.ts"
      provides: "Registered vi/en dayjs locale helper for runtime language changes"
    - path: "frontend/src/app/app-page-title-strategy.ts"
      provides: "Route-title translation strategy"
    - path: "frontend/src/app.config.ts"
      provides: "Standalone root translation/bootstrap wiring"
    - path: "frontend/public/i18n/vi.json"
      provides: "Merged Vietnamese donor translations preserving canonical donor hierarchies plus shell-specific app keys"
    - path: "frontend/public/i18n/en.json"
      provides: "Merged English donor translations preserving canonical donor hierarchies plus shell-specific app keys"
  key_links:
    - from: "frontend/src/app.config.ts"
      to: "frontend/src/app/config/translation.config.ts"
      via: "provideTranslateService + missing translation handler"
      pattern: "provideTranslateService\\("
    - from: "frontend/src/app/app.ts"
      to: "frontend/src/app/core/auth/state-storage.service.ts"
      via: "stored locale precedence"
      pattern: "getLocale\\("
    - from: "frontend/src/app/app.ts"
      to: "frontend/src/app/app-page-title-strategy.ts"
      via: "language-change title refresh"
      pattern: "updateTitle\\("
---

<objective>
Establish the standalone i18n runtime and locale lifecycle that every later shell and admin migration depends on.

Purpose: Phase 6 cannot safely port donor support files until the Angular 21 frontend has a real translation bootstrap, merged donor assets, and route-title/language-change plumbing.
Output: Root translation providers, merged vi/en JSON bundles, route-title strategy, and locale precedence tests.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-parity-foundation/06-CONTEXT.md
@.planning/phases/06-frontend-parity-foundation/06-RESEARCH.md
@.planning/phases/06-frontend-parity-foundation/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create merged translation assets and translation config</name>
  <files>
    frontend/public/i18n/en.json,
    frontend/public/i18n/vi.json,
    frontend/src/app/config/translation.config.ts,
    frontend/src/app/config/language.constants.ts,
    frontend/src/app/config/dayjs.ts,
    frontend/src/app/config/translation.config.spec.ts
  </files>
  <read_first>
    frontend/angular.json,
    frontend/package.json,
    angapp/src/main/webapp/app/config/translation.config.ts,
    angapp/src/main/webapp/app/entities/entity.routes.ts,
    angapp/src/main/webapp/i18n/en/global.json,
    angapp/src/main/webapp/i18n/en/login.json,
    angapp/src/main/webapp/i18n/en/home.json,
    angapp/src/main/webapp/i18n/en/error.json,
    angapp/src/main/webapp/i18n/en/user-management.json,
    angapp/src/main/webapp/i18n/en/adminAuthority.json,
    angapp/src/main/webapp/i18n/en/organization.json,
    angapp/src/main/webapp/i18n/en/department.json,
    angapp/src/main/webapp/i18n/vi/global.json,
    angapp/src/main/webapp/i18n/vi/login.json,
    angapp/src/main/webapp/i18n/vi/home.json,
    angapp/src/main/webapp/i18n/vi/error.json,
    angapp/src/main/webapp/i18n/vi/user-management.json,
    angapp/src/main/webapp/i18n/vi/adminAuthority.json,
    angapp/src/main/webapp/i18n/vi/organization.json,
    angapp/src/main/webapp/i18n/vi/department.json
  </read_first>
  <action>
Create `frontend/src/app/config/translation.config.ts` with:
- `export const translationNotFoundMessage = 'translation-not-found';`
- `MissingTranslationHandlerImpl` that returns ``${translationNotFoundMessage}[${key}]``
- a loader helper built around `provideTranslateHttpLoader({ prefix: './i18n/', suffix: '.json' })`

Create `frontend/src/app/config/language.constants.ts` with:
- `export const LANGUAGE_DEFAULT = 'vi';`
- `export const LANGUAGE_FALLBACK = 'en';`
- `export const LANGUAGES = ['vi', 'en'] as const;`

Create `frontend/src/app/config/dayjs.ts` with explicit locale-pack registration:
- `import 'dayjs/locale/en';`
- `import 'dayjs/locale/vi';`
- an `applyDayjsLocale(language: string): void` helper that maps `vi` to `dayjs.locale('vi')`, `en` to `dayjs.locale('en')`, and falls back to `en`

The locale-pack imports are required; do not assume `dayjs.locale('vi')` works without importing the locale modules first.

Create `frontend/public/i18n/en.json` and `frontend/public/i18n/vi.json` by merging the donor namespaces from:
- `global.json`
- `login.json`
- `home.json`
- `error.json`
- `user-management.json`
- `adminAuthority.json`
- `activate.json`
- `password.json`
- `register.json`
- `reset.json`
- `settings.json`
- `sessions.json`
- `organization.json`
- `department.json`

Preserve the donor translation contract exactly:
- keep donor top-level namespaces such as `global`, `login`, `home`, `error`, `userManagement`, `activate`, `password`, `register`, `reset`, `settings`, `sessions`, and `entity`
- keep `angappApp.adminAuthority.*`, `angappApp.organization.*`, and `angappApp.department.*` nested under `angappApp`, matching the donor JSON and `angapp/src/main/webapp/app/entities/entity.routes.ts`
- do not flatten those donor keys into top-level `adminAuthority.*`, `organization.*`, or `department.*`

Canonical contract for later plans:
- donor-backed migrated routes and templates must consume the donor hierarchy exactly (`userManagement.*`, `angappApp.adminAuthority.*`, `angappApp.organization.*`, `angappApp.department.*`)
- new shell-only keys introduced in this phase may live under app-specific namespaces such as `pageTitle.*`, `layout.*`, and `security.alert.*`
- backend `jhipstersec.*` alert headers are transport-only inputs and are normalized in Plan 06-02; they are not the canonical frontend translation contract

Add app-specific keys to both languages:
- `pageTitle.home`
- `pageTitle.login`
- `pageTitle.accessDenied`
- `pageTitle.notFound`
- `pageTitle.error`
- `pageTitle.securityRoles`
- `pageTitle.permissionMatrix`
- `pageTitle.rowPolicies`
- `error.backToHome`
- `layout.topbar.toggleMenu`
- `layout.topbar.toggleTheme`
- `layout.topbar.login`
- `layout.topbar.logout`
- `layout.language.en`
- `layout.language.vi`
- `layout.menu.security.main`
- `layout.menu.security.roles`
- `layout.menu.security.permissions`
- `layout.menu.security.rowPolicies`
- `layout.menu.entities.employee`
- `security.alert.secRole.created`
- `security.alert.secRole.updated`
- `security.alert.secRole.deleted`
- `security.alert.secPermission.created`
- `security.alert.secPermission.updated`
- `security.alert.secPermission.deleted`
- `security.alert.secRowPolicy.created`
- `security.alert.secRowPolicy.updated`
- `security.alert.secRowPolicy.deleted`
- `userManagement.placeholder.phase6`

Do not recreate JHipster's old namespace merge build step. The deliverable is two static bundles served from `frontend/public/i18n/`.

Create `frontend/src/app/config/translation.config.spec.ts` in Task 1, not Task 2, so the task's verification can run immediately after the translation loader, missing-translation handler, and static bundle contract exist. The spec must verify:
- `translationNotFoundMessage` formatting
- the configured static i18n path contract `./i18n/*.json`
- the missing-translation handler output for an unknown key
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <automated>npm --prefix frontend exec ng test -- --watch=false --include src/app/config/translation.config.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/config/translation.config.ts` exists and contains `translationNotFoundMessage = 'translation-not-found'`
    - `frontend/src/app/config/translation.config.spec.ts` exists
    - `frontend/src/app/config/language.constants.ts` exists and contains `LANGUAGE_DEFAULT = 'vi'`
    - `frontend/src/app/config/language.constants.ts` contains `LANGUAGE_FALLBACK = 'en'`
    - `frontend/src/app/config/dayjs.ts` contains `dayjs/locale/en` and `dayjs/locale/vi`
    - `frontend/public/i18n/en.json` exists and contains `"global"`, `"login"`, `"userManagement"`, `"angappApp"`, `"pageTitle"`, and `"layout"`
    - `frontend/public/i18n/vi.json` exists and contains `"global"`, `"login"`, `"userManagement"`, `"angappApp"`, `"pageTitle"`, and `"layout"`
    - `frontend/public/i18n/en.json` contains `angappApp.adminAuthority.home.title`, `angappApp.organization.home.title`, and `angappApp.department.home.title`
    - `frontend/public/i18n/vi.json` contains `angappApp.adminAuthority.home.title`, `angappApp.organization.home.title`, and `angappApp.department.home.title`
    - `frontend/public/i18n/en.json` contains `"userManagement.placeholder.phase6"` or nested `"placeholder"` with `"phase6"`
    - `frontend/public/i18n/vi.json` contains `"userManagement.placeholder.phase6"` or nested `"placeholder"` with `"phase6"`
    - `frontend/angular.json` continues to serve the `public` folder as a build asset input
  </acceptance_criteria>
  <done>Static vi/en bundles and translation config exist, with canonical donor hierarchies preserved and app-specific shell or security keys ready for runtime wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Wire standalone translation bootstrap, locale precedence, and route titles</name>
  <files>
    frontend/src/app/app-page-title-strategy.ts,
    frontend/src/app.config.ts,
    frontend/src/app/app.ts,
    frontend/src/app/shared/language/translate.directive.ts,
    frontend/src/app/app-page-title-strategy.spec.ts,
    frontend/src/app/app.spec.ts
  </files>
  <read_first>
    frontend/src/app.config.ts,
    frontend/src/app/app.ts,
    frontend/src/app/core/auth/account.service.ts,
    frontend/src/app/core/auth/state-storage.service.ts,
    frontend/src/app/shared/language/translate.directive.ts,
    angapp/src/main/webapp/app/shared/language/translate.directive.ts,
    aef-main/aef-main/src/app.config.ts,
    aef-main/aef-main/src/app-page-title-strategy.ts
  </read_first>
  <action>
Create `frontend/src/app/app-page-title-strategy.ts` that extends `TitleStrategy`, reads the built route title key, translates it through `TranslateService.get(...)`, and sets `document.title`. Use `pageTitle.home` as the fallback key when `buildTitle(routerState)` returns null.

Update `frontend/src/app.config.ts` to:
- keep `provideHttpClient(withInterceptorsFromDi())`
- add `provideTranslateService(...)` with the static JSON loader from `translation.config.ts`
- set `lang: LANGUAGE_DEFAULT`
- set `fallbackLang: LANGUAGE_FALLBACK`
- register `provideMissingTranslationHandler(MissingTranslationHandlerImpl)`
- register `{ provide: TitleStrategy, useClass: AppPageTitleStrategy }`

Create `frontend/src/app/shared/language/translate.directive.ts` by adapting the donor `jhiTranslate` directive to Angular standalone usage. Keep the selector `[jhiTranslate]`, support `translateValues`, and update DOM text when `onLangChange` or `onTranslationChange` emits.

Update `frontend/src/app/app.ts` to:
- keep `ApplicationConfigService.setEndpointPrefix(environment.SERVER_API_URL ?? '')`
- inject `TranslateService`, `AccountService`, `StateStorageService`, `Router`, and `AppPageTitleStrategy`
- resolve the startup language in this exact order: `stateStorageService.getLocale()` -> `LANGUAGE_DEFAULT`
- call `translateService.use(initialLanguage)` on startup
- update `document.documentElement.lang` and `applyDayjsLocale(...)` whenever the active language changes
- call `appPageTitleStrategy.updateTitle(router.routerState.snapshot)` inside the language-change path so browser titles refresh immediately when the locale changes without waiting for navigation
- after `accountService.identity()` resolves, apply `account.langKey` only when `stateStorageService.getLocale()` is empty
- keep locale persistence in `sessionStorage`; do not move locale storage to `localStorage`

Create tests:
- `frontend/src/app/app-page-title-strategy.spec.ts` verifying translation-key titles resolve to document titles and refresh after `onLangChange`
- `frontend/src/app/app.spec.ts` as the root bootstrap spec verifying:
  - stored locale wins over both `LANGUAGE_DEFAULT` and `account.langKey`
  - `account.langKey` only applies when `stateStorageService.getLocale()` is empty
  - `document.documentElement.lang` updates with the active language
  - `applyDayjsLocale(...)` drives the active `dayjs.locale()` to `vi` or `en`
  </action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
    <automated>npm --prefix frontend exec ng test -- --watch=false --include src/app/config/translation.config.spec.ts --include src/app/app-page-title-strategy.spec.ts --include src/app/app.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app.config.ts` contains `provideTranslateService(`
    - `frontend/src/app.config.ts` contains `fallbackLang: LANGUAGE_FALLBACK`
    - `frontend/src/app.config.ts` contains `useClass: AppPageTitleStrategy`
    - `frontend/src/app/app-page-title-strategy.ts` exists and contains `buildTitle(routerState) ?? 'pageTitle.home'`
    - `frontend/src/app/shared/language/translate.directive.ts` exists and contains `selector: '[jhiTranslate]'`
    - `frontend/src/app/app.ts` contains `stateStorageService.getLocale()`
    - `frontend/src/app/app.ts` contains `document.documentElement.lang`
    - `frontend/src/app/app.ts` contains `applyDayjsLocale`
    - `frontend/src/app/app.ts` contains `appPageTitleStrategy.updateTitle(router.routerState.snapshot)`
    - `frontend/src/app/config/translation.config.spec.ts` exists
    - `frontend/src/app/app-page-title-strategy.spec.ts` exists
    - `frontend/src/app/app-page-title-strategy.spec.ts` contains an `onLangChange` title-refresh assertion or equivalent title-refresh test
    - `frontend/src/app/app.spec.ts` exists
    - `frontend/src/app/app.spec.ts` contains stored-locale precedence assertions and a `dayjs.locale()` assertion
  </acceptance_criteria>
  <done>The frontend boots with standalone translation providers, translated route titles, registered dayjs locale packs, and the required locale precedence behavior for vi/en.</done>
</task>

</tasks>

<verification>
- `npm --prefix frontend run build` passes
- `npm --prefix frontend exec ng test -- --watch=false --include src/app/config/translation.config.spec.ts --include src/app/app-page-title-strategy.spec.ts --include src/app/app.spec.ts` passes
</verification>

<success_criteria>
- `frontend/public/i18n/en.json` and `frontend/public/i18n/vi.json` exist with canonical donor hierarchies plus shell/title/security supplements
- `frontend/src/app.config.ts` wires ngx-translate for static `./i18n/*.json` loading with vi default and en fallback
- `frontend/src/app/app.ts` applies stored-locale precedence and updates html/dayjs language state through registered vi/en dayjs locale packs
- `AppPageTitleStrategy` is registered and browser titles refresh both on navigation and on language changes
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-parity-foundation/06-01-SUMMARY.md`
</output>
