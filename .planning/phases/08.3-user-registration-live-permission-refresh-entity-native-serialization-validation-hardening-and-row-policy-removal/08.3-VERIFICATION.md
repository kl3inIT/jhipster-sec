---
phase: 08.3-user-registration-live-permission-refresh-entity-native-serialization-validation-hardening-and-row-policy-removal
verified: 2026-03-28T03:59:18Z
status: passed
score: 6/6 truths verified
re_verification: true
---

# Phase 08.3: User Registration, Live Permission Refresh, Entity-Native Serialization, Validation Hardening, And Row Policy Removal Verification Report

**Phase Goal:** Restore missing self-registration in the standalone frontend, make authority changes take effect without logout or login, move the secured entity path toward typed entity-native flows, make JSON validation explicit and fail closed, and remove row policy completely from the product.
**Verified:** 2026-03-28T03:59:18Z
**Status:** passed
**Re-verification:** Yes - final verification included post-UAT fixes for activation-link handling, local Mailpit-backed registration mail delivery, the login footer copy regression, and the `SecureDataManagerImplTest` unit blocker.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The standalone frontend exposes a working public registration flow and activation path on top of the preserved backend registration contract. | VERIFIED | `frontend/src/app/app.routes.ts`, `frontend/src/app/app.routes.ts`, `frontend/src/app/pages/register/*`, and the new `frontend/src/app/pages/activate/*` route keep registration and activation outside the shell. `src/main/resources/templates/mail/activationEmail.html` now links to `/activate?key=...`, and `MailServiceIT` asserts that contract. |
| 2 | Authority changes take effect without logout or login across current-user account, menu, and secured-entity capability surfaces. | VERIFIED | `AcceptsGrantedAuthorities`, `SecurityJwtConfiguration`, and `MergedSecurityContextBridge` refresh authorities at request time, while the frontend invalidates navigation and capability caches. UAT Tests 5 and 6 passed, and the phase backend and frontend regression suites stayed green. |
| 3 | The secured entity path operates on typed entities internally while JSON stays confined to explicit HTTP edge adapters. | VERIFIED | `SecuredEntityJsonAdapter` isolates JSON parsing and serialization, while `SecureDataManagerImpl`, `OrganizationService`, `DepartmentService`, and `EmployeeService` operate on typed entity mutations. `SecureDataManagerImplTest` now passes after aligning create-path ordering with the security contract. |
| 4 | Secured JSON controllers reject malformed or unsafe payloads explicitly and fail closed without partial persistence or server crashes. | VERIFIED | `SecuredEntityPayloadValidator`, `SecuredEntityQueryVM`, and the protected entity resources enforce explicit boundary validation. Live curl verification returned `400` for unknown fields, non-object bodies, missing association ids, negative paging, and unsafe nested filters while organization and department counts stayed unchanged and `/api/account` still returned `200`. |
| 5 | Row policy is removed end to end across schema, runtime, admin APIs, frontend navigation, and tests. | VERIFIED | The row-policy removal migration is active, row-policy backend/frontend code is gone, UAT Test 9 passed, and both `rg` scans for `SecRowPolicy`, `RowLevelPolicyProviderDbImpl`, `ROW_LEVEL`, `row-policies`, `rowPolicies`, `secRowPolicy`, `type.rowLevel`, and `jhipstersec.secRowPolicy` returned no matches. |
| 6 | Brownfield auth, account, admin, and mail flows remain green after the refactor and the late verification fixes. | VERIFIED | `AccountResourceIT`, `UserResourceIT`, `MailServiceIT`, the live registration UAT, the activation-link fix, and the local Mailpit dev wiring preserve the existing backend contract while making local verification viable. `08.3-UAT.md` now closes at 9/9 passed. |

**Score:** 6/6 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `frontend/src/app/pages/register/register.component.ts` | Standalone public registration flow over `/api/register` | VERIFIED | Registration page logic, duplicate-account handling, and success state remain in place. |
| `frontend/src/app/pages/activate/activate.component.ts` | Public frontend activation flow for emailed activation links | VERIFIED | Added standalone activation route and page so activation no longer bounces through a protected backend-facing path. |
| `src/main/resources/templates/mail/activationEmail.html` | Activation email points at the frontend activation route | VERIFIED | Template now emits `${baseUrl}/activate?key=...`, and the mirrored test template matches it. |
| `src/main/resources/config/application-dev.yml` | Local frontend/mail defaults support registration verification | VERIFIED | Mail defaults use `SPRING_MAIL_HOST`/`SPRING_MAIL_PORT`, and `jhipster.mail.base-url` defaults to `http://localhost:4200`. |
| `src/main/docker/mailpit.yml` | Local SMTP capture for registration/reset emails | VERIFIED | Mailpit is part of the local stack and validated through the compose configs. |
| `src/main/java/com/vn/core/security/AcceptsGrantedAuthorities.java` | Request-time authority refresh seam | VERIFIED | Principal authority replacement contract exists and is wired into JWT conversion. |
| `src/main/java/com/vn/core/security/web/SecuredEntityJsonAdapter.java` | Typed secure flow with explicit JSON boundary | VERIFIED | JSON parsing and serialization are centralized here rather than leaking into the secured core. |
| `src/main/java/com/vn/core/security/web/SecuredEntityPayloadValidator.java` | Explicit fail-closed mutation and query validation | VERIFIED | Shared validator rejects malformed or unsafe secured payloads. |
| `src/main/resources/config/liquibase/changelog/20260327000200_drop_sec_row_policy.xml` | Row-policy schema retirement | VERIFIED | Active Liquibase drop migration remains part of the current chain. |
| `.planning/phases/08.3-user-registration-live-permission-refresh-entity-native-serialization-validation-hardening-and-row-policy-removal/08.3-UAT.md` | Final human-facing verification record | VERIFIED | UAT closed at 9/9 passed after the login footer fix. |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `frontend/src/app/pages/login/login.component.html` | `frontend/src/app/pages/register/register.component.ts` | public registration discovery | WIRED | The login footer links directly to `/register` and now uses distinct prompt/CTA copy. |
| `src/main/resources/templates/mail/activationEmail.html` | `frontend/src/app/pages/activate/activate.component.ts` | activation email deep link | WIRED | Activation emails now land on the public frontend route that calls the preserved backend activation API. |
| `SecurityJwtConfiguration` | `DomainUserDetailsService` | request-time authority refresh | WIRED | JWT conversion rebuilds authorities from the current user state instead of only the login-time token snapshot. |
| `OrganizationResource / DepartmentResource / EmployeeResource` | `SecuredEntityPayloadValidator` and `SecuredEntityJsonAdapter` | explicit boundary validation plus typed save/query flow | WIRED | Protected entity controllers validate the request shape, parse JSON at the edge, and delegate typed work to the secure service path. |
| `SecureDataManagerImpl` | `SecureMergeService` and `UnconstrainedDataManager` | typed create/update save path | WIRED | The create path now checks CRUD before touching the unconstrained manager, which restored the expected unit-proof ordering. |

---

## Verification Runs

- `./gradlew.bat test --tests "com.vn.core.security.data.SecureDataManagerImplTest" --tests "com.vn.core.service.security.CurrentUserMenuPermissionServiceTest"` passed under Java 25.
- `./gradlew.bat integrationTest -x test --tests "com.vn.core.web.rest.AccountResourceIT" --tests "com.vn.core.web.rest.MenuPermissionResourceIT" --tests "com.vn.core.web.rest.SecuredEntityCapabilityResourceIT" --tests "com.vn.core.web.rest.SecuredEntityEnforcementIT" --tests "com.vn.core.web.rest.UserResourceIT" --tests "com.vn.core.service.MailServiceIT"` passed under Java 25.
- `cd frontend && npm exec ng test -- --watch=false --include="src/app/pages/register/register.component.spec.ts" --include="src/app/pages/activate/activate.component.spec.ts" --include="src/app/pages/activate/activate.service.spec.ts" --include="src/app/pages/login/login.component.spec.ts" --include="src/app/core/auth/account.service.spec.ts" --include="src/app/layout/navigation/navigation.service.spec.ts" --include="src/app/pages/entities/shared/service/secured-entity-capability.service.spec.ts"` passed with 16/16 tests green.
- `docker compose -f src/main/docker/services.yml config` and `docker compose -f src/main/docker/app.yml config` passed for the Mailpit-backed local mail stack.
- Live curl verification passed for the protected JSON edge and fail-closed validation cases, including create/update/read on allowed organization and department flows plus `400` responses for malformed or unsafe payloads with no persistence side effects.
- `rg -n "SecRowPolicy|RowLevelPolicyProviderDbImpl|ROW_LEVEL|row-policies" src frontend` and `rg -n "rowPolicies|secRowPolicy|type\\.rowLevel|jhipstersec\\.secRowPolicy" frontend src` returned no matches.
- `08.3-UAT.md` completed at 9 passed, 0 issues, 0 pending.

---

## Requirements Coverage

`ROADMAP.md` defines Phase 08.3 requirements as `PH83-01` through `PH83-05`.

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| `PH83-01` | `08.3-01` | Standalone frontend registration over the preserved backend register flow. | SATISFIED | Public register route, register page, live registration UAT, activation route, Mailpit-backed dev mail flow, and `MailServiceIT`. |
| `PH83-02` | `08.3-02` | Same-token authority refresh across backend and frontend permission surfaces. | SATISFIED | Request-time JWT authority refresh, frontend cache invalidation, and UAT Tests 5 and 6. |
| `PH83-03` | `08.3-03` | Typed entity-native secure data flow behind explicit JSON adapters. | SATISFIED | `SecuredEntityJsonAdapter`, typed services and `SecureDataManagerImpl`, `SecureDataManagerImplTest`, and live curl read/update coverage. |
| `PH83-04` | `08.3-04` | Explicit fail-closed validation for secured JSON controllers and query payloads. | SATISFIED | `SecuredEntityPayloadValidator`, `SecuredEntityQueryVM` constraints, `SecuredEntityEnforcementIT`, and live curl validation checks. |
| `PH83-05` | `08.3-05` | Complete row-policy retirement. | SATISFIED | Liquibase drop migration, removed backend/frontend row-policy surfaces, source scans, and UAT Test 9. |

All five Phase 08.3 requirements are accounted for.

---

## Gaps Summary

No product gaps remain.

The only UAT issue found during the first verification pass was the duplicated register footer copy on the login screen. That was fixed by reusing the existing `global.messages.info.register.*` translations, locked with `login.component.spec.ts`, and folded into the final 9/9 UAT result.

---

_Verified: 2026-03-28T03:59:18Z_
_Verifier: Codex (final verification after UAT-driven fixes and shipping preflight reruns)_
