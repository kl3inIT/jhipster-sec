---
phase: 08-user-management-delivery
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - frontend/src/app/core/auth/user-route-access.service.spec.ts
  - frontend/e2e/user-management.spec.ts
autonomous: true
requirements:
  - UMGT-03

must_haves:
  truths:
    - "Saved authority changes are verified against a real downstream access outcome, not only a changed payload"
    - "Granting admin access to a user leads to an allowed admin route or menu outcome for that user"
    - "Removing admin access from a user leads to denied admin route or menu outcome for that user"
    - "The final phase gate runs the focused user-management specs plus the Playwright user-management smoke"
  artifacts:
    - path: "frontend/src/app/core/auth/user-route-access.service.spec.ts"
      provides: "Guard-level regression coverage for authority-driven route access"
      contains: "user management"
    - path: "frontend/e2e/user-management.spec.ts"
      provides: "Grant-then-allow and revoke-then-deny smoke on the affected user session"
      contains: "access"
  key_links:
    - from: "user-management.spec.ts"
      to: "/admin/users"
      via: "admin route access outcome after authority save"
      pattern: "/admin/users"
    - from: "user-route-access.service.spec.ts"
      to: "UserRouteAccessService"
      via: "authority-based allow or deny assertions"
      pattern: "UserRouteAccessService"
---

<objective>
Close the last Phase 8 requirement gap by proving persisted authority edits change real app access outcomes for the affected user, then run the focused final gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-user-management-delivery/08-VALIDATION.md
@.planning/phases/08-user-management-delivery/08-03-SUMMARY.md
@AGENTS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add guard-level regression coverage for authority-driven admin access</name>
  <files>
    frontend/src/app/core/auth/user-route-access.service.spec.ts
  </files>
  <read_first>
    frontend/src/app/core/auth/user-route-access.service.ts,
    frontend/src/app/core/auth/user-route-access.service.spec.ts,
    frontend/src/app/layout/navigation/navigation-registry.ts,
    frontend/src/app/core/auth/account.service.ts
  </read_first>
  <action>
    Extend `frontend/src/app/core/auth/user-route-access.service.spec.ts` with a focused Phase 8 regression that proves a changed authority set changes the route guard outcome for the admin user-management route.

    Add tests that:
    - allow `/admin/users` when the mocked account includes the required admin authority
    - deny or redirect `/admin/users` when the mocked account loses that authority

    Name at least one new test with the string `user management` so the coverage is easy to target and grep.
  </action>
  <verify>
    <automated>npm --prefix frontend exec ng test -- --watch=false --include src/app/core/auth/user-route-access.service.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/core/auth/user-route-access.service.spec.ts` contains `user management`
    - `frontend/src/app/core/auth/user-route-access.service.spec.ts` contains `/admin/users`
    - `npm --prefix frontend exec ng test -- --watch=false --include src/app/core/auth/user-route-access.service.spec.ts` exits 0
  </acceptance_criteria>
  <done>The route guard has direct regression coverage for authority-driven access to the admin user-management route.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the Playwright smoke to prove grant-then-allow and revoke-then-deny</name>
  <files>
    frontend/e2e/user-management.spec.ts
  </files>
  <read_first>
    frontend/e2e/user-management.spec.ts,
    frontend/e2e/security-comprehensive.spec.ts,
    frontend/playwright.config.ts,
    frontend/src/app/pages/admin/user-management/update/user-management-update.component.ts
  </read_first>
  <action>
    Extend `frontend/e2e/user-management.spec.ts` so it does not stop at save success.

    Add or refine one scenario that:
    - starts as an admin user
    - edits another user's authorities through the real `/admin/users/:login/edit` flow
    - persists a grant or revoke affecting admin access
    - switches to the affected user session
    - asserts a real downstream access outcome changes, such as `/admin/users` becoming allowed after a grant or denied after a revoke

    Keep the scenario deterministic with request mocking if needed, but the assertions must cover actual app access behavior after the save, not only the request payload.

    Use the final phase gate command:
    - run the focused list, detail, and update specs
    - run the route-access spec
    - run the user-management Playwright smoke
  </action>
  <verify>
    <automated>npm --prefix frontend exec ng test -- --watch=false --include src/app/pages/admin/user-management/list/user-management-list.component.spec.ts --include src/app/pages/admin/user-management/detail/user-management-detail.component.spec.ts --include src/app/pages/admin/user-management/update/user-management-update.component.spec.ts --include src/app/core/auth/user-route-access.service.spec.ts && npm --prefix frontend run build && npm --prefix frontend exec playwright test e2e/user-management.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/e2e/user-management.spec.ts` contains `access`
    - `frontend/e2e/user-management.spec.ts` contains `/admin/users`
    - `frontend/e2e/user-management.spec.ts` contains `grant`
    - `frontend/e2e/user-management.spec.ts` contains `deny`
    - `npm --prefix frontend exec ng test -- --watch=false --include src/app/pages/admin/user-management/list/user-management-list.component.spec.ts --include src/app/pages/admin/user-management/detail/user-management-detail.component.spec.ts --include src/app/pages/admin/user-management/update/user-management-update.component.spec.ts --include src/app/core/auth/user-route-access.service.spec.ts && npm --prefix frontend run build && npm --prefix frontend exec playwright test e2e/user-management.spec.ts` exits 0
  </acceptance_criteria>
  <done>The final smoke proves that saved authority changes alter real app access for the affected user and closes the last UMGT-03 gap.</done>
</task>
</tasks>

<verification>
- `npm --prefix frontend exec ng test -- --watch=false --include src/app/core/auth/user-route-access.service.spec.ts` passes
- `npm --prefix frontend exec ng test -- --watch=false --include src/app/pages/admin/user-management/list/user-management-list.component.spec.ts --include src/app/pages/admin/user-management/detail/user-management-detail.component.spec.ts --include src/app/pages/admin/user-management/update/user-management-update.component.spec.ts --include src/app/core/auth/user-route-access.service.spec.ts` passes
- `npm --prefix frontend run build && npm --prefix frontend exec playwright test e2e/user-management.spec.ts` passes
- the affected user session sees a changed admin access outcome after authority save
</verification>

<success_criteria>
Phase 8 now proves UMGT-03 end to end: role or authority edits are persisted and produce a real downstream change in application access for the affected user.
</success_criteria>

<output>
After completion, create `.planning/phases/08-user-management-delivery/08-04-SUMMARY.md`
</output>
