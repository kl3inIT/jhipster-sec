---
phase: 08-user-management-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/vn/core/repository/UserRepository.java
  - src/main/java/com/vn/core/service/UserService.java
  - src/main/java/com/vn/core/web/rest/UserResource.java
  - src/test/java/com/vn/core/web/rest/UserResourceIT.java
autonomous: true
requirements:
  - UMGT-01

must_haves:
  truths:
    - "GET /api/admin/users accepts optional query without changing the path or response body shape"
    - "Query matching is case-insensitive across login, email, firstName, and lastName"
    - "Existing pagination headers and allowed-sort guard remain intact"
    - "UserResourceIT proves default browse, query browse, and unsupported sort rejection"
  artifacts:
    - path: "src/main/java/com/vn/core/repository/UserRepository.java"
      provides: "Specification-enabled repository"
      contains: "JpaSpecificationExecutor<User>"
    - path: "src/main/java/com/vn/core/service/UserService.java"
      provides: "Optional-query managed-user browse overload"
      contains: "getAllManagedUsers(Pageable pageable, String query)"
    - path: "src/main/java/com/vn/core/web/rest/UserResource.java"
      provides: "Optional query parameter on GET /api/admin/users"
      contains: "@RequestParam(name = \"query\", required = false)"
    - path: "src/test/java/com/vn/core/web/rest/UserResourceIT.java"
      provides: "Integration coverage for query browse"
      contains: "getAllUsersWithQuery"
  key_links:
    - from: "UserResource.getAllUsers"
      to: "UserService.getAllManagedUsers(Pageable pageable, String query)"
      via: "optional request parameter forwarding"
      pattern: "query"
    - from: "UserService.getAllManagedUsers(Pageable pageable, String query)"
      to: "UserRepository.findAll(specification, pageable)"
      via: "JpaSpecificationExecutor"
      pattern: "Specification<User>"
---

<objective>
Add the narrow backend seam Phase 8 needs for search-as-you-type: an optional `query` parameter on the preserved admin browse endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-user-management-delivery/08-CONTEXT.md
@.planning/phases/08-user-management-delivery/08-RESEARCH.md
@AGENTS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Make the existing admin browse endpoint query-aware</name>
  <files>
    src/main/java/com/vn/core/repository/UserRepository.java,
    src/main/java/com/vn/core/service/UserService.java,
    src/main/java/com/vn/core/web/rest/UserResource.java
  </files>
  <read_first>
    src/main/java/com/vn/core/repository/UserRepository.java,
    src/main/java/com/vn/core/service/UserService.java,
    src/main/java/com/vn/core/web/rest/UserResource.java,
    src/main/java/com/vn/core/domain/User.java,
    src/main/java/com/vn/core/service/dto/AdminUserDTO.java,
    .planning/phases/08-user-management-delivery/08-RESEARCH.md
  </read_first>
  <action>
    In `UserRepository.java`, extend `JpaSpecificationExecutor<User>` without removing any existing methods.

    In `UserService.java`:
    - keep `getAllManagedUsers(Pageable pageable)` and make it delegate to `getAllManagedUsers(pageable, null)`
    - add `getAllManagedUsers(Pageable pageable, String query)`
    - add `buildManagedUserQuery(String query)` returning `cb.conjunction()` for null or blank query
    - when query has text, trim it, lowercase with `Locale.ROOT`, wrap as `%value%`, and OR-match `login`, `email`, `firstName`, and `lastName`
    - use `cb.coalesce(root.get("firstName"), "")` and `cb.coalesce(root.get("lastName"), "")`
    - execute `userRepository.findAll(buildManagedUserQuery(query), pageable).map(AdminUserDTO::new)`

    In `UserResource.java`:
    - update `getAllUsers(...)` to accept `@RequestParam(name = "query", required = false) String query`
    - keep the same `@GetMapping("/users")`
    - keep `onlyContainsAllowedProperties(pageable)` as the first guard
    - call `userService.getAllManagedUsers(pageable, query)`
    - keep `PaginationUtil.generatePaginationHttpHeaders(...)` and do not add a new `/search` endpoint
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.core.web.rest.UserResourceIT"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/vn/core/repository/UserRepository.java` contains `JpaSpecificationExecutor<User>`
    - `src/main/java/com/vn/core/service/UserService.java` contains `getAllManagedUsers(Pageable pageable, String query)`
    - `src/main/java/com/vn/core/service/UserService.java` contains `buildManagedUserQuery`
    - `src/main/java/com/vn/core/service/UserService.java` contains `coalesce(root.get("firstName"), "")`
    - `src/main/java/com/vn/core/service/UserService.java` contains `coalesce(root.get("lastName"), "")`
    - `src/main/java/com/vn/core/web/rest/UserResource.java` contains `@RequestParam(name = "query", required = false)`
    - `src/main/java/com/vn/core/web/rest/UserResource.java` does not contain a new `/search` mapping
    - `./gradlew test --tests "com.vn.core.web.rest.UserResourceIT"` exits 0
  </acceptance_criteria>
  <done>GET /api/admin/users supports optional query filtering without changing the endpoint path or response contract.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the query contract in UserResourceIT</name>
  <files>
    src/test/java/com/vn/core/web/rest/UserResourceIT.java
  </files>
  <read_first>
    src/test/java/com/vn/core/web/rest/UserResourceIT.java,
    src/main/java/com/vn/core/web/rest/UserResource.java
  </read_first>
  <action>
    Add or update tests in `UserResourceIT.java` for:
    - default browse still returning `X-Total-Count`
    - `GET /api/admin/users?sort=id,desc&query=...` matching login, email, firstName, and lastName
    - blank query like `query=   ` behaving like normal browse
    - unsupported sort like `sort=authorities,asc&query=test` returning HTTP 400

    Keep the existing non-admin authorization test and existing create/update/delete coverage.
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.core.web.rest.UserResourceIT"</automated>
  </verify>
  <acceptance_criteria>
    - `src/test/java/com/vn/core/web/rest/UserResourceIT.java` contains `getAllUsersWithQuery`
    - `src/test/java/com/vn/core/web/rest/UserResourceIT.java` contains `X-Total-Count`
    - `src/test/java/com/vn/core/web/rest/UserResourceIT.java` contains `authorities,asc`
    - `./gradlew test --tests "com.vn.core.web.rest.UserResourceIT"` exits 0
  </acceptance_criteria>
  <done>UserResourceIT now protects the query-enabled browse contract, headers, and sort guard.</done>
</task>
</tasks>

<verification>
- `./gradlew test --tests "com.vn.core.web.rest.UserResourceIT"` passes
- `GET /api/admin/users?sort=id,desc&query=john` returns 200 with `X-Total-Count`
- `GET /api/admin/users?sort=authorities,asc&query=john` returns 400
</verification>

<success_criteria>
Phase 8 search can use the preserved admin-user endpoint with a single optional query parameter and verified backend coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/08-user-management-delivery/08-01-SUMMARY.md`
</output>
