---
phase: 08.1-jmix-style-datamanager-core-alignment
plan: 03
subsystem: security/verification
tags: [security, regression, brownfield-safety, verification]
requires:
  - phase: 08.1-jmix-style-datamanager-core-alignment
    provides: internal DataManager seam and union-of-ALLOW permission semantics from plans 01 and 02
provides:
  - integration proof for protected-entity CRUD and menu union semantics
  - brownfield account and admin safety sweep under Java 25
  - written alignment note documenting Jmix-aligned vs intentionally project-specific behavior
affects:
  - phase-09-readiness
  - protected-entity-regressions
  - brownfield-safety-gates
tech-stack:
  added: []
  patterns:
    - end-to-end proof over proof-domain entities and preserved brownfield APIs
    - explicit attribute-grant seeding in tests to match deny-default attribute semantics
key-files:
  created:
    - .planning/phases/08.1-jmix-style-datamanager-core-alignment/08.1-ALIGNMENT.md
  modified:
    - src/test/java/com/vn/core/web/rest/SecuredEntityEnforcementIT.java
    - src/test/java/com/vn/core/web/rest/MenuPermissionResourceIT.java
key-decisions:
  - "Proof-entity enforcement coverage now seeds explicit attribute ALLOW rows in-test so verified payloads match the current deny-default attribute model."
  - "Phase 08.1 verification runs through Gradle's integrationTest task under Java 25 because this repository excludes *IT classes from the test task."
patterns-established:
  - "Union-of-ALLOW semantics are now pinned end to end at both the menu and protected-entity HTTP boundaries."
  - "Brownfield account and admin regressions remain guarded by focused integration sweeps after security-core refactors."
requirements-completed: [SC-03, SC-05, SC-06]
duration: 35 min
completed: 2026-03-26
---

# Phase 08.1 Plan 03: Regression And Alignment Summary

**Verified the Jmix-style internal split end to end, locked union-of-ALLOW semantics with integration coverage, and recorded the intentional project-specific boundaries that remain.**

## Performance

- **Duration:** 35 min
- **Completed:** 2026-03-26
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Replaced the mocked menu-permission integration test with a database-backed proof that one role's `ALLOW` still surfaces a menu even when another role contributes `DENY`.
- Extended protected-entity integration coverage to prove multi-authority `ALLOW` accumulation, create/update behavior after the internal DataManager split, and unchanged row-policy boundaries.
- Added `08.1-ALIGNMENT.md` documenting what is now Jmix-aligned versus what remains intentionally project-specific for brownfield safety.
- Ran the brownfield safety sweep under Java 25 across `AccountResourceIT`, `UserResourceIT`, `SecuredEntityEnforcementIT`, and `MenuPermissionResourceIT`.

## Verification

- `JAVA_HOME=C:\Users\admin\.jdks\temurin-25.0.2 ./gradlew integrationTest --tests "com.vn.core.web.rest.SecuredEntityEnforcementIT" --tests "com.vn.core.web.rest.MenuPermissionResourceIT"` passed
- `JAVA_HOME=C:\Users\admin\.jdks\temurin-25.0.2 ./gradlew integrationTest --tests "com.vn.core.web.rest.AccountResourceIT" --tests "com.vn.core.web.rest.UserResourceIT" --tests "com.vn.core.web.rest.SecuredEntityEnforcementIT" --tests "com.vn.core.web.rest.MenuPermissionResourceIT"` passed

## Files Created/Modified

- `.planning/phases/08.1-jmix-style-datamanager-core-alignment/08.1-ALIGNMENT.md` - recorded Jmix-aligned behavior, intentional project-specific boundaries, and the exact brownfield verification commands.
- `src/test/java/com/vn/core/web/rest/MenuPermissionResourceIT.java` - converted the menu proof to a real integration path using persisted `SecMenuPermission` rows and multi-authority assertions.
- `src/test/java/com/vn/core/web/rest/SecuredEntityEnforcementIT.java` - added union-of-ALLOW coverage and explicit attribute-grant seeding for read/create/update proofs under the current deny-default attribute model.

## Decisions Made

- Kept the verification honest to current runtime behavior by seeding explicit attribute `ALLOW` rows in the proof tests instead of weakening the serializer or merge assertions.
- Recorded the real Gradle entrypoint used for verification (`integrationTest`) because the repository intentionally excludes `*IT` classes from the `test` task.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Adjusted verification commands to `integrationTest`**
- **Found during:** Task 2 brownfield safety sweep
- **Issue:** The plan specified `./gradlew test --tests ...`, but this repository excludes `*IT` classes from the `test` task in `gradle/spring-boot.gradle`, so the command cannot execute these integration suites.
- **Fix:** Ran the exact same class filters through `./gradlew integrationTest --tests ...` under Java 25 and documented that deviation in the alignment note and summary.
- **Files modified:** `.planning/phases/08.1-jmix-style-datamanager-core-alignment/08.1-ALIGNMENT.md`
- **Verification:** Both focused and brownfield integration sweeps passed.

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Verification completed successfully without changing product behavior; only the Gradle task entrypoint differed from the original plan text.

## Issues Encountered

- The shell defaulted to Java 17 at first, but this project requires Java 25 for Gradle evaluation. Verification ran with `JAVA_HOME=C:\Users\admin\.jdks\temurin-25.0.2`.
- The new union-of-ALLOW model exposed that proof-entity integration coverage needed explicit attribute `ALLOW` rows for visible and editable fields. The tests were updated to seed those rows directly so they reflect the current runtime contract.

## User Setup Required

None.

## Next Phase Readiness

- Phase 08.1 is complete and verified.
- Phase 9 can now start from a security core that is both structurally closer to Jmix and pinned by end-to-end regression coverage.

## Self-Check: PASSED

---
*Phase: 08.1-jmix-style-datamanager-core-alignment*
*Completed: 2026-03-26*
