---
phase: 08.1-jmix-style-datamanager-core-alignment
plan: 02
type: execute
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - src/main/java/com/vn/core/security/permission/RolePermissionServiceDbImpl.java
  - src/main/java/com/vn/core/security/permission/AttributePermissionEvaluatorImpl.java
  - src/main/java/com/vn/core/service/security/CurrentUserMenuPermissionService.java
  - src/main/java/com/vn/core/service/security/SecuredEntityCapabilityService.java
  - src/main/java/com/vn/core/security/repository/SecPermissionRepository.java
  - src/test/java/com/vn/core/security/permission/RolePermissionServiceDbImplTest.java
  - src/test/java/com/vn/core/security/permission/AttributePermissionEvaluatorImplTest.java
  - src/test/java/com/vn/core/service/security/CurrentUserMenuPermissionServiceTest.java
  - src/test/java/com/vn/core/service/security/SecuredEntityCapabilityServiceTest.java
autonomous: true
requirements:
  - SC-04
  - SC-05

must_haves:
  truths:
    - "Entity, attribute, menu, and capability resource checks all use the same default-deny plus union-of-`ALLOW` rule"
    - "A `DENY` record no longer wipes out another authority's `ALLOW` for the same resource"
    - "Empty match sets still deny access"
    - "Wildcard attribute grants remain aligned between live enforcement and capability payload generation"
  artifacts:
    - path: "src/main/java/com/vn/core/security/permission/RolePermissionServiceDbImpl.java"
      provides: "Entity/resource permission union logic"
      contains: "anyMatch(p -> \"ALLOW\".equals(p.getEffect()))"
    - path: "src/main/java/com/vn/core/security/permission/AttributePermissionEvaluatorImpl.java"
      provides: "Attribute union logic with wildcard alignment"
      contains: "wildcardTarget"
    - path: "src/main/java/com/vn/core/service/security/CurrentUserMenuPermissionService.java"
      provides: "Menu visibility union logic"
      contains: "distinct()"
    - path: "src/main/java/com/vn/core/service/security/SecuredEntityCapabilityService.java"
      provides: "Capability payloads aligned to union semantics"
      contains: "allowedKeys"
  key_links:
    - from: "RolePermissionServiceDbImpl"
      to: "CrudEntityConstraint"
      via: "entity CRUD permission lookup"
      pattern: "isEntityOpPermitted"
    - from: "AttributePermissionEvaluatorImpl"
      to: "SecureEntitySerializerImpl / SecureMergeServiceImpl"
      via: "attribute visibility and edit enforcement"
      pattern: "canView"
    - from: "CurrentUserMenuPermissionService"
      to: "MenuPermissionResource"
      via: "current-user visible menu ids"
      pattern: "getAllowedMenuIds"
---

<objective>
Migrate every resource-permission hot spot to the same default-deny plus union-of-`ALLOW` semantics so the secure core, menu visibility, and capability payloads stop disagreeing with each other.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.1-jmix-style-datamanager-core-alignment/08.1-CONTEXT.md
@.planning/phases/08.1-jmix-style-datamanager-core-alignment/08.1-RESEARCH.md
@AGENTS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Replace deny-wins resource merges with union-of-`ALLOW` logic</name>
  <files>
    src/main/java/com/vn/core/security/permission/RolePermissionServiceDbImpl.java,
    src/main/java/com/vn/core/security/permission/AttributePermissionEvaluatorImpl.java,
    src/main/java/com/vn/core/security/repository/SecPermissionRepository.java,
    src/test/java/com/vn/core/security/permission/RolePermissionServiceDbImplTest.java,
    src/test/java/com/vn/core/security/permission/AttributePermissionEvaluatorImplTest.java
  </files>
  <read_first>
    src/main/java/com/vn/core/security/permission/RolePermissionServiceDbImpl.java,
    src/main/java/com/vn/core/security/permission/AttributePermissionEvaluatorImpl.java,
    src/main/java/com/vn/core/security/permission/RolePermissionService.java,
    src/main/java/com/vn/core/security/repository/SecPermissionRepository.java,
    src/test/java/com/vn/core/security/permission/RolePermissionServiceDbImplTest.java,
    src/test/java/com/vn/core/security/permission/AttributePermissionEvaluatorImplTest.java,
    src/main/java/com/vn/core/service/security/SecuredEntityCapabilityService.java
  </read_first>
  <action>
    In `RolePermissionServiceDbImpl.java`:
    - keep empty-authority behavior as `false`
    - keep target normalization and repository lookup
    - remove the `hasDeny` short-circuit entirely
    - make `hasPermission(...)` return `true` only when the fetched list contains at least one `ALLOW`
    - keep empty fetched list as `false`
    - update class Javadoc to describe default-deny plus union-of-`ALLOW`, not deny-wins

    In `SecPermissionRepository.java`, add one repository helper that lets attribute evaluation read both an exact target and a wildcard target in one call:
    - `List<SecPermission> findByRolesAndTargets(Collection<String> authorityNames, TargetType targetType, Collection<String> targets, String action)`

    In `AttributePermissionEvaluatorImpl.java`:
    - keep empty-authority behavior as `false`
    - build `specificTarget = ENTITY.ATTRIBUTE` and `wildcardTarget = ENTITY.*`
    - fetch permissions for both targets
    - return `false` when the combined permission list is empty
    - return `true` when the combined permission list contains at least one `ALLOW`
    - do not short-circuit on `DENY`
    - update logging/Javadoc to reflect the new semantics

    In `RolePermissionServiceDbImplTest.java`, replace deny-wins assertions with:
    - multi-role allow + deny returns `true`
    - deny-only returns `false`
    - allow-only returns `true`
    - empty result returns `false`

    In `AttributePermissionEvaluatorImplTest.java`, replace deny-wins assertions with:
    - allow + deny returns `true`
    - wildcard allow returns `true`
    - deny-only returns `false`
    - empty result returns `false`
    - repository query includes both the specific and wildcard attribute targets
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.core.security.permission.RolePermissionServiceDbImplTest" --tests "com.vn.core.security.permission.AttributePermissionEvaluatorImplTest"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/vn/core/security/permission/RolePermissionServiceDbImpl.java` contains `anyMatch(p -> "ALLOW".equals(p.getEffect()))`
    - `src/main/java/com/vn/core/security/permission/RolePermissionServiceDbImpl.java` does not contain `hasDeny`
    - `src/main/java/com/vn/core/security/repository/SecPermissionRepository.java` contains `findByRolesAndTargets`
    - `src/main/java/com/vn/core/security/permission/AttributePermissionEvaluatorImpl.java` contains `wildcardTarget`
    - `src/main/java/com/vn/core/security/permission/AttributePermissionEvaluatorImpl.java` does not contain `hasDeny`
    - `src/test/java/com/vn/core/security/permission/RolePermissionServiceDbImplTest.java` contains `testAllowAndDenyStillReturnsTrue`
    - `src/test/java/com/vn/core/security/permission/AttributePermissionEvaluatorImplTest.java` contains `testWildcardAllowReturnsTrue`
    - `./gradlew test --tests "com.vn.core.security.permission.RolePermissionServiceDbImplTest" --tests "com.vn.core.security.permission.AttributePermissionEvaluatorImplTest"` exits 0
  </acceptance_criteria>
  <done>Entity and attribute permission checks now use the same union-of-`ALLOW` rule instead of deny-wins logic.</done>
</task>

<task type="auto">
  <name>Task 2: Align menu visibility and capability payloads to the same semantics</name>
  <files>
    src/main/java/com/vn/core/service/security/CurrentUserMenuPermissionService.java,
    src/main/java/com/vn/core/service/security/SecuredEntityCapabilityService.java,
    src/test/java/com/vn/core/service/security/CurrentUserMenuPermissionServiceTest.java,
    src/test/java/com/vn/core/service/security/SecuredEntityCapabilityServiceTest.java
  </files>
  <read_first>
    src/main/java/com/vn/core/service/security/CurrentUserMenuPermissionService.java,
    src/main/java/com/vn/core/service/security/SecuredEntityCapabilityService.java,
    src/test/java/com/vn/core/service/security/CurrentUserMenuPermissionServiceTest.java,
    src/main/java/com/vn/core/service/dto/security/SecuredEntityCapabilityDTO.java,
    src/main/java/com/vn/core/service/dto/security/SecuredAttributeCapabilityDTO.java,
    .planning/phases/08.1-jmix-style-datamanager-core-alignment/08.1-RESEARCH.md
  </read_first>
  <action>
    In `CurrentUserMenuPermissionService.java`:
    - keep empty-authority behavior returning `List.of()`
    - remove the denied-menu-id set logic completely
    - return the distinct, sorted set of menu ids whose permission rows include at least one `ALLOW`
    - keep the method signature unchanged

    In `SecuredEntityCapabilityService.java`:
    - update `PermissionMatrix.isEntityPermitted(...)` so it returns `true` when `allowedKeys` contains the entity key and `false` otherwise
    - remove entity-level deny-wins behavior
    - update `PermissionMatrix.isAttributePermitted(...)` so it returns `true` when either the specific key or wildcard key is present in `allowedKeys`
    - remove all `deniedKeys` handling from the matrix
    - keep empty match sets as denied
    - update the class-level comments so they describe union-of-`ALLOW`, not deny-wins

    In `CurrentUserMenuPermissionServiceTest.java`, replace the deny-wins expectation with:
    - allow rows across roles still union and sort
    - allow + deny for the same menu still returns the menu id because another role allows it
    - no-authority case still returns empty without repository access

    Create `SecuredEntityCapabilityServiceTest.java` to prove:
    - an entity capability becomes readable when one role allows READ even if another role denies READ
    - an attribute capability becomes visible/editable when one role allows either the exact target or the wildcard target
    - empty permission sets remain denied
    - the DTO list stays sorted by secured entity code
  </action>
  <verify>
    <automated>./gradlew test --tests "com.vn.core.service.security.CurrentUserMenuPermissionServiceTest" --tests "com.vn.core.service.security.SecuredEntityCapabilityServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/vn/core/service/security/CurrentUserMenuPermissionService.java` does not contain `deniedMenuIds`
    - `src/main/java/com/vn/core/service/security/CurrentUserMenuPermissionService.java` contains `ALLOW`
    - `src/main/java/com/vn/core/service/security/SecuredEntityCapabilityService.java` does not contain `deniedKeys`
    - `src/main/java/com/vn/core/service/security/SecuredEntityCapabilityService.java` contains `allowedKeys.contains`
    - `src/test/java/com/vn/core/service/security/CurrentUserMenuPermissionServiceTest.java` contains `allowsMenuWhenAnyAuthorityAllowsIt`
    - `src/test/java/com/vn/core/service/security/SecuredEntityCapabilityServiceTest.java` exists
    - `src/test/java/com/vn/core/service/security/SecuredEntityCapabilityServiceTest.java` contains `wildcard`
    - `./gradlew test --tests "com.vn.core.service.security.CurrentUserMenuPermissionServiceTest" --tests "com.vn.core.service.security.SecuredEntityCapabilityServiceTest"` exits 0
  </acceptance_criteria>
  <done>Menu visibility and secured capability payloads now match the same union-of-`ALLOW` behavior as the core permission evaluators.</done>
</task>
</tasks>

<verification>
- `./gradlew test --tests "com.vn.core.security.permission.RolePermissionServiceDbImplTest" --tests "com.vn.core.security.permission.AttributePermissionEvaluatorImplTest"` passes
- `./gradlew test --tests "com.vn.core.service.security.CurrentUserMenuPermissionServiceTest" --tests "com.vn.core.service.security.SecuredEntityCapabilityServiceTest"` passes
- `CurrentUserMenuPermissionService` no longer strips a menu because one role denies it
- `SecuredEntityCapabilityService.PermissionMatrix` no longer maintains `deniedKeys`
</verification>

<success_criteria>
Every resource-permission surface now answers access questions with the same union-of-`ALLOW` logic, so protected CRUD, menu visibility, and frontend capability payloads stay consistent.
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-jmix-style-datamanager-core-alignment/08.1-02-SUMMARY.md`
</output>
