---
phase: 01-identity-and-authority-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/java/com/vn/core/web/rest/AccountResourceIT.java
  - src/test/java/com/vn/core/web/rest/UserResourceIT.java
  - src/test/java/com/vn/core/web/rest/AuthenticateControllerIT.java
  - src/test/java/com/vn/core/web/rest/AuthorityResourceIT.java
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Existing login, register, activate, password reset, password change flows pass integration tests after any Phase 1 changes"
    - "Admin user CRUD and authority listing pass integration tests after any Phase 1 changes"
    - "Error cases (wrong password, unactivated account, duplicate email, bad activation key, bad reset key, wrong old password) are locked in as baseline assertions"
  artifacts:
    - path: "src/test/java/com/vn/core/web/rest/AccountResourceIT.java"
      provides: "Regression baseline for account lifecycle (AUTH-02)"
      contains: "testChangePasswordWithIncorrectCurrentPassword"
    - path: "src/test/java/com/vn/core/web/rest/UserResourceIT.java"
      provides: "Regression baseline for admin user management (AUTH-03)"
      contains: "testNonAdminCannotAccessAdminEndpoints"
    - path: "src/test/java/com/vn/core/web/rest/AuthenticateControllerIT.java"
      provides: "Regression baseline for login flow"
      contains: "testAuthorize"
    - path: "src/test/java/com/vn/core/web/rest/AuthorityResourceIT.java"
      provides: "Regression baseline for authority listing"
      contains: "shouldGetAuthority"
  key_links:
    - from: "src/test/java/com/vn/core/web/rest/AccountResourceIT.java"
      to: "/api/account/change-password"
      via: "MockMvc POST with wrong current password"
      pattern: "testChangePasswordWithIncorrectCurrentPassword"
    - from: "src/test/java/com/vn/core/web/rest/UserResourceIT.java"
      to: "/api/admin/users"
      via: "MockMvc GET without ROLE_ADMIN"
      pattern: "testNonAdminCannotAccessAdminEndpoints"
---

<objective>
Establish the regression test baseline that locks in current authentication, account lifecycle, and admin user management behavior before any structural changes.

Purpose: Per D-11, integration-level regression tests must be established before or at the very start of Phase 1 implementation work. They serve as the safety net for AUTH-02 (account lifecycle) and AUTH-03 (admin user management). The existing test suite has confirmed gaps that must be filled.

Output: Extended integration test files with gap coverage added, all passing against the current codebase.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-and-authority-baseline/1-CONTEXT.md
@.planning/phases/01-identity-and-authority-baseline/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and extend AccountResourceIT and AuthenticateControllerIT regression baseline</name>
  <files>
    src/test/java/com/vn/core/web/rest/AccountResourceIT.java,
    src/test/java/com/vn/core/web/rest/AuthenticateControllerIT.java
  </files>
  <read_first>
    src/test/java/com/vn/core/web/rest/AccountResourceIT.java,
    src/test/java/com/vn/core/web/rest/AuthenticateControllerIT.java,
    src/main/java/com/vn/core/web/rest/AccountResource.java,
    src/main/java/com/vn/core/web/rest/AuthenticateController.java,
    src/test/java/com/vn/core/IntegrationTest.java,
    src/main/java/com/vn/core/security/AuthoritiesConstants.java,
    .planning/phases/01-identity-and-authority-baseline/01-RESEARCH.md
  </read_first>
  <action>
Read the full AccountResourceIT.java and AuthenticateControllerIT.java to inventory existing test methods. Then fill the confirmed gaps per the research test gap map (D-12, D-13):

**AccountResourceIT gaps to fill:**

1. `testChangePasswordWithIncorrectCurrentPassword` — POST `/api/account/change-password` with a JSON body `{"currentPassword": "wrong-password", "newPassword": "new-valid-password123"}` while authenticated as the test user. Assert `status().isBadRequest()` (InvalidPasswordException maps to 400 via ExceptionTranslator). The test user's password is set during `@BeforeEach` setup; use a password value that does NOT match. Follow the existing `testChangePassword` method's pattern for MockMvc setup and authentication.

2. Verify that `testActivateAccountWithWrongKey` (or equivalent) exists and asserts `status().isInternalServerError()`. Per D-13 and CONCERNS.md, the 500 for bad activation keys is a known bug — the baseline must lock it in as-is, NOT fix it. If the test exists at approximately line 389, confirm the assertion. If it does not exist, add it: GET `/api/activate?key=WRONG_ACTIVATION_KEY`, assert `status().isInternalServerError()`.

3. Verify that password reset init (`POST /api/account/reset-password/init`) and password reset finish (`POST /api/account/reset-password/finish`) have test methods covering both happy path and error cases. The bad-reset-key test at approximately line 776 should assert `status().isInternalServerError()` (same known bug as activation). If any of these tests are missing, add them following the existing test patterns in the file.

**AuthenticateControllerIT gaps to verify:**

4. Verify `testAuthorize` (successful login) and `testAuthorizeFails` (wrong password) exist. If an unactivated-user rejection test is missing, add `testAuthorizeWithNotActivatedUser` — create a user in the database with `activated=false`, attempt login via POST `/api/authenticate` with `{"username": "notactivated", "password": "test-password"}`, and assert `status().isUnauthorized()` (401). Follow the existing test's pattern for user creation using the EntityManager.

**Implementation rules per locked decisions:**
- Per D-08: Do NOT modify AccountResource.java or AuthenticateController.java. Tests only.
- Per D-13: Cover happy paths plus key error cases. Do not exhaustively test every response field.
- Per D-04: No angapp classes. JHipster-native test patterns only.
- Use `@Transactional` on each test method (existing pattern in AccountResourceIT).
- Use the existing `@IntegrationTest` + `@AutoConfigureMockMvc` annotation stack.
- Use `AuthoritiesConstants.ADMIN` and `AuthoritiesConstants.USER` for authority strings.
  </action>
  <verify>
    <automated>./gradlew integrationTest --tests "*AccountResourceIT" --tests "*AuthenticateControllerIT" -x webpack 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - AccountResourceIT.java contains a method named `testChangePasswordWithIncorrectCurrentPassword`
    - AccountResourceIT.java contains assertion `isInternalServerError` for bad activation key test
    - AccountResourceIT.java contains assertion `isInternalServerError` for bad reset key test
    - AuthenticateControllerIT.java contains a test covering unactivated user login rejection (method name containing `NotActivated` or equivalent)
    - `./gradlew integrationTest --tests "*AccountResourceIT" --tests "*AuthenticateControllerIT"` exits 0
  </acceptance_criteria>
  <done>All account lifecycle and authentication regression tests pass, including the newly added wrong-old-password, bad-activation-key baseline, bad-reset-key baseline, and unactivated-user login rejection tests.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and extend UserResourceIT and AuthorityResourceIT regression baseline</name>
  <files>
    src/test/java/com/vn/core/web/rest/UserResourceIT.java,
    src/test/java/com/vn/core/web/rest/AuthorityResourceIT.java
  </files>
  <read_first>
    src/test/java/com/vn/core/web/rest/UserResourceIT.java,
    src/test/java/com/vn/core/web/rest/AuthorityResourceIT.java,
    src/main/java/com/vn/core/web/rest/UserResource.java,
    src/main/java/com/vn/core/web/rest/AuthorityResource.java,
    src/main/java/com/vn/core/config/SecurityConfiguration.java,
    src/main/java/com/vn/core/security/AuthoritiesConstants.java,
    .planning/phases/01-identity-and-authority-baseline/01-RESEARCH.md
  </read_first>
  <action>
Read the full UserResourceIT.java and AuthorityResourceIT.java to inventory existing test methods. Then fill the confirmed gaps per the research test gap map (D-12, D-13):

**UserResourceIT gaps to fill:**

1. `testNonAdminCannotAccessAdminEndpoints` — The class-level annotation is `@WithMockUser(authorities = AuthoritiesConstants.ADMIN)`. Add a test method that overrides this with `@WithMockUser(authorities = AuthoritiesConstants.USER)` (a regular user, not admin). Perform GET `/api/admin/users` and assert `status().isForbidden()` (403). This proves the admin endpoint authorization check works. Follow the existing MockMvc injection pattern in the class (`restUserMockMvc` or equivalent).

2. Verify that the existing test suite covers: create user (`testCreateUser` or similar), update user, delete user, and list users with pagination. Confirm these tests pass. If any are missing (unlikely based on research), add them following the existing patterns.

**AuthorityResourceIT gaps to fill:**

3. Verify that `shouldGetAuthority` or equivalent test exists for the authority listing endpoint. If a non-admin rejection test for `/api/authorities` is missing, add `testNonAdminCannotListAuthorities` — use `@WithMockUser(authorities = AuthoritiesConstants.USER)` and GET `/api/authorities`, assert `status().isForbidden()`. Check `SecurityConfiguration.java` and `AuthorityResource.java` to confirm the endpoint requires admin authority before writing the test — if the endpoint uses `@PreAuthorize("hasAuthority('ROLE_ADMIN')")` or the URL is under an admin-only path, the 403 assertion is correct.

**Implementation rules per locked decisions:**
- Per D-08: Do NOT modify UserResource.java, AuthorityResource.java, or SecurityConfiguration.java. Tests only.
- Per D-05: Do NOT touch jhi_authority table or Authority entity.
- Per D-06: Use existing `AuthoritiesConstants.ADMIN`, `AuthoritiesConstants.USER` constants.
- Use `@Transactional` on each test method (existing pattern).
- Use the existing `@IntegrationTest` + `@AutoConfigureMockMvc` annotation stack.
  </action>
  <verify>
    <automated>./gradlew integrationTest --tests "*UserResourceIT" --tests "*AuthorityResourceIT" -x webpack 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - UserResourceIT.java contains a method named `testNonAdminCannotAccessAdminEndpoints` (or closely equivalent name like `testForbiddenForNonAdminUser`)
    - That method contains `@WithMockUser` annotation with `AuthoritiesConstants.USER` or `"ROLE_USER"`
    - That method asserts `isForbidden` (403 status)
    - AuthorityResourceIT.java contains a non-admin rejection test if the endpoint requires ROLE_ADMIN
    - `./gradlew integrationTest --tests "*UserResourceIT" --tests "*AuthorityResourceIT"` exits 0
  </acceptance_criteria>
  <done>All admin user management and authority listing regression tests pass, including the newly added non-admin rejection tests that prove authorization is enforced on admin endpoints.</done>
</task>

</tasks>

<verification>
Run the full integration test suite to confirm no regressions:
```bash
./gradlew integrationTest -x webpack
```
All tests must pass (exit 0). No existing test behavior may change — only new test methods are added.
</verification>

<success_criteria>
- All existing integration tests continue to pass unchanged
- New gap-filling tests (wrong-old-password, non-admin rejection, unactivated login, bad-key baselines) pass
- The regression baseline is complete enough to catch regressions in AUTH-02 and AUTH-03 flows during subsequent Phase 1 work
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-and-authority-baseline/01-01-SUMMARY.md`
</output>
