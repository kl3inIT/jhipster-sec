---
phase: 12-production-runtime-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/config/application-prod.yml
  - src/main/docker/app.yml
  - src/main/docker/postgresql.yml
  - src/main/docker/services.yml
  - src/main/docker/jib/entrypoint.sh
autonomous: true
requirements:
  - PROD-01
must_haves:
  truths:
    - User can start the backend runtime with committed Docker Compose assets and environment inputs rather than machine-specific values.
    - The prod profile reads datasource and mail runtime values from environment-backed placeholders instead of hardcoded host, username, and password literals.
    - PostgreSQL and Mailpit are first-class runtime dependencies for the production-like stack, and backend readiness waits on healthy dependencies.
  artifacts:
    - path: src/main/resources/config/application-prod.yml
      provides: Environment-driven prod profile for datasource, Liquibase, mail host/port, and mail base URL.
      contains: SPRING_DATASOURCE_URL
    - path: src/main/docker/app.yml
      provides: Primary production-like stack entrypoint using the Jib-built backend image plus healthy dependencies.
      contains: condition: service_healthy
    - path: src/main/docker/postgresql.yml
      provides: Password-backed PostgreSQL service definition without trust auth.
      contains: POSTGRES_PASSWORD
    - path: src/main/docker/jib/entrypoint.sh
      provides: File-backed env resolution for runtime variables used by the prod-like stack.
      contains: JHIPSTER_MAIL_BASE_URL
  key_links:
    - from: src/main/docker/app.yml
      to: src/main/resources/config/application-prod.yml
      via: environment variables passed into Spring Boot
      pattern: SPRING_DATASOURCE_URL
    - from: src/main/docker/postgresql.yml
      to: src/main/docker/app.yml
      via: shared database name, user, and password env contract
      pattern: POSTGRES_PASSWORD
    - from: src/main/docker/jib/entrypoint.sh
      to: src/main/resources/config/application-prod.yml
      via: _FILE-aware env export before Java starts
      pattern: file_env 'JHIPSTER_MAIL_BASE_URL'
---

<objective>
Normalize the production-like runtime contract so Phase 12 uses committed Docker Compose assets plus environment-driven values instead of checked-in machine-specific production settings.

Purpose: Satisfy PROD-01 and D-01/D-02/D-04/D-06/D-08/D-11 by making the baseline stack portable, reproducible, and aligned with the existing Jib packaging path.
Output: A normalized prod profile and compose baseline that can be started on any machine with Docker and the committed repo assets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-production-runtime-foundation/12-CONTEXT.md
@.planning/phases/12-production-runtime-foundation/12-RESEARCH.md
@src/main/resources/config/application.yml
@src/main/resources/config/application-prod.yml
@src/main/docker/app.yml
@src/main/docker/services.yml
@src/main/docker/postgresql.yml
@src/main/docker/mailpit.yml
@src/main/docker/jib/entrypoint.sh
@buildSrc/src/main/groovy/jhipster.docker-conventions.gradle

<interfaces>
From src/main/resources/config/application.yml:
```yml
management:
  endpoint:
    health:
      probes:
        enabled: true
      group:
        readiness:
          include: readinessState,db
```

From buildSrc/src/main/groovy/jhipster.docker-conventions.gradle:
```groovy
jib {
  to {
    image = 'jhipster-sec:latest'
  }
  container {
    entrypoint = ['bash', '-c', '/entrypoint.sh']
    ports = ['8080', '5701/udp']
  }
}
```

From src/main/docker/jib/entrypoint.sh:
```bash
file_env 'SPRING_DATASOURCE_URL'
file_env 'SPRING_DATASOURCE_USERNAME'
file_env 'SPRING_DATASOURCE_PASSWORD'
file_env 'SPRING_LIQUIBASE_URL'
file_env 'SPRING_LIQUIBASE_USER'
file_env 'SPRING_LIQUIBASE_PASSWORD'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Externalize the prod-profile runtime contract</name>
  <read_first>
    src/main/resources/config/application-prod.yml
    src/main/resources/config/application.yml
    .planning/phases/12-production-runtime-foundation/12-CONTEXT.md
    .planning/phases/12-production-runtime-foundation/12-RESEARCH.md
  </read_first>
  <files>src/main/resources/config/application-prod.yml</files>
  <action>Replace the hardcoded datasource URL, datasource username, and datasource password in `src/main/resources/config/application-prod.yml` with environment-backed placeholders per D-01 and D-02. Keep `spring.datasource.type`, Hikari settings, and `spring.liquibase.contexts: prod`, but make the runtime contract explicit with `${SPRING_DATASOURCE_URL}`, `${SPRING_DATASOURCE_USERNAME}`, `${SPRING_DATASOURCE_PASSWORD}`, and matching Liquibase placeholders if a separate Liquibase user/password is needed. Keep mail host and port environment-driven, and keep `jhipster.mail.base-url` environment-driven via `JHIPSTER_MAIL_BASE_URL` rather than a localhost literal. Do not add fixed IPs, usernames, passwords, TLS rollout, real SMTP provider config, or reverse-proxy-specific hostnames; those are deferred by D-10 and must stay out of this phase.</action>
  <acceptance_criteria>
    - `src/main/resources/config/application-prod.yml` contains `${SPRING_DATASOURCE_URL}` and does not contain `157.230.42.136`.
    - `src/main/resources/config/application-prod.yml` contains `${SPRING_DATASOURCE_USERNAME}` and `${SPRING_DATASOURCE_PASSWORD}` and does not contain `admin123`.
    - `src/main/resources/config/application-prod.yml` contains `base-url: ${JHIPSTER_MAIL_BASE_URL:`.
    - `src/main/resources/config/application-prod.yml` still contains `contexts: prod` under `spring.liquibase`.
  </acceptance_criteria>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p = Path('D:/jhipster/src/main/resources/config/application-prod.yml').read_text()
assert '${SPRING_DATASOURCE_URL}' in p
assert '${SPRING_DATASOURCE_USERNAME}' in p
assert '${SPRING_DATASOURCE_PASSWORD}' in p
assert '157.230.42.136' not in p
assert 'admin123' not in p
assert 'base-url: ${JHIPSTER_MAIL_BASE_URL:' in p
print('application-prod env contract verified')
PY</automated>
  </verify>
  <done>The prod profile is portable, contains no machine-specific datasource secrets, and documents the exact env contract used by the production-like stack.</done>
</task>

<task type="auto">
  <name>Task 2: Align Compose and entrypoint files to one production-like env contract</name>
  <read_first>
    src/main/docker/app.yml
    src/main/docker/services.yml
    src/main/docker/postgresql.yml
    src/main/docker/mailpit.yml
    src/main/docker/jib/entrypoint.sh
    buildSrc/src/main/groovy/jhipster.docker-conventions.gradle
    src/main/resources/config/application-prod.yml
    .planning/phases/12-production-runtime-foundation/12-CONTEXT.md
    .planning/phases/12-production-runtime-foundation/12-RESEARCH.md
  </read_first>
  <files>src/main/docker/app.yml, src/main/docker/services.yml, src/main/docker/postgresql.yml, src/main/docker/jib/entrypoint.sh</files>
  <action>Update the Docker manifests to reuse the existing Jib image path per D-06 and to express the production-like runtime as backend + PostgreSQL + Mailpit per D-04. In `src/main/docker/postgresql.yml`, remove `POSTGRES_HOST_AUTH_METHOD=trust` and replace it with explicit database name, user, and password environment variables plus a health check that still uses `pg_isready`. In `src/main/docker/app.yml`, keep the backend image on the Jib-built image name, set `SPRING_PROFILES_ACTIVE=prod` unless a documented Phase 12 validation need requires `api-docs`, wire the same datasource and Liquibase values used by the prod profile, keep `SPRING_MAIL_HOST=mailpit` and `SPRING_MAIL_PORT=1025`, make `JHIPSTER_MAIL_BASE_URL` environment-driven, and keep `depends_on.condition: service_healthy` for PostgreSQL and Mailpit. Use the readiness endpoint for app health checks if the container already exposes it through Actuator. In `src/main/docker/services.yml`, keep dependency-only startup aligned with the same PostgreSQL and Mailpit service definitions. In `src/main/docker/jib/entrypoint.sh`, add `_FILE` support for any new env-driven runtime values introduced here, especially `JHIPSTER_MAIL_BASE_URL` and any Liquibase user/password split used in Task 1. Do not invent a second image build path, a frontend container, CI-only compose shape, or internet-exposed hardening.</action>
  <acceptance_criteria>
    - `src/main/docker/postgresql.yml` contains `POSTGRES_PASSWORD=` and does not contain `POSTGRES_HOST_AUTH_METHOD=trust`.
    - `src/main/docker/app.yml` contains `SPRING_PROFILES_ACTIVE=prod` or a documented Phase 12 equivalent and does not keep `http://localhost:4200` as the fixed mail base URL.
    - `src/main/docker/app.yml` contains `condition: service_healthy` for both `postgresql` and `mailpit`.
    - `src/main/docker/jib/entrypoint.sh` contains `file_env 'JHIPSTER_MAIL_BASE_URL'`.
    - `src/main/docker/app.yml` still references the existing backend image path rather than a new Dockerfile-based image name.
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f D:/jhipster/src/main/docker/app.yml config > /tmp/phase12-app-compose.txt && python - <<'PY'
from pathlib import Path
postgres = Path('D:/jhipster/src/main/docker/postgresql.yml').read_text()
app = Path('D:/jhipster/src/main/docker/app.yml').read_text()
entry = Path('D:/jhipster/src/main/docker/jib/entrypoint.sh').read_text()
assert 'POSTGRES_HOST_AUTH_METHOD=trust' not in postgres
assert 'POSTGRES_PASSWORD=' in postgres
assert 'condition: service_healthy' in app
assert 'http://localhost:4200' not in app
assert "file_env 'JHIPSTER_MAIL_BASE_URL'" in entry
print('compose env contract verified')
PY</automated>
  </verify>
  <done>The committed production-like stack expresses one portable env contract across Spring Boot, Compose, PostgreSQL, and the Jib entrypoint.</done>
</task>

</tasks>

<verification>
Run `docker compose -f src/main/docker/app.yml config` to verify the compose graph is valid, then verify `application-prod.yml` and the Docker files expose the same datasource and mail-base env contract.
</verification>

<success_criteria>
- `application-prod.yml` contains environment-backed datasource and mail-base placeholders instead of checked-in host or credentials.
- `postgresql.yml` no longer uses trust auth.
- `app.yml` expresses the production-like runtime shape as backend + PostgreSQL + Mailpit with healthy dependency waits.
- `jib/entrypoint.sh` supports file-backed resolution for the runtime values introduced by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/12-production-runtime-foundation/12-01-SUMMARY.md`
</output>
