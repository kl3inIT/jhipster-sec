---
phase: 12-production-runtime-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - package.json
  - scripts/phase12-stack-smoke.sh
  - scripts/phase12-prodlike-regression.mjs
  - .planning/phases/12-production-runtime-foundation/12-VALIDATION.md
autonomous: true
requirements:
  - PROD-02
must_haves:
  truths:
    - The production-like Compose stack is started from the committed runtime assets before PROD-02 validation runs.
    - Phase 12 has repeatable runtime-targeted checks that hit the live Compose-launched backend instead of only wrapping in-process integration suites.
    - Auth, account, admin-user, mail, and secured-runtime proof commands are documented as part of the Phase 12 validation contract.
  artifacts:
    - path: package.json
      provides: Root commands for Compose-backed Phase 12 runtime validation.
      contains: phase12:
    - path: scripts/phase12-stack-smoke.sh
      provides: Deterministic smoke check for the live production-like stack.
      contains: /management/health/readiness
    - path: scripts/phase12-prodlike-regression.mjs
      provides: Live-stack regression probe that mirrors brownfield-safe API and mail/runtime expectations.
      contains: process.env.PHASE12_BASE_URL
    - path: .planning/phases/12-production-runtime-foundation/12-VALIDATION.md
      provides: Validation map tied to the committed Phase 12 runtime commands.
      contains: phase12:stack:smoke
  key_links:
    - from: package.json
      to: scripts/phase12-stack-smoke.sh
      via: npm wrapper for stack startup and readiness proof
      pattern: phase12:stack:smoke
    - from: scripts/phase12-prodlike-regression.mjs
      to: src/main/docker/app.yml
      via: HTTP probes executed against the Compose-launched backend base URL
      pattern: PHASE12_BASE_URL
    - from: .planning/phases/12-production-runtime-foundation/12-VALIDATION.md
      to: scripts/phase12-stack-smoke.sh
      via: per-task verification map references the committed runtime command
      pattern: phase12:stack:smoke
---

<objective>
Add production-like validation commands that prove the Compose-launched stack preserves the brownfield-safe runtime behaviors required by PROD-02.

Purpose: Address the checker gap that Plan 02 only wrapped existing suites by adding live-stack validation against the finalized Phase 12 runtime contract from Plan 01.
Output: Committed Phase 12 wrapper commands plus runtime-targeted smoke and regression probes for the Compose-launched backend, database, and mail sink.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-production-runtime-foundation/12-CONTEXT.md
@.planning/phases/12-production-runtime-foundation/12-RESEARCH.md
@.planning/phases/12-production-runtime-foundation/12-VALIDATION.md
@.planning/phases/12-production-runtime-foundation/12-01-PLAN.md
@package.json
@src/main/docker/app.yml
@src/main/docker/mailpit.yml
@src/test/java/com/vn/core/web/rest/AccountResourceIT.java
@src/test/java/com/vn/core/web/rest/UserResourceIT.java
@src/test/java/com/vn/core/web/rest/SecuredEntityCapabilityResourceIT.java
@src/test/java/com/vn/core/web/rest/SecuredEntityEnforcementIT.java
@src/test/java/com/vn/core/service/MailServiceIT.java

<interfaces>
From package.json:
```json
{
  "scripts": {
    "app:up": "docker compose -f src/main/docker/app.yml up --wait",
    "java:docker:prod": "npm run java:docker -- -Pprod"
  }
}
```

From src/main/docker/app.yml expectations established by Plan 01:
```yml
services:
  app:
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    depends_on:
      postgresql:
        condition: service_healthy
      mailpit:
        condition: service_healthy
```

From validation targets that must be mirrored, not reimagined:
```text
AccountResourceIT
UserResourceIT
SecuredEntityCapabilityResourceIT
SecuredEntityEnforcementIT
MailServiceIT
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a Compose-backed stack smoke command for the finalized runtime contract</name>
  <read_first>
    .planning/phases/12-production-runtime-foundation/12-01-PLAN.md
    package.json
    src/main/docker/app.yml
    .planning/phases/12-production-runtime-foundation/12-VALIDATION.md
    .planning/phases/12-production-runtime-foundation/12-CONTEXT.md
  </read_first>
  <files>package.json, scripts/phase12-stack-smoke.sh, .planning/phases/12-production-runtime-foundation/12-VALIDATION.md</files>
  <action>Create a committed `scripts/phase12-stack-smoke.sh` helper and root `package.json` wrapper such as `phase12:stack:smoke` that uses the Plan 01 contract per D-01, D-04, D-06, and D-08: build the existing Jib image if needed, start `src/main/docker/app.yml`, wait for the live backend container, then assert the running stack through real runtime checks rather than test-suite indirection. The smoke command must at minimum prove the Compose-launched backend answers `/management/health/readiness` with database-backed readiness, the app container is healthy, PostgreSQL is healthy, and Mailpit is reachable on its compose service port. Add deterministic teardown behavior or a paired down command so repeated runs do not leave the stack in an unknown state. Update `12-VALIDATION.md` so PROD-02 validation references this committed runtime smoke command instead of ad hoc shell fragments. Do not add CI-lane orchestration, Dockerfile packaging, or a frontend container; keep the stack exactly backend + PostgreSQL + Mailpit per D-04.</action>
  <acceptance_criteria>
    - `package.json` contains a committed script named `phase12:stack:smoke`.
    - `scripts/phase12-stack-smoke.sh` checks `/management/health/readiness` on the Compose-launched backend and fails non-zero when the endpoint is not healthy.
    - The smoke helper verifies healthy/running state for `app`, `postgresql`, and `mailpit` from `src/main/docker/app.yml`.
    - `.planning/phases/12-production-runtime-foundation/12-VALIDATION.md` maps the Phase 12 runtime smoke step to `phase12:stack:smoke`.
  </acceptance_criteria>
  <verify>
    <automated>python - <<'PY'
import json
from pathlib import Path
pkg = json.loads(Path('D:/jhipster/package.json').read_text())
assert 'phase12:stack:smoke' in pkg['scripts']
smoke = Path('D:/jhipster/scripts/phase12-stack-smoke.sh').read_text()
assert '/management/health/readiness' in smoke
for token in ['postgresql', 'mailpit', 'app.yml']:
    assert token in smoke, token
validation = Path('D:/jhipster/.planning/phases/12-production-runtime-foundation/12-VALIDATION.md').read_text()
assert 'phase12:stack:smoke' in validation
print('phase12 stack smoke contract verified')
PY</automated>
  </verify>
  <done>The repo has one repeatable command that proves the live Compose-launched Phase 12 stack starts and reaches readiness using the committed production-like runtime contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add live-stack PROD-02 regression probes that mirror the brownfield-safe runtime flows</name>
  <read_first>
    src/test/java/com/vn/core/web/rest/AccountResourceIT.java
    src/test/java/com/vn/core/web/rest/UserResourceIT.java
    src/test/java/com/vn/core/web/rest/SecuredEntityCapabilityResourceIT.java
    src/test/java/com/vn/core/web/rest/SecuredEntityEnforcementIT.java
    src/test/java/com/vn/core/service/MailServiceIT.java
    package.json
    .planning/phases/12-production-runtime-foundation/12-VALIDATION.md
    .planning/phases/12-production-runtime-foundation/12-CONTEXT.md
    .planning/phases/12-production-runtime-foundation/12-RESEARCH.md
  </read_first>
  <files>package.json, scripts/phase12-prodlike-regression.mjs, .planning/phases/12-production-runtime-foundation/12-VALIDATION.md</files>
  <action>Create a committed runtime probe script, for example `scripts/phase12-prodlike-regression.mjs`, plus a root wrapper such as `phase12:backend:prodlike`, that talks to the live Compose-launched stack over HTTP and Mailpit APIs rather than rerunning only in-process Spring tests. Read the listed IT files first, then mirror their already-approved request paths and behavioral expectations into a small deterministic live-stack check set: authenticate against the real backend, confirm account lookup succeeds with the issued auth context, confirm at least one admin-user endpoint and one secured-entity endpoint still return the allow/deny behavior those tests cover, and confirm Mailpit receives mail for the exercised runtime flow or a dedicated mail trigger path proven by `MailServiceIT`. Use an explicit env-driven base URL such as `PHASE12_BASE_URL` with a default matching the Compose app port, and fail fast with clear diagnostics when any runtime behavior diverges. Update `12-VALIDATION.md` so PROD-02 coverage explicitly includes the new live-stack regression command plus the existing IT suite as supporting coverage, not as the only proof. Do not invent new business behavior, seed data models, or reverse-proxy assumptions; mirror the existing brownfield-safe flows per D-07 and D-11.</action>
  <acceptance_criteria>
    - `package.json` contains a committed script named `phase12:backend:prodlike`.
    - `scripts/phase12-prodlike-regression.mjs` reads `PHASE12_BASE_URL` and defaults to the Compose backend URL if unset.
    - The runtime probe covers auth/account, admin-user, secured-entity, and mail verification paths derived from the listed existing ITs.
    - `.planning/phases/12-production-runtime-foundation/12-VALIDATION.md` makes clear that `phase12:backend:prodlike` is a live-stack PROD-02 command and that the Spring ITs remain complementary coverage.
  </acceptance_criteria>
  <verify>
    <automated>python - <<'PY'
import json
from pathlib import Path
pkg = json.loads(Path('D:/jhipster/package.json').read_text())
assert 'phase12:backend:prodlike' in pkg['scripts']
probe = Path('D:/jhipster/scripts/phase12-prodlike-regression.mjs').read_text()
for token in ['PHASE12_BASE_URL', 'AccountResourceIT', 'UserResourceIT', 'SecuredEntityCapabilityResourceIT', 'SecuredEntityEnforcementIT', 'MailServiceIT']:
    assert token in probe, token
validation = Path('D:/jhipster/.planning/phases/12-production-runtime-foundation/12-VALIDATION.md').read_text()
assert 'phase12:backend:prodlike' in validation
print('phase12 live-stack regression contract verified')
PY</automated>
  </verify>
  <done>Phase 12 has a committed, live-stack PROD-02 regression command that exercises the Compose-launched runtime directly and ties each probe back to an existing brownfield-safe test target.</done>
</task>

</tasks>

<verification>
Run `npm run phase12:stack:smoke` first, then `npm run phase12:backend:prodlike`, and confirm `12-VALIDATION.md` treats those commands as the primary live-stack proof for Phase 12 runtime behavior.
</verification>

<success_criteria>
- Phase 12 has a repeatable smoke command that proves the Compose-launched stack reaches readiness.
- Phase 12 has a repeatable live-stack regression command that directly exercises auth, account, admin-user, mail, and secured-entity behavior against the running runtime.
- `12-VALIDATION.md` no longer implies that wrapper calls around existing in-process suites are sufficient by themselves for PROD-02.
</success_criteria>

<output>
After completion, create `.planning/phases/12-production-runtime-foundation/12-02-SUMMARY.md`
</output>
